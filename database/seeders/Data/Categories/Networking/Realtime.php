<?php

namespace Database\Seeders\Data\Categories\Networking;

class Realtime
{
    public static function all(): array
    {
        return [
            [
                'category' => 'Сети',
                'question' => 'Как работают WebSocket и чем отличаются от обычных HTTP-запросов?',
                'answer' => '**WebSocket** (RFC 6455) — полнодуплексный протокол двусторонней связи поверх **одного долгоживущего TCP-соединения**.

**Старт через HTTP Upgrade:**

1. Клиент шлёт `GET / HTTP/1.1` + `Connection: Upgrade` + `Upgrade: websocket` + `Sec-WebSocket-Key` (случайный nonce) + `Sec-WebSocket-Version: 13`.
2. Сервер отвечает `101 Switching Protocols` + `Sec-WebSocket-Accept` (SHA1 от nonce + magic string, base64).
3. Дальше тот же TCP уже **не HTTP** — это **WebSocket-фреймы**.

**Фрейминг:** каждое сообщение — один или несколько frame с `FIN`-битом, `opcode` (`text`/`binary`/`ping`/`pong`/`close`), маской (от клиента к серверу — всегда XOR-маска против cache poisoning через legacy-прокси), длиной и payload.

**Главные отличия от HTTP:**

- **Двунаправленный** — сервер инициирует push без запроса.
- **Долгоживущий** — одно соединение на минуты-часы.
- **Низкий overhead** — `2-14` байт заголовка vs `~500` байт HTTP-заголовков.
- Не идемпотентный — внутри один **поток с порядком сообщений**.

**Применение:** чаты, нотификации, биржевые тикеры, multiplayer-игры, collaborative editing.

**Минусы:**

- **Sticky session обязателен** (одно физическое соединение на один backend).
- Балансировщики **`L4` лучше `L7`** — некоторые L7 LB ломают upgrade.
- Нет встроенного **reconnect** (городить на клиенте) и явных **ack** (своё на уровне приложения).
- **`HTTP/2` multiplexing не используется** — каждый WebSocket держит свой TCP.

**Альтернативы:**

- **`SSE`** — если поток только от сервера.
- **`HTTP/2` streams**, **`gRPC` streaming**.
- **WebTransport** поверх **`HTTP/3`** (новое).',
                'difficulty' => 3,
                'topic' => 'networking.realtime',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое Server-Sent Events (SSE) и когда использовать вместо WebSocket?',
                'answer' => '**Server-Sent Events** (стандарт HTML5, MIME-type `text/event-stream`) — однонаправленный push от сервера к клиенту поверх **обычного HTTP**.

**Как работает:**

- Клиент открывает `new EventSource("/stream")`.
- Сервер держит соединение и шлёт текстовые блоки `data: {payload}\\n\\n` (двойной перенос строки — терминатор).
- Можно указывать `id` (возобновление через `Last-Event-ID` при reconnect), `event` (кастомное имя события), `retry` (рекомендация интервала reconnect).

**SSE vs WebSocket:**

| | **SSE** | **WebSocket** |
|---|---|---|
| Направление | сервер → клиент | двустороннее |
| Транспорт | обычный HTTP/1.1 или HTTP/2 | upgrade поверх TCP |
| Через прокси/CDN/firewall | работает везде | часто ломается |
| Auto-reconnect | **встроен** + `Last-Event-ID` | городить руками |
| HTTP/2 multiplexing | работает | нет |
| Формат | только текст (бинарь — base64) | text/binary |
| Балансировка | обычный `L7` nginx | sticky + специальные настройки |

**Когда SSE лучше:**

- Лента уведомлений, прогресс длительной операции.
- Live-цены, биржевые тикеры (без обратного канала).
- **Stream-ответ LLM** (ChatGPT, Anthropic API, OpenAI streaming).

**Когда WebSocket:** чат с двусторонней передачей, игры, collaborative editing.

**Лимит:** в `HTTP/1.1` браузер держит **6 SSE-коннектов на origin** — в `HTTP/2` мультиплексирование снимает проблему.',
                'difficulty' => 3,
                'topic' => 'networking.realtime',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое long polling и зачем он, если есть WebSocket?',
                'answer' => '**Long polling** — техника эмуляции push поверх обычных HTTP-запросов.

**Как работает:**

1. Клиент шлёт `GET /updates`.
2. Сервер **не отвечает сразу**, держит соединение открытым до 30-60 секунд:
    - Если за это время что-то изменилось — отдаёт ответ с данными, клиент сразу шлёт следующий long-poll.
    - Если ничего нет — по таймауту отвечает `204 No Content` или пустой массив, клиент шлёт следующий.
3. Получается имитация push: задержка от события до клиента — миллисекунды (соединение уже открыто).

**Сравнение:**

| | **Polling** | **Long polling** | **WebSocket** |
|---|---|---|---|
| Запросов | каждые `N` сек | один на ожидание | один на всё время |
| Латентность | до `N` сек | миллисекунды | миллисекунды |
| Транспорт | HTTP | HTTP | upgrade на TCP-фреймы |
| Через прокси/middleboxes | работает везде | работает везде | часто ломается |

**Когда long polling актуален в 2025:**

- Корпоративные сети с middleboxes, блокирующими WebSocket upgrade.
- **Fallback в библиотеках** — `Socket.IO`, `SignalR` пробуют WebSocket, потом откатываются.
- Webhook-стиль доставки на устройства за NAT.
- **Backend-to-backend** очереди — `Amazon SQS long-poll`.

**Минусы:**

- Каждый запрос — отдельный HTTP overhead.
- **Долгие коннекты держат FPM-воркеры в PHP** → быстро кладут прод. Нужен **Octane/Swoole/RoadRunner** или отдельный async backend.',
                'difficulty' => 3,
                'topic' => 'networking.realtime',
            ],
            [
                'category' => 'Сети',
                'question' => 'Чем webhook отличается от WebSocket и когда что использовать?',
                'answer' => 'Несмотря на схожие названия — это **разные паттерны**.

**Webhook** — обратный HTTP-запрос **«сервер → сервер»**. Вместо того чтобы поллить чужое API, внешний сервис сам шлёт `POST` на ваш URL, когда событие случилось.

- **Примеры:** Stripe (оплата прошла), GitHub (был push), Slack (новое сообщение), Twilio (звонок завершён).

**WebSocket** — постоянное двустороннее соединение **«клиент-браузер ↔ сервер»** для live UI: чаты, игры, нотификации.

**Сравнение:**

| | **Webhook** | **WebSocket** |
|---|---|---|
| Кто инициирует | внешний сервер | клиент-браузер |
| Кто принимает | ваш бэкенд | ваш бэкенд |
| Соединение | разовое HTTP `POST` | долгое TCP с фреймами |
| Направление | server → server (раз) | двустороннее (живёт постоянно) |
| Где работает | публичный URL вашего бэкенда | браузер пользователя |

**Когда что:**

- Интеграция с чужой бизнес-логикой (платежи, CI/CD, мессенджеры) → **webhook**.
- Real-time UI пользователя в браузере → **WebSocket / SSE**.
- Связать два своих сервиса асинхронно → **очередь сообщений** (Kafka, RabbitMQ, SQS) с гарантиями доставки, лучше webhook.

**Жизненный цикл webhook:**

1. Регистрируете **URL и секрет** у провайдера.
2. При событии провайдер шлёт `POST` с JSON и подписью (`HMAC-SHA256` в `X-Signature`).
3. Ваш сервер **валидирует подпись**, обрабатывает, отвечает `200`.
4. Если `5xx`/timeout — провайдер **ретраит с backoff** (часы, дни).

**Подводные камни webhook:**

- **Endpoint в интернете** — нужна валидация подписи, rate limit, защита от DDoS.
- **Идемпотентность** обязательна — провайдер может слать дубли при ретраях.
- **Порядок не гарантирован** — события приходят out-of-order.
- **Отвечать `200` быстро** — тяжёлую работу класть в очередь.',
                'difficulty' => 3,
                'topic' => 'networking.realtime',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое WebSocket простыми словами?',
                'answer' => '**WebSocket** — постоянное **двустороннее** соединение между браузером и сервером поверх одного TCP.

В отличие от обычного HTTP («один запрос → один ответ → соединение закрыто»), WebSocket остаётся открытым как «телефонный звонок» — сервер может в любой момент послать данные клиенту без запроса, и наоборот.

**Как открывается:**

- HTTP **Upgrade**-запрос с заголовками `Connection: Upgrade` и `Upgrade: websocket`.
- Дальше тот же TCP уже не HTTP, а **WebSocket-фреймы**.
- URL начинается с `ws://` (или `wss://` поверх TLS).

**Где используют:** чаты, live-нотификации, игры, биржевые тикеры, collaborative-редакторы.

**Минусы:**

- Нужен **sticky session** на балансировщике (одно физическое соединение к одному backend).
- Не работает HTTP-кэш.',
                'code_example' => "// JavaScript на клиенте\nconst ws = new WebSocket('wss://example.com/chat');\nws.onmessage = e => console.log(e.data);\nws.onopen = () => ws.send('hello');",
                'code_language' => 'javascript',
                'difficulty' => 1,
                'topic' => 'networking.realtime',
            ],
            [
                'category' => 'Сети',
                'question' => 'В чём разница между WebSocket и обычным HTTP-запросом простыми словами?',
                'answer' => '**`HTTP`** — короткий обмен **«один запрос → один ответ»**:

- Работает по **pull**: инициирует всегда клиент.
- Соединение можно закрыть после ответа.
- Ответы **кэшируются** прокси/CDN.

**WebSocket** — одно **длинное двустороннее соединение** поверх TCP:

- Старт через **HTTP Upgrade** (`Connection: Upgrade`, `Upgrade: websocket`).
- После upgrade обе стороны шлют сообщения **в любой момент** — сервер сам **пушит** клиенту без запроса.
- Overhead на сообщение — несколько байт (vs ~500 байт HTTP-заголовков).

**Минусы WebSocket:** соединение нужно держать, нужен **sticky session** на балансировщике, **нет кэша**, нет HTTP/2 multiplexing.

**Где что:**

- **HTTP** — классические API, страницы, формы.
- **WebSocket** — real-time UI: чаты, нотификации, биржевые тикеры, multiplayer-игры, collaborative-редакторы.',
                'difficulty' => 2,
                'topic' => 'networking.realtime',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое webhook простыми словами?',
                'answer' => '**Webhook** — **обратный звонок «сервер → сервер»**. Вместо того чтобы вы каждые 10 секунд спрашивали «есть ли новости?», внешний сервис сам шлёт вам `POST`-запрос с JSON, когда событие случилось.

**Жизненный цикл:**

1. Вы регистрируете **URL** и секретный ключ у провайдера.
2. Когда событие произошло, провайдер шлёт `POST` на этот URL с JSON-телом и подписью (`X-Signature` HMAC).
3. Вы валидируете подпись, обрабатываете, отвечаете `200`.
4. Если `5xx`/timeout — провайдер **ретраит** с backoff (часы, дни).

**Примеры:** **Stripe** — оплата прошла; **GitHub** — был push; **Telegram** — новое сообщение; **GitLab** — собрался pipeline.

**Требования к endpoint:**

- **Доступен из интернета** (никаких `localhost` без туннеля типа ngrok).
- **Быстро отвечает `200`** — тяжёлую обработку класть в очередь.
- **Идемпотентен** — могут прийти **дубли** при ретраях.',
                'difficulty' => 2,
                'topic' => 'networking.realtime',
            ],
            [
                'category' => 'Сети',
                'question' => 'В чём разница между polling и push?',
                'answer' => '- **Polling** — клиент **каждые N секунд** спрашивает «есть новости?». Простой, легко отлаживать, но **трафика много** (большая часть запросов пустых) и задержка от события до клиента до **N секунд**.
- **Push** — **сервер сам уведомляет клиента**, когда событие случилось. Эффективнее, задержка миллисекунды, но требует **постоянного открытого соединения**.
- **Long polling** — компромисс: клиент шлёт запрос, сервер **держит его открытым до события** или таймаута (30-60 сек), сразу отвечает данными — клиент тут же шлёт следующий long-poll. Минимум трафика без поддержки WebSocket.

**Реализации push:**

- **WebSocket** — двусторонний.
- **SSE** (Server-Sent Events) — push только от сервера к клиенту.
- **HTTP/2 push** — устарел, выпиливается из браузеров.

В современном вебе большинство «реалтайма» — это WebSocket или SSE; long polling остался как fallback и для backend-API типа Amazon SQS.',
                'difficulty' => 2,
                'topic' => 'networking.realtime',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое SSE (Server-Sent Events) простыми словами?',
                'answer' => '**SSE** — **однонаправленный поток** сообщений «сервер → браузер» поверх обычного HTTP. MIME-тип `text/event-stream`.

**Как работает:**

- Клиент один раз открывает `new EventSource(\'/stream\')`.
- Сервер держит соединение открытым и шлёт текстовые куски в формате `data: ...\\n\\n`.
- Браузер парсит и вызывает обработчик `onmessage`.

**Чем удобен:**

- Это **обычный HTTP** — работает через любые прокси, CDN, балансировщики.
- **Автореконнект** из коробки, `Last-Event-ID` позволяет догнать пропущенное.
- Легко отлаживать через `curl`.

**Где применяют:** ленты уведомлений, прогресс-бары длительных операций, **стриминг ответов LLM** (ChatGPT, Claude API).

**Минусы:** только в одну сторону — для отправки от клиента нужен отдельный `POST`. Бинарь — через base64.

Если нужна **двусторонняя** связь — берите WebSocket.',
                'code_example' => "// сервер: text/event-stream\n// data: {\"msg\":\"hello\"}\n//\n// data: {\"msg\":\"world\"}\n//\n\nconst es = new EventSource('/stream');\nes.onmessage = e => console.log(JSON.parse(e.data));",
                'code_language' => 'javascript',
                'difficulty' => 2,
                'topic' => 'networking.realtime',
            ],
        ];
    }
}
