<?php

namespace Database\Seeders\Data\Categories\Networking;

class Realtime
{
    public static function all(): array
    {
        return [
            [
                'category' => 'Сети',
                'question' => 'Как работают WebSocket и чем отличаются от обычных HTTP-запросов?',
                'answer' => 'WebSocket (RFC 6455) — полнодуплексный протокол двусторонней связи поверх одного долгоживущего TCP-соединения. Старт через HTTP Upgrade: клиент шлёт GET / HTTP/1.1 + Connection: Upgrade + Upgrade: websocket + Sec-WebSocket-Key (случайный nonce) + Sec-WebSocket-Version: 13. Сервер отвечает 101 Switching Protocols + Sec-WebSocket-Accept (SHA1 от nonce + magic string, base64), и дальше тот же TCP уже не HTTP — это WebSocket-фреймы. Фрейминг: каждое сообщение — это один или несколько frame с FIN-битом, opcode (text/binary/ping/pong/close), маской (от клиента к серверу всегда маскированы XOR-ключом — защита от cache poisoning через legacy-прокси), длиной и payload. Главные отличия от HTTP: 1) Двунаправленный — сервер инициирует push без запроса. 2) Долгоживущий — одно соединение на минуты-часы вместо коротких запросов. 3) Низкий overhead — 2-14 байт заголовка vs ~500 байт HTTP-заголовков. 4) Не идемпотентный — внутри один поток с порядком сообщений. Применение: чаты, нотификации, биржевые тикеры, multiplayer-игры, collaborative editing. Минусы: 1) Sticky session обязателен (одно физическое соединение на один backend). 2) Балансировщики L4 (а не L7) лучше — некоторые L7 LB ломают upgrade. 3) Нет встроенного reconnect-а (надо городить на клиенте) и явных ack-ов (надо своё на уровне приложения). 4) HTTP/2 multiplexing не используется. Альтернативы: SSE (если поток только от сервера), HTTP/2 streams, gRPC streaming, WebTransport (поверх HTTP/3).',
                'difficulty' => 3,
                'topic' => 'networking.realtime',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое Server-Sent Events (SSE) и когда использовать вместо WebSocket?',
                'answer' => 'Server-Sent Events (стандарт HTML5, MIME type text/event-stream) — простой механизм однонаправленного push от сервера к клиенту поверх обычного HTTP. Клиент открывает new EventSource("/stream"), сервер держит соединение открытым и шлёт текстовые сообщения в формате: "data: {payload}\\n\\n". Каждый блок данных терминируется двойным переносом строки. Можно указывать id (для возобновления через Last-Event-ID при reconnect), event (имя кастомного события), retry (рекомендация интервала retry в миллисекундах). Преимущества vs WebSocket: 1) Это обычный HTTP/1.1 или HTTP/2 — работает через любые прокси, CDN, firewall. 2) Браузер сам делает auto-reconnect при разрыве + автоматически шлёт Last-Event-ID — сервер может догнать клиента. 3) Простой текстовый протокол, легко дебажить через curl. 4) Работает с HTTP/2 multiplexing — несколько SSE-потоков по одному TCP. 5) Нет специальных балансировщиков — обычный L7 nginx без upgrade-логики. Минусы: 1) Только сервер → клиент. Для отправки от клиента — отдельный обычный POST/PUT. 2) Только текст — для бинаря нужен base64 (или WebSocket). 3) Старые Internet Explorer не поддерживают (актуально, если у вас legacy). 4) Лимит браузера на 6 SSE-коннектов на origin в HTTP/1.1 (в HTTP/2 — нет проблемы). Когда SSE лучше: лента уведомлений, прогресс длительной операции, live-цены, stream-ответ LLM (ChatGPT, Anthropic API streaming). Когда WebSocket: чат с двусторонней передачей, игры, collaborative editing — там SSE недостаточно.',
                'difficulty' => 3,
                'topic' => 'networking.realtime',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое long polling и зачем он, если есть WebSocket?',
                'answer' => 'Long polling — техника эмуляции push поверх обычных HTTP-запросов. Клиент шлёт GET /updates. Сервер НЕ отвечает сразу, а держит соединение открытым до 30-60 секунд: 1) Если за это время что-то изменилось — отдаёт ответ с новыми данными, клиент сразу шлёт следующий long-poll. 2) Если ничего нет — сервер по таймауту отвечает 204 No Content или пустой массив, клиент шлёт следующий запрос. Получается имитация push: задержка от события до клиента — миллисекунды (соединение уже открыто). Сравнение с обычным polling: обычный polling — клиент шлёт GET каждые 5 секунд, неэффективен (большая часть запросов пустых, новые данные ждут до 5 сек). Long polling экономит запросы и даёт почти push-семантику. Сравнение с WebSocket: WebSocket эффективнее (одно длинное соединение, фрейминг легкий), но требует upgrade-handshake, не везде проходит через прокси, требует sticky sessions. Long polling — обычный HTTP, работает ВЕЗДЕ, легко балансируется любым L7, легко отлаживается. Когда long polling актуален в 2025: 1) Корпоративные сети с агрессивными middleboxes, блокирующими WebSocket upgrade. 2) Простой fallback в библиотеках (Socket.IO, SignalR пробуют WebSocket первым, потом откатываются на long-polling). 3) Webhook-стиль доставки на устройства за NAT без постоянного коннекта. 4) Backend-to-backend long-poll задач в очереди (Amazon SQS long-poll). Минусы: 1) Каждый запрос — отдельный HTTP overhead. 2) Длинные коннекты держат FPM-воркеры в PHP — кладёт прод (нужен Octane/Swoole/RoadRunner или отдельный async backend).',
                'difficulty' => 3,
                'topic' => 'networking.realtime',
            ],
            [
                'category' => 'Сети',
                'question' => 'Чем webhook отличается от WebSocket и когда что использовать?',
                'answer' => 'Несмотря на схожие названия — это разные паттерны. Webhook — это обратный HTTP-запрос: вместо того, чтобы вы поллили чужое API («есть ли событие?»), внешний сервис сам делает POST на ваш URL, когда событие случилось. Это HTTP push «сервер → сервер». Stripe, GitHub, Slack, Twilio — все рассылают webhook\'и на зарегистрированные URL: «оплата прошла», «комментарий написан», «звонок завершён». Жизненный цикл: 1) Вы регистрируете URL и секрет. 2) Внешний сервис при событии шлёт POST с JSON-телом и подписью (HMAC-SHA256 заголовок типа X-Signature). 3) Ваш сервер валидирует подпись, обрабатывает, отвечает 200. 4) Если 5xx/timeout — внешний сервис ретраит с backoff (часы, дни). Идемпотентность критична — могут прийти дубли. WebSocket — постоянное двустороннее соединение «клиент-браузер ↔ сервер». Live-обновления для UI пользователя, чат, игры. Когда что: 1) Нужно интегрировать вашу систему с чужой бизнес-логикой (платежи, CI/CD, мессенджеры, CRM) — webhook. 2) Нужно показать пользователю real-time данные в браузере — WebSocket / SSE. 3) Связать два своих сервиса асинхронно — webhook тоже подходит, но обычно лучше очередь сообщений (Kafka, RabbitMQ, SQS) с гарантиями доставки. Подводные камни webhook: 1) Ваш endpoint доступен в интернет — DDoS, нужна валидация подписи, rate limit. 2) Идемпотентность — внешний сервис может ретраить (дубли). 3) Порядок не гарантирован (события приходят out of order). 4) Need 200 быстро — иначе ретраи; тяжёлая работа → kick to queue.',
                'difficulty' => 3,
                'topic' => 'networking.realtime',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое WebSocket простыми словами?',
                'answer' => '**WebSocket** — постоянное **двустороннее** соединение между браузером и сервером поверх одного TCP.

В отличие от обычного HTTP («один запрос → один ответ → соединение закрыто»), WebSocket остаётся открытым как «телефонный звонок» — сервер может в любой момент послать данные клиенту без запроса, и наоборот.

**Как открывается:**

- HTTP **Upgrade**-запрос с заголовками `Connection: Upgrade` и `Upgrade: websocket`.
- Дальше тот же TCP уже не HTTP, а **WebSocket-фреймы**.
- URL начинается с `ws://` (или `wss://` поверх TLS).

**Где используют:** чаты, live-нотификации, игры, биржевые тикеры, collaborative-редакторы.

**Минусы:**

- Нужен **sticky session** на балансировщике (одно физическое соединение к одному backend).
- Не работает HTTP-кэш.',
                'code_example' => "// JavaScript на клиенте\nconst ws = new WebSocket('wss://example.com/chat');\nws.onmessage = e => console.log(e.data);\nws.onopen = () => ws.send('hello');",
                'code_language' => 'javascript',
                'difficulty' => 1,
                'topic' => 'networking.realtime',
            ],
            [
                'category' => 'Сети',
                'question' => 'В чём разница между WebSocket и обычным HTTP-запросом простыми словами?',
                'answer' => '**`HTTP`** — короткий обмен **«один запрос → один ответ»**:

- Работает по **pull**: инициирует всегда клиент.
- Соединение можно закрыть после ответа.
- Ответы **кэшируются** прокси/CDN.

**WebSocket** — одно **длинное двустороннее соединение** поверх TCP:

- Старт через **HTTP Upgrade** (`Connection: Upgrade`, `Upgrade: websocket`).
- После upgrade обе стороны шлют сообщения **в любой момент** — сервер сам **пушит** клиенту без запроса.
- Overhead на сообщение — несколько байт (vs ~500 байт HTTP-заголовков).

**Минусы WebSocket:** соединение нужно держать, нужен **sticky session** на балансировщике, **нет кэша**, нет HTTP/2 multiplexing.

**Где что:**

- **HTTP** — классические API, страницы, формы.
- **WebSocket** — real-time UI: чаты, нотификации, биржевые тикеры, multiplayer-игры, collaborative-редакторы.',
                'difficulty' => 2,
                'topic' => 'networking.realtime',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое webhook простыми словами?',
                'answer' => '**Webhook** — **обратный звонок «сервер → сервер»**. Вместо того чтобы вы каждые 10 секунд спрашивали «есть ли новости?», внешний сервис сам шлёт вам `POST`-запрос с JSON, когда событие случилось.

**Жизненный цикл:**

1. Вы регистрируете **URL** и секретный ключ у провайдера.
2. Когда событие произошло, провайдер шлёт `POST` на этот URL с JSON-телом и подписью (`X-Signature` HMAC).
3. Вы валидируете подпись, обрабатываете, отвечаете `200`.
4. Если `5xx`/timeout — провайдер **ретраит** с backoff (часы, дни).

**Примеры:** **Stripe** — оплата прошла; **GitHub** — был push; **Telegram** — новое сообщение; **GitLab** — собрался pipeline.

**Требования к endpoint:**

- **Доступен из интернета** (никаких `localhost` без туннеля типа ngrok).
- **Быстро отвечает `200`** — тяжёлую обработку класть в очередь.
- **Идемпотентен** — могут прийти **дубли** при ретраях.',
                'difficulty' => 2,
                'topic' => 'networking.realtime',
            ],
            [
                'category' => 'Сети',
                'question' => 'В чём разница между polling и push?',
                'answer' => '- **Polling** — клиент **каждые N секунд** спрашивает «есть новости?». Простой, легко отлаживать, но **трафика много** (большая часть запросов пустых) и задержка от события до клиента до **N секунд**.
- **Push** — **сервер сам уведомляет клиента**, когда событие случилось. Эффективнее, задержка миллисекунды, но требует **постоянного открытого соединения**.
- **Long polling** — компромисс: клиент шлёт запрос, сервер **держит его открытым до события** или таймаута (30-60 сек), сразу отвечает данными — клиент тут же шлёт следующий long-poll. Минимум трафика без поддержки WebSocket.

**Реализации push:**

- **WebSocket** — двусторонний.
- **SSE** (Server-Sent Events) — push только от сервера к клиенту.
- **HTTP/2 push** — устарел, выпиливается из браузеров.

В современном вебе большинство «реалтайма» — это WebSocket или SSE; long polling остался как fallback и для backend-API типа Amazon SQS.',
                'difficulty' => 2,
                'topic' => 'networking.realtime',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое SSE (Server-Sent Events) простыми словами?',
                'answer' => '**SSE** — **однонаправленный поток** сообщений «сервер → браузер» поверх обычного HTTP. MIME-тип `text/event-stream`.

**Как работает:**

- Клиент один раз открывает `new EventSource(\'/stream\')`.
- Сервер держит соединение открытым и шлёт текстовые куски в формате `data: ...\\n\\n`.
- Браузер парсит и вызывает обработчик `onmessage`.

**Чем удобен:**

- Это **обычный HTTP** — работает через любые прокси, CDN, балансировщики.
- **Автореконнект** из коробки, `Last-Event-ID` позволяет догнать пропущенное.
- Легко отлаживать через `curl`.

**Где применяют:** ленты уведомлений, прогресс-бары длительных операций, **стриминг ответов LLM** (ChatGPT, Claude API).

**Минусы:** только в одну сторону — для отправки от клиента нужен отдельный `POST`. Бинарь — через base64.

Если нужна **двусторонняя** связь — берите WebSocket.',
                'code_example' => "// сервер: text/event-stream\n// data: {\"msg\":\"hello\"}\n//\n// data: {\"msg\":\"world\"}\n//\n\nconst es = new EventSource('/stream');\nes.onmessage = e => console.log(JSON.parse(e.data));",
                'code_language' => 'javascript',
                'difficulty' => 2,
                'topic' => 'networking.realtime',
            ],
        ];
    }
}
