<?php

namespace Database\Seeders\Data\Categories\Networking;

class Dns
{
    public static function all(): array
    {
        return [
            [
                'category' => 'Сети',
                'question' => 'Как работает DNS — что происходит после ввода example.com в браузере?',
                'answer' => '**`DNS`** — иерархический распределённый каталог, который превращает домен в `IP`.

**Цепочка после ввода `example.com`:**

1. **Кэш браузера** (Chrome держит ~60 сек).
2. **Кэш ОС** (`nscd`/`systemd-resolved`/`getaddrinfo` на Linux, `dscacheutil` на Mac).
3. **Recursive resolver** — обычно DNS провайдера или публичный (`1.1.1.1`, `8.8.8.8`).
4. Если у резолвера нет в кэше — он идёт по иерархии:
    - **Root-серверы** (`.`) → отдают `NS` зоны `.com`.
    - **TLD-серверы** `.com` → отдают `NS` для `example.com`.
    - **Authoritative** `example.com` → отдаёт финальную `A`/`AAAA`-запись с IP.
5. Каждый ответ кэшируется на время **`TTL`**.

**Типы записей:**

- `A` — `IPv4`, `AAAA` — `IPv6`.
- `CNAME` — алиас, `MX` — почта.
- `TXT` — SPF/DKIM/верификация.
- `NS` — делегирование, `SOA` — метаданные зоны.

**Защиты:**

- **`DNSSEC`** — цифровая **подпись ответов** (от подмены).
- **`DoH`** / **`DoT`** — **шифрование канала** к резолверу (от подслушивания).

**TTL trade-off:**

- Низкий `TTL` → быстрая смена `IP` при миграции, больше нагрузка на authoritative.
- Высокий `TTL` → меньше запросов, но миграция тянется сутки.

Проверить путь резолва без кэшей: **`dig +trace example.com`**.',
                'difficulty' => 3,
                'topic' => 'networking.dns',
            ],
            [
                'category' => 'Сети',
                'question' => 'Какие основные типы DNS-записей и для чего каждая?',
                'answer' => 'Основные типы DNS-записей, которые встретишь в продакшене.

**Адреса:**

- **`A`** — `IPv4`-адрес домена: `example.com → 93.184.216.34`.
- **`AAAA`** — `IPv6`-адрес: `example.com → 2606:2800:220:1:248:1893:25c8:1946`.

**Алиасы и делегирование:**

- **`CNAME`** (Canonical Name) — алиас: «`www.example.com` — псевдоним `example.com`», резолвер идёт резолвить указанный домен дальше. **Нельзя на apex** (`example.com` сам по себе) — отсюда `ALIAS`/`ANAME` у Cloudflare и Route 53.
- **`NS`** — делегирование зоны: для `example.com` `NS = ns1.example.com`.
- **`SOA`** — метаданные зоны (`serial`, `refresh`, `retry`, `expire` — для синхронизации primary/secondary NS).

**Почта:**

- **`MX`** (Mail Exchanger) — куда слать почту, с приоритетом (`mx1.example.com 10`, `mx2 20`).
- **`TXT`** — произвольная строка. Главные применения:
    - **`SPF`** — разрешённые отправители (`v=spf1 include:_spf.google.com ~all`).
    - **`DKIM`** — публичный ключ для подписи писем.
    - **`DMARC`** — политика обработки фейков.
    - **Верификация владельца** (Google/Microsoft/Stripe просят добавить `site-verification=...`).

**Прочее:**

- **`SRV`** — сервис, порт и приоритет (XMPP, SIP, LDAP, Kubernetes).
- **`PTR`** — обратная зона `IP → имя`. Нужна для **anti-spam**: почтовый сервер без `PTR` считается подозрительным.
- **`CAA`** — кто может выписывать `TLS`-сертификаты на домен (Let\'s Encrypt, DigiCert).',
                'code_example' => "dig example.com A +short\n# 93.184.216.34\ndig example.com MX +short\n# 10 mx1.example.com.\ndig example.com TXT +short\n# \"v=spf1 include:_spf.google.com ~all\"\ndig example.com NS +short\n# ns1.example.com.\n# ns2.example.com.",
                'code_language' => 'bash',
                'difficulty' => 3,
                'topic' => 'networking.dns',
            ],
            [
                'category' => 'Сети',
                'question' => 'В чём разница между recursive resolver и authoritative DNS server?',
                'answer' => 'Два разных типа DNS-серверов с противоположными ролями.

**Authoritative server** — **источник истины** для конкретной зоны. Содержит реальные записи (`A`, `MX`, `TXT`) и отвечает «вот данные для `example.com`».

- Их обычно **2-4 на зону** (`NS`-записи перечисляют их в родительской зоне).
- **Примеры:** AWS Route 53, Cloudflare DNS, NS1, `bind9` в self-hosted.
- Каждый — либо **primary** (тут редактируются записи), либо **secondary** (тянет копию через zone transfer `AXFR`/`IXFR`).
- **Не выполняет рекурсию** — отдаёт только свои записи или referral на `NS` другой зоны.

**Recursive resolver** — **клиент пользователя**. Получает запрос от приложения/ОС, идёт по иерархии (root → TLD → authoritative), кэширует и отдаёт ответ.

- **Никаких данных зоны не содержит** — только кэширует чужие ответы на `TTL`.
- **Примеры:** `1.1.1.1` (Cloudflare), `8.8.8.8` (Google), `9.9.9.9` (Quad9), DNS провайдера, локальный `systemd-resolved`, `dnsmasq`.

**Сравнение:**

| | **Authoritative** | **Recursive resolver** |
|---|---|---|
| Что хранит | реальные записи зоны | только кэш |
| Делает рекурсию | нет | да |
| Кто опрашивает | резолверы со всего мира | клиенты-приложения |
| Примеры | Route 53, Cloudflare DNS | `1.1.1.1`, `8.8.8.8` |

**Важное:**

- DNS-«бан» (домен в `RPZ`-блоклисте) — это политика на **resolver**, authoritative тут ни при чём.
- **Open recursive resolver** — уязвимость, используется для **DNS amplification** атак. Свой резолвер наружу не выставляют.',
                'difficulty' => 3,
                'topic' => 'networking.dns',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое TTL DNS, как он влияет на миграцию и почему «DNS propagation» долгая?',
                'answer' => '**`TTL`** (Time To Live) — поле каждой DNS-записи, **сколько секунд её разрешено кэшировать**.

Когда recursive resolver получил `A`-запись с `TTL=3600` — он отдаёт её всем своим клиентам ровно час и только потом снова идёт к authoritative.

**Trade-off:**

| `TTL` | Плюсы | Минусы |
|---|---|---|
| **Высокий** (24-48 ч) | меньше нагрузка на authoritative, быстрый ответ | миграция сервера — 2 дня боли, старый IP получает трафик |
| **Низкий** (60 сек) | быстрая миграция (за минуты) | больше queries, больше нагрузка на authoritative |

**Типовая стратегия миграции:**

1. За **1-2 дня** до переезда снизить `TTL` до 60 сек.
2. Дать существующим кэшам истечь.
3. Мигрировать (поменять `A`-запись).
4. После стабилизации **вернуть высокий `TTL`**.

**«DNS propagation» — миф в кавычках:**

- Сами записи никуда не «распространяются» — authoritative обновился **мгновенно**.
- А кэши resolvers по всему миру истекают **в разное время** — пользователь продолжает видеть старый IP, пока его resolver не перезапросит запись.

**Дополнительно:**

- Браузеры и ОС держат свой кэш **поверх** resolver (Chrome — 60 сек по умолчанию).
- Некоторые resolvers «прилипают» к ответу **больше указанного TTL** (нарушение RFC, но встречается).
- **CDN с GeoDNS** даёт разные IP разным регионам — это не propagation, а design.

**Проверить реальный путь без кэшей:** `dig +trace example.com`.',
                'difficulty' => 3,
                'topic' => 'networking.dns',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое DNSSEC, DoH и DoT и какую проблему каждый решает?',
                'answer' => 'Три разные защиты DNS, часто путают. DNSSEC (DNS Security Extensions) — цифровая ПОДПИСЬ записей. Authoritative-сервер хранит RRSIG-подписи рядом с записями, цепочка доверия идёт через DS-запись в родительской зоне до корня (.) — root KSK подписан публичными ключами IANA. Resolver валидирует подпись и отказывается отдавать невалидные записи. Защищает от: подмены ответов (DNS spoofing), отравления кэша (cache poisoning). НЕ защищает от подслушивания — запросы идут открытым текстом по UDP/53. DoH (DNS over HTTPS, RFC 8484) — DNS-запросы упакованы в HTTPS POST/GET на резолвер. Шифрует канал, скрывает запросы от провайдера, ходит по 443 порту (не блокируется наравне с обычным трафиком). Используется Firefox, Chrome, Apple, Cloudflare 1.1.1.1, Google 8.8.8.8. DoT (DNS over TLS, RFC 7858) — DNS поверх выделенного TLS-канала на порту 853. То же шифрование, но отдельный порт — легче администрировать (можно блокировать на корпоративном firewall). Используется Android Private DNS, AdGuard. DoH vs DoT: DoH сложнее блокировать (выглядит как обычный HTTPS), DoT прозрачнее для админа сети. Можно ли использовать все три? Да: DNSSEC валидирует authoritative-данные, DoH/DoT шифруют канал к resolver — это ортогональные защиты. На практике для пользователя DoH/DoT дают приватность, DNSSEC незаметно подстраховывает от подмен.',
                'difficulty' => 4,
                'topic' => 'networking.dns',
            ],
            [
                'category' => 'Сети',
                'question' => 'Почему CNAME нельзя на apex домена (example.com) и как это обходят?',
                'answer' => 'RFC 1034 запрещает CNAME сосуществовать с другими записями на одном имени: «If a CNAME RR is present at a node, no other data should be present». А apex-домен (example.com) ОБЯЗАН иметь NS-записи (делегирование), а часто и SOA, MX. Поэтому CNAME на apex физически нарушает стандарт. Это становится проблемой, когда нужно указать «example.com → CDN», у которого только DNS-имя (например, d111111abcdef8.cloudfront.net), а IP меняются. Обходы: 1) ALIAS / ANAME records (Cloudflare, Route 53, DNSimple) — собственное расширение провайдеров: внешне ведут себя как CNAME, но при ответе на запрос провайдер сам резолвит таргет и отдаёт A-записи. Стандартом не закреплено, но работает прозрачно для клиентов. 2) Flattening (Cloudflare CNAME Flattening) — то же самое: провайдер на лету заменяет CNAME на резолвленные A. 3) Жёсткий A-record — указать на статический IP CDN, но IP CDN могут меняться, и без согласования с провайдером можно потерять связность. Никогда не указывайте на чужой IP — всегда через имя. 4) www-домен с редиректом — apex отдаёт 301 redirect на www.example.com, у которого уже валидный CNAME на CDN. Старый паттерн, до сих пор используется. Современная рекомендация — DNS-провайдер с ALIAS-поддержкой.',
                'difficulty' => 4,
                'topic' => 'networking.dns',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое DNS простыми словами?',
                'answer' => '**DNS** — телефонная книга интернета. Превращает читаемое имя (`google.com`) в **IP-адрес** (`142.250.190.46`), к которому реально подключается компьютер — браузер по чистому имени никуда не пойдёт, ему нужен IP.

- Без DNS пришлось бы помнить IP всех сайтов наизусть и менять закладки при переезде сервера.
- Запрос — это маленький пакет (обычно **UDP**, на крупные ответы — TCP) на порт `53`.
- Ответ кэшируется на время **TTL** (Time To Live) в браузере, ОС и провайдерском DNS, поэтому каждый раз ходить в DNS не нужно.

Проверить можно командой `dig` или `nslookup`.',
                'code_example' => "dig +short google.com\n# 142.250.190.46\nnslookup google.com",
                'code_language' => 'bash',
                'difficulty' => 1,
                'topic' => 'networking.dns',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое A-запись и AAAA-запись DNS простыми словами?',
                'answer' => '- **`A`-запись** связывает доменное имя с **IPv4**-адресом: `example.com → 93.184.216.34`.
- **`AAAA`** («quad A») — то же самое, но для **IPv6**: `example.com → 2606:2800:220:1:248:1893:25c8:1946`. Четыре «A» отражают, что IPv6-адрес в 4 раза длиннее IPv4.

Обе настраиваются в панели DNS-провайдера (Cloudflare, Route 53, Namecheap).

- При резолвинге браузер обычно запрашивает оба типа параллельно и идёт по тому, что вернулся быстрее (**Happy Eyeballs**).
- У одного домена может быть несколько A-записей — это простой **round-robin** балансировки на DNS-уровне.',
                'code_example' => "dig example.com A +short\n# 93.184.216.34\ndig example.com AAAA +short\n# 2606:2800:220:1:248:1893:25c8:1946",
                'code_language' => 'bash',
                'difficulty' => 1,
                'topic' => 'networking.dns',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое CNAME-запись простыми словами?',
                'answer' => '**`CNAME`** (Canonical Name) — **алиас одного DNS-имени на другое имя**: `www.example.com` → `example.com`. При резолвинге `www` сначала превратится в `example.com`, потом в `IP`.

**Когда пригодится:**

- Поддомен указывает на основной домен: `www.example.com` → `example.com`.
- Поддомен указывает на чужой сервис: `shop.example.com` → `myshop.shopify.com`, `app.example.com` → `cname.vercel-dns.com`.
- IP меняется — правится одна `A`-запись на конечном имени, все `CNAME` следуют автоматически.

**Главное ограничение:** **`CNAME` нельзя ставить на apex** (корневой домен `example.com`), только на поддомены. Обход — `ALIAS`/`ANAME` (специфика провайдера: Cloudflare, Route 53).',
                'code_example' => "dig www.example.com +short\n# example.com.\n# 93.184.216.34",
                'code_language' => 'bash',
                'difficulty' => 2,
                'topic' => 'networking.dns',
            ],
            [
                'category' => 'Сети',
                'question' => 'Как руками проверить DNS-запись домена?',
                'answer' => 'Утилитой **`dig`** (Linux/Mac) или **`nslookup`** (Windows). Это must-have при отладке «почему сайт не открывается».

**Полезные варианты:**

- `dig example.com` — `A`-запись (IPv4).
- `dig example.com AAAA` — IPv6.
- `dig example.com MX` — почтовые серверы.
- `dig example.com TXT` — SPF/DKIM/верификации.
- `dig example.com NS` — authoritative-серверы зоны.
- `dig +short example.com` — только итог, без шума.
- `dig +trace example.com` — **полный путь резолва от корня без кэшей** (полезно, чтобы убедиться, что новая запись реально опубликована).
- `dig @1.1.1.1 example.com` — спросить у конкретного резолвера.',
                'code_example' => "dig +short example.com\n# 93.184.216.34\ndig example.com MX +short\n# 0 .\nnslookup example.com",
                'code_language' => 'bash',
                'difficulty' => 2,
                'topic' => 'networking.dns',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое TTL DNS простыми словами?',
                'answer' => '**`TTL`** (Time To Live) — сколько секунд DNS-запись разрешено держать в кэше. Сервер выдал `A`-запись с `TTL=3600` — значит ваш resolver и браузер будут использовать этот ответ **час**, не переспрашивая authoritative-сервер.

**Trade-off:**

- **Низкий TTL (60 сек):** быстрая смена IP при миграции, но больше нагрузка на DNS и резолверы.
- **Высокий TTL (24 часа):** меньше запросов, но миграция сервера превращается в сутки боли — старые IP продолжают получать трафик из чужих кэшей.

**Типовой сценарий миграции:** за день-два до переезда **снизить TTL до 60 сек**, дать старым кэшам истечь, мигрировать, после стабилизации вернуть высокий TTL.

«DNS propagation» — это и есть **постепенное истечение TTL** в кэшах резолверов по миру.',
                'difficulty' => 2,
                'topic' => 'networking.dns',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое DNS-резолвер простыми словами?',
                'answer' => '**Recursive resolver** — сервер, который **от имени клиента ищет ответ в DNS** и **кэширует** результат. Опрашивает по очереди корневые серверы → `TLD` (`.com`, `.ru`) → **authoritative** серверы зоны → отдаёт `A`/`AAAA`/`MX`/... обратно клиенту.

**По умолчанию:** резолвер вашего провайдера (`/etc/resolv.conf`, в роутере).

**Популярные публичные резолверы:**

- `1.1.1.1` — Cloudflare (быстрый, приватный).
- `8.8.8.8` / `8.8.4.4` — Google.
- `9.9.9.9` — Quad9 (блокирует malware-домены).

**Не путать с authoritative DNS:** резолвер только спрашивает и кэширует, **authoritative** — это источник истины, где реально лежат записи зоны (Cloudflare DNS, Route 53, ns1.example.com).',
                'difficulty' => 2,
                'topic' => 'networking.dns',
            ],
        ];
    }
}
