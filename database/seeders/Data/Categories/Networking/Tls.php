<?php

namespace Database\Seeders\Data\Categories\Networking;

class Tls
{
    public static function all(): array
    {
        return [
            [
                'category' => 'Сети',
                'question' => 'Как работает TLS-handshake (на примере TLS 1.3)?',
                'answer' => '**`TLS 1.3`** handshake занимает **`1 RTT`** (`TLS 1.2` — `2 RTT`, заметная разница для мобильных).

**Шаги:**

1. **`ClientHello`** — клиент шлёт:
    - Список поддерживаемых **cipher suites** (в 1.3 их всего 5: `TLS_AES_128_GCM_SHA256`, `TLS_AES_256_GCM_SHA384`, `TLS_CHACHA20_POLY1305_SHA256`, `TLS_AES_128_CCM_SHA256`, `TLS_AES_128_CCM_8_SHA256`).
    - **Группы для key exchange**: `X25519`, `P-256`.
    - **`SNI`** (какой домен — критично для multi-site хостинга).
    - **`ALPN`**: `http/1.1`, `h2`, `h3` — что готов говорить.
    - `random nonce`.
    - **КЛЮЧЕВАЯ ОПТИМИЗАЦИЯ 1.3** — сразу шлёт `keyshare` с `ECDHE`-public-key для предположенной группы.
2. **`ServerHello`** — сервер выбирает cipher suite и группу, отдаёт свой `ECDHE`-public-key. Этого достаточно, чтобы обе стороны вычислили **общий ключ** (Diffie-Hellman). Дальше всё **уже шифруется**.
3. **`Certificate` + `CertificateVerify` + `Finished`** от сервера:
    - `Certificate` — `X.509` + цепочка до промежуточного `CA` (без корня).
    - `CertificateVerify` — подпись handshake-данных приватным ключом.
4. **Валидация клиентом:**
    - Цепочка до доверенного root `CA` в своём хранилище.
    - Имя в сертификате против hostname (`SNI`).
    - Срок действия и `OCSP` (revocation).
    - Шлёт `Finished`.
5. **Application data** поехало.

**`0-RTT` (early data)** — для **повторных подключений**: клиент шлёт application data в первом же пакете, используя ключ из предыдущей сессии.

- **Плюс:** нулевая задержка на handshake.
- **Минус:** **`replay attack`** — атакующий может повторить тот же запрос.
- **Правило:** разрешать `0-RTT` **только для безопасных идемпотентных запросов** (`GET`).',
                'difficulty' => 5,
                'topic' => 'networking.tls',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое цепочка сертификатов, intermediate CA и почему один сертификат сам по себе не работает?',
                'answer' => 'Доверие в TLS строится на **иерархии**:

```
root CA → intermediate CA → leaf certificate (сертификат сервера)
```

**Уровни цепочки:**

- **Root CA.** Браузеры и ОС хранят список доверенных root — это «корни доверия». Выдаются раз в десятилетия, охраняются **offline HSM** и церемониями подписи.
- **Intermediate CA.** Root **не подписывает leaf напрямую** — root подписывает intermediate, а intermediate уже подписывает leaf-сертификаты сервисов.
- **Leaf** — конечный сертификат сервера (`example.com`).

**Зачем прокладка из intermediate:**

1. **Безопасность.** Intermediate живёт онлайн — его компрометация **не равна компрометации root**. При взломе его **отзывают** и выпускают новый без перевыпуска всех браузеров.
2. **Делегирование.** Let\'s Encrypt оперирует своими intermediate, но конечное доверие идёт через `ISRG Root X1` / `X2`.

**Правило выдачи цепочки сервером:** при handshake сервер **ОБЯЗАН** отдать `leaf + все intermediates` (chain bundle), но **НЕ root** (его уже знает клиент).

**Если intermediate забыт:**

- Современные браузеры пытаются достроить через **`AIA`** (Authority Information Access) URL внутри сертификата.
- Не все клиенты так умеют — особенно **мобильные SDK** и **старые Android**.
- Симптом: **«works on my machine»**, ломается на каких-то клиентах.

**Как проверить:**',
                'code_example' => "openssl s_client -connect example.com:443 -servername example.com -showcerts\n# смотреть на полную цепочку: leaf + intermediate(s)\n# Verify return code: 0 (ok) - всё ок\n# Verify return code: 21 - забыт intermediate\n\n# Альтернатива - онлайн:\n# https://www.ssllabs.com/ssltest/",
                'code_language' => 'bash',
                'difficulty' => 4,
                'topic' => 'networking.tls',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое SNI и зачем он нужен? Что такое ESNI/ECH?',
                'answer' => '**`SNI`** (Server Name Indication) — расширение TLS, где клиент в `ClientHello` указывает, **какой именно домен он хочет** (`server_name=example.com`).

**До `SNI`:** на одном IP мог жить **только один TLS-сайт** — сервер не знал, какой сертификат отдать, потому что `Host:` header идёт **после handshake**, уже зашифрованный.

**С `SNI`:** сервер видит запрашиваемый домен **ДО** handshake и выбирает правильный сертификат — на одном IP **тысячи HTTPS-сайтов**. Виртуальный хостинг HTTPS стал возможен.

**Минус `SNI`:** имя домена идёт **ОТКРЫТЫМ ТЕКСТОМ** в `ClientHello` — провайдер видит, к какому сайту ты идёшь, даже если сам трафик зашифрован.

**Эволюция защиты:**

- **`ESNI`** (Encrypted SNI) — экспериментально, **отменено**.
- **`ECH`** (Encrypted Client Hello) — RFC draft, стандартизация в процессе.

**Как работает `ECH`:**

- Внешний (открытый) `ClientHello` идёт на «прикрытие» — например, общий `cdn.cloudflare.com`.
- Внутренний (зашифрованный) `ClientHello` раскрывает **реальный домен** только на сервере с приватным ключом.

**Поддержка:**

| Клиент / провайдер | Статус |
|---|---|
| **Firefox** | с 2023 |
| **Chrome** | частично, в Origin Trial |
| **Cloudflare** | да |
| **РФ / Иран** | блокируется на провайдерском уровне |

Для приложений это **прозрачно** — браузер сам решает, использовать ли `ECH`.',
                'difficulty' => 5,
                'topic' => 'networking.tls',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое mTLS (mutual TLS) и где его применяют?',
                'answer' => '**Обычный TLS** аутентифицирует **только сервер**: клиент проверяет, что говорит с настоящим `example.com`. Клиент остаётся **анонимным** до уровня приложения (логин/пароль, токен).

**`mTLS`** (mutual TLS) добавляет аутентификацию **КЛИЕНТА**: у клиента тоже есть сертификат, выписанный доверенным `CA` (часто внутренним).

**Поток:**

1. Сервер шлёт **`CertificateRequest`**.
2. Клиент отвечает **`Certificate` + `CertificateVerify`** (подпись handshake-данных своим приватным ключом).
3. Сервер валидирует цепочку до своего trust store.
4. Если сертификат **отсутствует или невалиден** — TCP-коннект **рвётся**, до уровня HTTP даже не доходит.

**Где применяют:**

- **Service mesh** — внутри Kubernetes (`Istio`, `Linkerd`), каждый pod имеет свой сертификат от mesh-CA (`SPIFFE`/`SPIRE`). Это и есть **Zero Trust** в инфраструктуре.
- **Backend-to-backend API** без user context — банковские интеграции, B2B-партнёры.
- **IoT** — устройства аутентифицируются устройственным сертификатом, у них нет логина.
- **VPN** — `OpenVPN`, `WireGuard` используют похожую идею.
- **Admin-доступ** к критическим панелям.

**`mTLS` vs API-key:**

| | **`mTLS`** | **API-key / Bearer token** |
|---|---|---|
| Ротация | автоматическая через `CA` | вручную |
| «Утечка токена» | приватный ключ **не передаётся** | передаётся каждый запрос |
| Аудит | привязка к конкретной identity (cert subject) | по строке токена |
| Инфраструктура | сложнее (`CA`, ротация, revocation) | простая |
| Браузерный UX | плохой | хороший |',
                'difficulty' => 4,
                'topic' => 'networking.tls',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое Perfect Forward Secrecy и почему все cipher suites теперь её используют?',
                'answer' => '**`PFS`** (Perfect Forward Secrecy) — свойство протокола: **компрометация долгосрочного приватного ключа сервера НЕ позволяет расшифровать ранее перехваченный трафик**.

**Без `PFS` (старые cipher suites с `RSA` key exchange):**

- Клиент шифровал **premaster secret** приватным ключом сервера.
- Атакующий, записавший трафик и потом получивший приватный ключ (взлом, изъятие, court order), может **расшифровать всё ретроспективно**.
- Случилось известно с **Heartbleed (2014)** — годы трафика стали ретроактивно уязвимыми.

**С `PFS` (`ECDHE` — Ephemeral Diffie-Hellman на эллиптических кривых):**

- На каждое соединение генерируется **НОВАЯ ephemeral-пара ключей**, используется только для этой сессии и **сразу выбрасывается**.
- Долгосрочный приватный ключ сервера используется **только для подписи** ephemeral-параметров — доказывает идентичность, но **не участвует в шифровании контента**.
- Скомпрометировав долгосрочный ключ, атакующий может **выдать себя за сервер в будущем**, но **НЕ может расшифровать прошлый трафик** — ephemeral-ключи нигде не хранились.

**По версиям:**

| Версия | Поддержка `PFS` |
|---|---|
| **`TLS 1.3`** | **все** cipher suites — `PFS`; `RSA` key exchange **удалён** |
| **`TLS 1.2`** | нужно явно настраивать — предпочтение `ECDHE_RSA_*`, `ECDHE_ECDSA_*` |
| **`TLS 1.0` / `1.1`** | depreciated, не использовать |

**Стандарт настройки:** **Mozilla SSL Config Generator** (`ssl-config.mozilla.org`) — генерит секции для `nginx`/`Apache`/`HAProxy` с правильными cipher suites.',
                'difficulty' => 5,
                'topic' => 'networking.tls',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое TLS/SSL простыми словами?',
                'answer' => '**`TLS`** — криптографический протокол, который оборачивает обычное TCP-соединение и даёт **три гарантии**:

- **Шифрование** — никто посередине не прочитает трафик.
- **Целостность** — подмена байтов на лету будет замечена.
- **Аутентификация сервера** — сертификат подтверждает, что `example.com` это правда `example.com`.

**`SSL`** — старое название версий 1.0-3.0; давно устарели и опасны.
**`TLS`** — современное имя, актуальные версии — **`TLS 1.2`** и **`TLS 1.3`**.

В стеке TLS лежит **между TCP и протоколом приложения**:

- `HTTP` + `TLS` = `HTTPS` (порт `443`).
- `SMTP` + `TLS` = `SMTPS`.
- `IMAP` + `TLS` = `IMAPS`.

Названия SSL и TLS в речи часто смешивают, но в проде везде уже TLS.',
                'difficulty' => 2,
                'topic' => 'networking.tls',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое сертификат TLS простыми словами?',
                'answer' => '**TLS-сертификат** — цифровой документ, подтверждающий, что `example.com` действительно принадлежит конкретной организации.

**Что внутри:**

- **Доменное имя** и список `SAN` (Subject Alternative Name) — для каких доменов валиден.
- **Публичный ключ** сервера.
- **Сроки действия** (`notBefore` / `notAfter`).
- **Подпись доверенного центра сертификации (CA)** — Let\'s Encrypt, DigiCert, Sectigo и т.п.

**Что проверяет браузер при подключении:**

1. **Подпись валидна** (цепочка тянется до доверенного root CA).
2. **Сертификат не истёк** и ещё «начался».
3. **Доменное имя совпадает** с `SAN` (старое поле `CN` современные браузеры игнорируют).
4. Сертификат **не отозван** (OCSP / CRL).

Если хоть один пункт не сошёлся — браузер показывает страшный красный экран `NET::ERR_CERT_*`.',
                'difficulty' => 2,
                'topic' => 'networking.tls',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое центр сертификации (CA) простыми словами?',
                'answer' => '**CA** (Certificate Authority) — **организация, которой доверяют браузеры и ОС**. CA подписывает сертификаты сайтов своим ключом, гарантируя их подлинность.

**Как устроено доверие:**

- В каждом браузере/ОС зашит **список доверенных root CA** (несколько сотен организаций).
- Root CA подписывает **intermediate CA** (промежуточные), которые уже подписывают конечные сертификаты сайтов — это **цепочка сертификатов**.
- Если сертификат подписан root-ом, которого нет в списке, или цепочка не дотягивается до доверенного root — браузер показывает **«Untrusted certificate»**.

**Известные публичные CA:**

- **Let\'s Encrypt** — бесплатно, автоматизированно через ACME.
- **DigiCert**, **Sectigo**, **GlobalSign** — коммерческие, EV/OV-сертификаты.

В корпоративной среде часто поднимают **свой внутренний CA** для mTLS и сервисов внутри VPN.',
                'difficulty' => 2,
                'topic' => 'networking.tls',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое TLS-handshake простыми словами?',
                'answer' => '**TLS-handshake** — короткий обмен сообщениями **в начале каждого HTTPS-соединения**, в котором клиент и сервер договариваются о шифровании.

**Что происходит:**

1. **`ClientHello`** — клиент шлёт список поддерживаемых cipher suites, версий TLS, расширения (`SNI` — какой домен, `ALPN` — `http/1.1` или `h2`).
2. **`ServerHello` + сертификат** — сервер выбирает cipher suite и показывает свой сертификат.
3. **Обмен ключами** (ECDHE) — обе стороны вычисляют **общий симметричный ключ**, никогда не передавая его по сети.
4. **Клиент валидирует** цепочку сертификата до доверенного root CA, проверяет имя домена, срок действия.
5. **`Finished`** — поехало приложение (HTTP внутри).

**Скорость:**

- В **`TLS 1.3`** handshake укладывается в **1 RTT** (один раунд туда-обратно).
- В **`TLS 1.2`** — 2 RTT.
- При повторном подключении — **0-RTT** (early data) для идемпотентных запросов.

После handshake вся переписка идёт **зашифрованной симметричным ключом**.',
                'difficulty' => 2,
                'topic' => 'networking.tls',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое Let\'s Encrypt простыми словами?',
                'answer' => '**Let\'s Encrypt** — **бесплатный публичный центр сертификации**, выпускающий TLS-сертификаты автоматически по протоколу **ACME**.

**Как работает:**

- Клиент (`certbot`/`acme.sh`) подтверждает, что вы владеете доменом — через файл по HTTP (`/.well-known/acme-challenge/...`) или TXT-запись в DNS.
- Let\'s Encrypt выписывает сертификат на **90 дней**.
- Тот же клиент **автоматически продлевает** сертификат за ~30 дней до окончания.

**Инструменты:**

- `certbot` — официальный.
- `acme.sh` — лёгкий, на bash.
- Встроено в `Caddy` и `Traefik` — нулевая конфигурация.

Благодаря Let\'s Encrypt **HTTPS стал бесплатной нормой**, а не платной опцией. К 2026 году обычный HTTP без TLS браузеры явно помечают как небезопасный.',
                'code_example' => "# nginx + certbot\nsudo certbot --nginx -d example.com -d www.example.com\n\n# проверить таймер автообновления\nsudo systemctl status certbot.timer",
                'code_language' => 'bash',
                'difficulty' => 2,
                'topic' => 'networking.tls',
            ],
            [
                'category' => 'Сети',
                'question' => 'Как проверить TLS-сертификат сайта руками и на что смотреть?',
                'answer' => 'Базовый инструмент — **`openssl s_client`**: открывает TLS-соединение и показывает, что прислал сервер.

**Важно:** обязательно передавать **`-servername`** (флаг **`SNI`**) — без него на multi-site хостинге вернётся не тот сертификат или вообще default.

**На что смотреть в выводе:**

- **`Subject` / `SAN`** (Subject Alternative Name) — список доменов, на которые сертификат валиден. Современные браузеры **игнорируют `CN`** и смотрят только `SAN`.
- **`Issuer`** — кто подписал (обычно intermediate `CA`, например `Let\'s Encrypt R3`).
- **Validity** (`notBefore` / `notAfter`) — сроки.
- **`Verify return code`** — `0 (ok)` значит цепочка валидна. Ненулевые:
    - `21` — unable to verify the first certificate (забыт intermediate в bundle).
    - `10` — expired.
    - `18` — self-signed.
- **Цепочка** через `-showcerts` — leaf + все intermediates до root.

**Альтернативы:**

- **Браузер:** значок замка → «Details» — то же самое в GUI.
- **Онлайн:** `ssllabs.com/ssltest` — grade `A`-`F` с разбором cipher suites, версий `TLS`, `OCSP`-stapling, `HSTS`.

**Типовые ошибки:**

- `certificate verify failed` — нет intermediate в bundle или просрочен.
- `hostname mismatch` — в `SAN` нет нужного домена.
- `unable to get local issuer` — устарел системный CA-store (старый сервер).',
                'code_example' => "openssl s_client -connect example.com:443 -servername example.com < /dev/null\n# Verify return code: 0 (ok)  ← цепочка валидна\nopenssl s_client -connect example.com:443 -servername example.com < /dev/null 2>/dev/null \\\n  | openssl x509 -noout -subject -issuer -dates -ext subjectAltName\n# subject= /CN=example.com\n# issuer=  /C=US/O=Let's Encrypt/CN=R3\n# notBefore=... notAfter=...\n# X509v3 Subject Alternative Name: DNS:example.com, DNS:www.example.com",
                'code_language' => 'bash',
                'difficulty' => 3,
                'topic' => 'networking.tls',
            ],
        ];
    }
}
