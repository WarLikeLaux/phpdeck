<?php

namespace Database\Seeders\Data\Categories\Networking;

class Transport
{
    public static function all(): array
    {
        return [
            [
                'category' => 'Сети',
                'question' => 'В чём ключевая разница между TCP и UDP?',
                'answer' => '**`TCP`** — connection-oriented, **надёжный упорядоченный байтовый поток**:

- Перед обменом — **three-way handshake** (`SYN` → `SYN-ACK` → `ACK`), при завершении — `FIN`/`ACK`.
- Гарантирует доставку (**retransmission** при потерях), порядок (sequence numbers).
- **Flow control** (скользящее окно — защита получателя) и **congestion control** (замедление при перегрузке сети).
- Заголовок **20-60 байт**, есть state в ядре.
- **Где:** `HTTP`, `SMTP`, `SSH`, соединения к БД — везде, где важна целостность.

**`UDP`** — connectionless, **fire-and-forget**, передаёт «датаграммы»:

- Никаких гарантий: пакет может потеряться, прийти не по порядку, продублироваться.
- Заголовок **8 байт**, нет state.
- **Где:** `DNS` (один пакет туда — один обратно), видео/аудио-стриминг, **VoIP**, онлайн-игры, мониторинг (StatsD).

Над UDP можно строить **свою надёжность** — так работает `QUIC` (под `HTTP/3`) и WireGuard: даёт TCP-семантику + мультиплексирование без head-of-line blocking + 0-RTT и шифрование из коробки.',
                'difficulty' => 2,
                'topic' => 'networking.transport',
            ],
            [
                'category' => 'Сети',
                'question' => 'Как работает three-way handshake в TCP и что в нём обсуждается?',
                'answer' => 'Открытие TCP-соединения проходит за **3 пакета**.

**Шаги:**

1. **Клиент → сервер: `SYN`** с начальным sequence number клиента (**ISN_c**) и опциями. Клиентский сокет в `SYN_SENT`.
2. **Сервер → клиент: `SYN-ACK`** со своим `ISN_s` и `ACK = ISN_c + 1`. Сокет сервера в `SYN_RECEIVED`.
3. **Клиент → сервер: `ACK`** с `ACK = ISN_s + 1`. Оба сокета в `ESTABLISHED`, поехали данные.

**Что согласуется в опциях:**

- **`ISN`** — случайные начальные sequence numbers (защита от подделки пакетов).
- **`MSS`** (Maximum Segment Size) — макс. размер TCP-сегмента, обычно `MTU − 40 = 1460` для Ethernet.
- **Window scaling** — расширение окна с 16 до 30 бит (для гигабитных сетей с большим BDP).
- **`SACK`** (Selective ACK) — подтверждать «дырки» в потоке.
- **Timestamps** — для измерения RTT и защиты от wrap-around sequence numbers (`PAWS`).

**Стоимость:** ровно **1 RTT** до начала отправки данных. На коротких HTTP-запросах это бутылочное горлышко — отсюда `keepalive` и `HTTP/2` multiplexing.

**Что поверх:**

- `TLS 1.2` добавляет ещё **2 RTT** на handshake, `TLS 1.3` — **1 RTT** (с **0-RTT** на повторе).
- **`QUIC`** объединяет TCP+TLS в **1 RTT**, а на повторе соединения — **0 RTT**.

**Атака `SYN flood`:** атакующий шлёт много `SYN` с поддельных IP, заполняет очередь полуоткрытых соединений. Защита — **`SYN cookies`** (`net.ipv4.tcp_syncookies=1` в Linux).',
                'difficulty' => 3,
                'topic' => 'networking.transport',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое состояние TIME_WAIT и почему его так много в логах?',
                'answer' => '**`TIME_WAIT`** — финальное состояние **стороны, инициировавшей закрытие** TCP-соединения (вызвавшей `FIN` первой). После четырёхэтапного teardown (`FIN` → `ACK` → `FIN` → `ACK`) активная сторона остаётся в `TIME_WAIT` на **`2×MSL`** (Maximum Segment Lifetime), на Linux это обычно **60 секунд**.

**Зачем оно вообще нужно:**

1. **Поглотить опоздавшие дубликаты** пакетов из предыдущего соединения, чтобы они не попали в новое с тем же кортежем `(src_ip, src_port, dst_ip, dst_port)`.
2. **Гарантировать доставку финального `ACK`** — если он потерян, удалённая сторона ретрансмитит `FIN`, и наш `TIME_WAIT` отвечает `ACK`-ом.

**Проблема в продакшене:**

- Сервис с большим количеством **исходящих коротких соединений** (например, `PHP-FPM`, дёргающий внутренний API без keepalive) копит десятки тысяч `TIME_WAIT`, исчерпывая **ephemeral ports** (по умолчанию `32768-60999`, ~28k).
- **Симптом:** `Cannot assign requested address` / `EADDRNOTAVAIL`.
- На **reverse proxy** перед массой коротких клиентов `TIME_WAIT` копится реже — серверная сторона обычно не инициирует `close`.

**Решения (по убыванию полезности):**

1. **Keepalive + connection pooling** (главное и самое важное).
2. Снизить **`tcp_fin_timeout`**.
3. Расширить **ephemeral port range** (`net.ipv4.ip_local_port_range`).
4. В крайнем случае — **`net.ipv4.tcp_tw_reuse=1`** (Linux 4.12+, безопасно с TCP timestamps).

**Не использовать:** `tcp_tw_recycle` — опасен за NAT и удалён из ядра.',
                'difficulty' => 4,
                'topic' => 'networking.transport',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое congestion control в TCP и в чём разница между CUBIC, Reno и BBR?',
                'answer' => '**Congestion control** — алгоритм, которым TCP подбирает скорость отправки, **чтобы не залить сеть**. Идея: **`cwnd`** (congestion window) растёт, пока нет потерь, и режется при потере (которая трактуется как сигнал перегруженной очереди в роутере).

**Сравнение алгоритмов:**

| Алгоритм | Год | Стратегия | Где хорош | Где плох |
|---|---|---|---|---|
| **`Reno`** | 80-е | loss-based, AIMD (`cwnd += 1` за RTT, `cwnd /= 2` при потере) | простой, исторический | медленно растёт при больших **BDP** |
| **`CUBIC`** | 2006 (Linux default) | loss-based, рост по кубической функции от времени с последней потери | высокоскоростные сети | bufferbloat, мобильные |
| **`BBR`** | 2016 (Google) | model-based: моделирует **bottleneck bandwidth** и RTT, держит `cwnd` ≈ BDP | bufferbloat, WiFi, мобильные | «выдавливает» соседние loss-based TCP |

**Slow start** (общий для loss-based): в начале `cwnd` **удваивается каждый RTT** до `ssthresh`, потом переходит в congestion avoidance.

**`BBR` принципиально иначе:** не реагирует на потери, а измеряет пропускную способность и RTT — без переполнения буферов. Используется в **YouTube**, **Google Cloud LB**.

**Установка на Linux:**

```bash
# проверить текущий
sysctl net.ipv4.tcp_congestion_control
# переключить на BBR (kernel 4.9+)
sudo sysctl -w net.ipv4.tcp_congestion_control=bbr
```

Это **глобальная настройка на хост**, влияющая на ВСЕ исходящие коннекты.',
                'difficulty' => 5,
                'topic' => 'networking.transport',
            ],
            [
                'category' => 'Сети',
                'question' => 'В чём разница между flow control и congestion control в TCP?',
                'answer' => 'Два разных механизма ограничения скорости с **разной целью**.

| | **Flow control** | **Congestion control** |
|---|---|---|
| От чего защищает | **получателя** (его буфер) | **сеть** (узкое место маршрута) |
| Через что работает | **`rwnd`** (receive window) | **`cwnd`** (congestion window) |
| Где живёт | поле в TCP-заголовке, передаётся получателем | внутреннее состояние отправителя, в пакетах не передаётся |
| Сигнал | приложение не успевает читать | потери / задержки |

**Flow control:** отправитель не может слать быстрее, чем получатель успевает читать из сокетного буфера. Если приложение долго не вызывает `read()` — буфер заполняется, **`rwnd → 0`**, отправитель останавливается. **`Window scaling`** расширяет `rwnd` с **16 бит** (макс 64KB) до **30 бит** для гигабитных сетей с большим BDP.

**Congestion control:** отправитель не может слать быстрее, чем выдерживает узкое место маршрута — иначе буфер роутера переполнится, начнутся drop-ы.

**Реальный размер окна отправителя:**

```
window = min(rwnd, cwnd)
```

- **`rwnd` мал** (получатель тормозит) → упор в **flow control**.
- **`cwnd` мал** (сеть перегружена) → упор в **congestion control**.

**Симптомы:**

- **`cwnd`-проблемы:** высокий retransmission rate, `ss -i` показывает большой `backoff`.
- **`rwnd`-проблемы:** `ss -i` показывает маленький `rwnd` при свободном CPU/сети сервера.',
                'difficulty' => 4,
                'topic' => 'networking.transport',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое алгоритм Нейгла (Nagle), TCP_NODELAY и когда какой нужен?',
                'answer' => '**Алгоритм Нейгла** — оптимизация в TCP, которая **агрегирует мелкие отправки**, чтобы избежать «пачки тиньки» (много 1-байтных пакетов с 40-байтным TCP/IP-заголовком — катастрофически неэффективно).

**Правило:** если есть неподтверждённые маленькие сегменты в полёте — **не слать новый маленький сегмент**, а копить данные, пока:

- не наберётся **`MSS`**, **или**
- не придёт **`ACK`** на предыдущий сегмент.

По умолчанию **включён** в Linux.

**Проблема — «Nagle delay»:** интерактивные протоколы (request-response с маленькими сообщениями) ловят задержки **до 200ms** из-за взаимодействия Nagle с **delayed ACK** на другой стороне.

**`TCP_NODELAY`** — опция `setsockopt`, **отключающая Nagle**: `send()` сразу отправляет, не дожидаясь.

**Когда какой:**

| Сценарий | Настройка |
|---|---|
| `SSH`, `telnet`, игры, real-time мессенджеры, `gRPC`, любые request-response | **`TCP_NODELAY=1`** (отключить Nagle) |
| Bulk file transfer, streaming, много мелких `write()` подряд | **оставить Nagle** |

Современные библиотеки (`Go net`, `Java NIO`, `Node.js net`) обычно ставят `TCP_NODELAY` по умолчанию.

**Альтернатива — `TCP_CORK`** (Linux-only): принудительно **копит** пакеты до явного `uncork`, удобно для отдачи статических файлов через `sendfile()`.',
                'difficulty' => 4,
                'topic' => 'networking.transport',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое TCP keepalive и зачем настраивать его параметры?',
                'answer' => '**TCP keepalive** — механизм ядра, который **периодически шлёт «пустые» пробы** по простаивающему соединению, чтобы **детектировать мёртвую другую сторону**.

**Зачем нужен:** без keepalive `ESTABLISHED`-соединения, у которых партнёр сгинул без `FIN` (упал роутер, отвалилась сеть, `kill -9` процесса), остаются **«зомби»** неограниченно — занимают сокеты и ресурсы.

**Параметры Linux** (`sysctl net.ipv4.tcp_keepalive_*`):

| Параметр | Дефолт | Что значит |
|---|---|---|
| `tcp_keepalive_time` | `7200` (2 часа) | через сколько секунд простоя начинать пробы |
| `tcp_keepalive_intvl` | `75` | интервал между пробами |
| `tcp_keepalive_probes` | `9` | сколько проб без ответа = «мертво» |

**По дефолту:** `~2 часа + 9×75 сек ≈ 2ч 11мин` до детекта — слишком долго для большинства приложений.

Включается per-socket через **`setsockopt SO_KEEPALIVE`**.

**Production-настройка:** `tcp_keepalive_time` ~30-60 сек + `intvl` 5-10 сек + 3-6 проб → детект за 1-2 минуты.

**TCP keepalive vs application-level heartbeat:**

- **TCP keepalive** — на уровне ядра, прозрачно для приложения.
- **App-level heartbeat** — свои ping-pong (`HTTP/2`, `WebSocket`, MySQL) — **гибче** и **работает через NAT/middleboxes**.
- **NAT-таблицы истекают** за 5-30 минут — поэтому WebSocket-серверы шлют app-level ping каждые 30-60 сек.',
                'difficulty' => 4,
                'topic' => 'networking.transport',
            ],
            [
                'category' => 'Сети',
                'question' => 'Зачем нужны SO_REUSEADDR и SO_REUSEPORT?',
                'answer' => 'Опции `setsockopt`, которые меняют семантику **`bind()`**.

**`SO_REUSEADDR`** (POSIX-стандарт):

1. **Перепривязка после рестарта** — не дожидаться окончания `TIME_WAIT` предыдущих соединений (иначе `Address already in use` 60+ сек).
2. **Параллельный bind** на `0.0.0.0:80` и `127.0.0.1:80` (wildcard + конкретный интерфейс).

Это **must-have** для любого долгоживущего сервера — без неё рестарт сервиса вешает на минуту. В Linux семантика чуть слабее BSD: один сокет **не может отнять** порт у активно слушающего.

**`SO_REUSEPORT`** (Linux 3.9+, FreeBSD):

1. **Несколько процессов/потоков** могут одновременно `bind()` на один и тот же `ip:port` — ядро **равномерно распределяет** входящие соединения. Это **аппаратный load balancing на уровне сокетов**.
2. **Решает «thundering herd»** — десяток воркеров просыпались на каждое новое соединение; теперь у каждого свой listening socket без лок-контейшна.

Используется в **`nginx`** (`--with-http_so_reuseport`) и любых high-performance сетевых приложениях.

**Сравнение:**

| | **`SO_REUSEADDR`** | **`SO_REUSEPORT`** |
|---|---|---|
| Цель | переиспользовать порт в `TIME_WAIT` | параллельный listen с балансировкой |
| Где | POSIX-стандарт | Linux 3.9+, FreeBSD |
| Распределение коннектов | нет | да, ядром |

**Подвох:** при рестарте одного воркера его очередь принятых соединений **теряется** (ядро не перераспределяет). В Linux 4.5+ добавили **`SO_INCOMING_CPU`**, в 5.x — **eBPF**-программы для тонкой настройки распределения.',
                'difficulty' => 4,
                'topic' => 'networking.transport',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое TCP простыми словами?',
                'answer' => '**TCP** — протокол транспортного уровня (**L4**), который гарантирует **надёжную и упорядоченную** доставку данных. Аналогия — заказное письмо с уведомлением: каждый пакет «расписывается» в получении.

Что делает TCP:

- Перед обменом устанавливает соединение через **three-way handshake** (`SYN` → `SYN-ACK` → `ACK`).
- Если пакет потерялся — **переотправит**.
- Если пришли не по порядку — **пересортирует** на приёме перед отдачей приложению.
- Управляет скоростью отправки, чтобы не залить сеть и получателя.

**Где используется:** `HTTP`/`HTTPS`, `SSH`, `SMTP`, соединения к БД — везде, где важна целостность.

**Цена надёжности** — больше overhead (заголовок 20-60 байт) и задержки на handshake.',
                'difficulty' => 1,
                'topic' => 'networking.transport',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое UDP простыми словами?',
                'answer' => '**UDP** — протокол транспортного уровня (**L4**) **без гарантий** доставки. Аналогия — открытка: бросил в ящик и не знаешь, дошла ли.

- Шлёт «датаграммы» по принципу **fire-and-forget**: не следит, дошли ли они, в каком порядке и были ли дубли.
- Очень быстрый, минимум overhead (заголовок **8 байт** против 20 у TCP).
- **Без рукопожатий** — первый же пакет несёт данные.

**Где используется** (потеря отдельных пакетов терпимее задержки):

- `DNS` — один пакет туда, один обратно.
- Видеостриминг и **VoIP** — потерянный кадр проще пропустить, чем ждать ретрансмиссии.
- Онлайн-игры.

Поверх UDP построены **QUIC** и **HTTP/3** — добавляют свою надёжность и шифрование, обходя ограничения TCP.',
                'difficulty' => 1,
                'topic' => 'networking.transport',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое порт простыми словами?',
                'answer' => '**Порт** — число от `0` до `65535`, идентифицирующее конкретное приложение на машине.

Если IP-адрес — это «дом», то порт — это «номер квартиры»: IP говорит «на какой компьютер», порт — «какому процессу там». На одном IP сразу слушают много сервисов на разных портах: nginx на `80`, MySQL на `3306`, PHP-FPM на `9000`.

**Стандартные well-known порты (0-1023):**

- `80` — HTTP, `443` — HTTPS
- `22` — SSH, `53` — DNS
- `3306` — MySQL, `5432` — PostgreSQL, `6379` — Redis
- `25` / `587` — SMTP

Слушать порт ниже `1024` на Linux обычно требует **root**. Посмотреть, кто что слушает: `ss -tulpn`.',
                'difficulty' => 1,
                'topic' => 'networking.transport',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое сокет простыми словами?',
                'answer' => '**Сокет** — точка входа/выхода для сетевого общения процесса. На уровне ОС — обычный **файловый дескриптор**, в который можно `read()`/`write()`, а ядро под капотом гоняет `TCP`/`UDP`-пакеты.

Идентифицируется **четвёркой**: `(src_ip, src_port, dst_ip, dst_port)` + протокол.

**Серверный сокет:**

- `bind(порт)` → `listen()` → `accept()`.
- На каждое новое соединение `accept()` возвращает **отдельный сокет** для общения с этим клиентом.

**Клиентский сокет:** `connect(ip:port)` к серверу.

**Unix-сокеты** (`/var/run/php-fpm.sock`) — то же API, но связь **между процессами на одной машине**, без сетевого стека. Быстрее, чем TCP по `localhost` — часто так связывают `nginx` и `php-fpm`.',
                'difficulty' => 2,
                'topic' => 'networking.transport',
            ],
            [
                'category' => 'Сети',
                'question' => 'В чём разница между TCP и UDP простыми словами?',
                'answer' => '**`TCP`** — «надёжный, с гарантиями». Аналогия — **заказное письмо** с уведомлением.

- Устанавливает соединение через **handshake**.
- Гарантирует доставку всех пакетов **в правильном порядке**.
- Переотправляет потерянное, тормозит при перегрузке сети.
- Используется, когда важна целостность: `HTTP`/`HTTPS`, БД, `SSH`, `SMTP`.

**`UDP`** — «быстрый, без гарантий». Аналогия — **открытка**.

- Шлёт пакеты как есть, без подтверждений и порядка, потеря — нормально.
- Используется, когда задержка важнее потери отдельных пакетов: видеозвонки и **VoIP**, онлайн-игры, `DNS`, стриминг.

Над UDP можно построить свою надёжность — так делает **QUIC** (HTTP/3).',
                'difficulty' => 1,
                'topic' => 'networking.transport',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое three-way handshake простыми словами?',
                'answer' => 'Трёхпакетное **«рукопожатие»** в начале каждого `TCP`-соединения:

1. Клиент → сервер: **`SYN`** («хочу подключиться»).
2. Сервер → клиент: **`SYN-ACK`** («ок, и я к тебе»).
3. Клиент → сервер: **`ACK`** («подтверждаю»).

После этого соединение в состоянии `ESTABLISHED`, можно обмениваться данными.

**Цена:** один полный **round-trip** до начала отправки данных. Отсюда:

- Заметная задержка при открытии новых TCP-коннектов (особенно на мобильных).
- Стимул использовать **keepalive** (один TCP — много запросов), `HTTP/2` (мультиплексирование), `HTTP/3`/`QUIC` (handshake за 1 RTT, иногда 0).',
                'difficulty' => 2,
                'topic' => 'networking.transport',
            ],
            [
                'category' => 'Сети',
                'question' => 'Какие стандартные порты надо знать?',
                'answer' => '**Веб:** `80` — HTTP, `443` — HTTPS.

**Удалённый доступ:** `22` — SSH, `21` — FTP.

**Почта:**

- `25` — SMTP (между серверами).
- `587` — SMTP от клиента (с STARTTLS).
- `143` / `993` — IMAP / IMAPS.
- `110` / `995` — POP3 / POP3S.

**DNS:** `53`.

**Базы данных и инфраструктура:**

- `3306` — MySQL/MariaDB.
- `5432` — PostgreSQL.
- `6379` — Redis.
- `27017` — MongoDB.
- `5672` — RabbitMQ.
- `9200` — Elasticsearch.
- `9000` — PHP-FPM.

**Диапазоны:**

- `0-1023` — **well-known** (для их bind на Linux нужен root или `CAP_NET_BIND_SERVICE`).
- `1024-49151` — registered.
- `49152-65535` — динамические/**ephemeral** (клиентские соединения).',
                'difficulty' => 1,
                'topic' => 'networking.transport',
            ],
            [
                'category' => 'Сети',
                'question' => 'Как посмотреть, какие порты слушает машина?',
                'answer' => 'На Linux — утилита **`ss`** (современная) или `netstat` (старая).

**`ss -tulpn`** покажет все TCP/UDP-сокеты в режиме `LISTEN` с PID и именем процесса. Расшифровка флагов:

- `-t` — TCP.
- `-u` — UDP.
- `-l` — только `LISTEN`.
- `-p` — показать процесс (нужен `sudo`).
- `-n` — без резолва имён (быстрее, видны сырые порты).

Это **первая команда** при «почему мой сервис не отвечает» — а вдруг он вообще не поднялся или поднялся не на том интерфейсе. `127.0.0.1:3306` слушает только локально, `0.0.0.0:3306` — на всех интерфейсах.',
                'code_example' => "sudo ss -tulpn\n# tcp LISTEN 0 511 0.0.0.0:80 ... users:((\"nginx\",pid=1234,fd=6))\nsudo ss -tulpn | grep :8000\n# проверить, кто слушает 8000",
                'code_language' => 'bash',
                'difficulty' => 2,
                'topic' => 'networking.transport',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое QUIC простыми словами?',
                'answer' => '**QUIC** — современный транспортный протокол поверх **`UDP`**, на котором работает **`HTTP/3`**. Объединяет функции `TCP` + `TLS` в одном протоколе.

**Что даёт:**

- **Надёжная упорядоченная доставка** (как TCP), но реализованная в user space, а не в ядре.
- **Шифрование всегда включено** — открытого QUIC не бывает.
- **Мультиплексирование потоков без head-of-line blocking**: потеря пакета в одном потоке не тормозит остальные (в HTTP/2 поверх TCP — тормозит).
- **Быстрый handshake**: 1-RTT, для повторных подключений — 0-RTT.
- **Connection migration**: телефон переключился с Wi-Fi на 4G — соединение не рвётся (привязка не к IP, а к Connection ID).

Разработан Google, стандартизирован IETF в 2021 (`RFC 9000`).',
                'difficulty' => 2,
                'topic' => 'networking.transport',
            ],
        ];
    }
}
