<?php

namespace Database\Seeders\Data\Categories\Networking;

class Http
{
    public static function all(): array
    {
        return [
            [
                'category' => 'Сети',
                'question' => 'Что происходит, когда пользователь набирает URL в браузере и жмёт Enter?',
                'answer' => 'Этапы: 1) Браузер парсит URL (scheme, host, path, query). 2) Резолвит домен в IP через DNS (кэш браузера → кэш ОС → recursive resolver → корень → TLD → authoritative). 3) Открывает TCP-соединение к IP:порту (по HTTPS — порт 443). 4) Делает TLS handshake: ClientHello/ServerHello, обмен ключами (обычно ECDHE), сервер показывает сертификат, браузер валидирует цепочку до доверенного CA, согласуется симметричный ключ; в TLS 1.3 — за 1 RTT, с 0-RTT для повторных сессий. 5) Отправляет HTTP-запрос (метод, путь, заголовки, куки). 6) Запрос обычно бьёт сначала в CDN/обратный прокси (Cloudflare, nginx) → балансировщик (Layer 7 — nginx/HAProxy/ALB) → апстрим (php-fpm, app-сервер). 7) Приложение обрабатывает: маршрутизация, middleware (auth, CSRF), контроллер, обращения к БД/кэшу, рендеринг ответа. 8) Ответ идёт обратно тем же путём. 9) Браузер парсит HTML, начинает грузить статические ресурсы (CSS, JS, картинки) — параллельно по тем же или новым соединениям (HTTP/2 мультиплексирует один TCP, HTTP/3 — поверх QUIC/UDP). 10) Строит DOM/CSSOM → render tree → layout → paint, выполняет JS, отправляет XHR/fetch. На каждом шаге могут быть кэши: HTTP cache, Service Worker, CDN edge cache, application cache (Redis), query cache БД.',
                'difficulty' => 3,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Какие основные HTTP-методы и их семантика?',
                'answer' => 'GET — получить ресурс. Безопасный (не меняет состояние), идемпотентный, кэшируется. Параметры через query, тело игнорируется. POST — создать или «выполнить операцию». Не идемпотентный, не кэшируется. Тело — любого формата (JSON, form-data). PUT — заменить ресурс целиком по известному URI. Идемпотентный. PATCH — частично обновить ресурс. DELETE — удалить ресурс. Идемпотентный. HEAD — как GET, но только заголовки без тела (для проверки существования/размера). OPTIONS — запрос поддерживаемых методов, используется браузером для CORS preflight. Запоминалка: GET читает, POST создаёт, PUT заменяет целиком, PATCH правит частично, DELETE удаляет.',
                'code_example' => "GET    /users/42      # получить\nPOST   /users         # создать\nPUT    /users/42      # заменить целиком\nPATCH  /users/42      # частично обновить\nDELETE /users/42      # удалить",
                'code_language' => 'http',
                'difficulty' => 2,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что значит «идемпотентный» в контексте HTTP и какие методы идемпотентны?',
                'answer' => 'Идемпотентность: повторный вызов метода с теми же параметрами даёт тот же эффект, что и один вызов. Не «возвращает то же значение», а «оставляет систему в том же состоянии». PUT /users/42 {name: "John"} вызывается 1 раз — пользователь 42 получит имя John. Повтор 5 раз — то же состояние, имя всё ещё John. Это идемпотентно. DELETE /users/42 — первый вызов удаляет, повтор возвращает 404 (или 204), но состояние не меняется → идемпотентно. POST /users {name: "John"} — каждый вызов создаёт нового пользователя, состояние меняется → НЕ идемпотентно. Идемпотентные по RFC 9110: GET, HEAD, PUT, DELETE, OPTIONS, TRACE. Безопасные (≡ идемпотентные + не меняют состояние): GET, HEAD, OPTIONS, TRACE. PATCH формально НЕ идемпотентный, но обычно реализуется так. Зачем это важно: 1) Прокси и клиенты автоматически РЕТРАЯТ идемпотентные методы при сетевых ошибках, ретранслируют GET/HEAD — нельзя ретраить POST без явного согласия. 2) Балансировщики могут безопасно повторить запрос на другом backend. 3) Кэширование — только идемпотентные. Хитрость: чтобы сделать POST идемпотентным (для платежей, регистраций), добавляют заголовок Idempotency-Key — клиент генерирует UUID, сервер запоминает результат на N часов и при повторе с тем же ключом возвращает закэшированный ответ. Используется в Stripe, Square, AWS API Gateway.',
                'difficulty' => 3,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'В чём разница между PUT и PATCH?',
                'answer' => 'PUT заменяет ресурс ЦЕЛИКОМ. Семантика: «вот полное представление /users/42, перезапиши». Если в теле нет поля email, сервер обязан УБРАТЬ email из ресурса (или установить в default), а не «оставить как было». Идемпотентен по определению. PATCH делает ЧАСТИЧНОЕ обновление. Тело описывает изменения, а не финальное состояние. Поля, отсутствующие в теле, не трогаются. Идемпотентность зависит от формата патча: 1) JSON Merge Patch (RFC 7396) — простой формат, типовой для REST API: тело — это объект с полями, которые нужно изменить, null значит «удалить поле». Идемпотентен. {name: "John"} — обновить name, оставить остальное. 2) JSON Patch (RFC 6902) — серия операций: [{op: "add"}, {op: "remove"}, {op: "replace"}, {op: "move"}, {op: "copy"}, {op: "test"}]. Сложнее, мощнее, чаще НЕ идемпотентен (op: add к массиву — двойной вызов даст два элемента). На практике: 1) Если у вас «полные» обновления, фронт всегда шлёт весь объект — используйте PUT. 2) Если нужно поменять одно поле (например, статус заказа), не пересылая весь ресурс — PATCH. 3) Реализуют PATCH с JSON Merge Patch как 90% дефолт. Главная ошибка: реализовать PUT как «обновить только переданные поля» — это семантический PATCH под именем PUT, ломает контракт.',
                'difficulty' => 3,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое REST API и какие у него ключевые принципы?',
                'answer' => 'REST (Representational State Transfer) — архитектурный стиль API, описанный Роем Филдингом в 2000 г. Не протокол, а набор ограничений (constraints). Ключевые: 1) Client-server — разделение клиента и сервера, развиваются независимо. 2) Stateless — сервер не хранит контекст между запросами; каждый запрос содержит всю информацию для обработки. Сессионное состояние — на клиенте. 3) Cacheable — ответы должны явно говорить, можно ли их кэшировать (Cache-Control, ETag). 4) Uniform interface — единообразный интерфейс: ресурсы идентифицируются URI (/users/42), манипулируются через стандартные HTTP-методы (GET/POST/PUT/DELETE), включают гипермедиа-ссылки (HATEOAS — почти никто не делает). 5) Layered system — клиент не знает, ходит ли он напрямую к серверу или через прокси/CDN/LB. 6) Code on demand (опционально) — сервер может слать исполняемый код (JS, апплеты). На практике под REST понимают: ресурсо-ориентированный API на HTTP с осмысленными URL и методами + JSON. То, что обычно НЕ соблюдают: HATEOAS (гипермедиа-ссылки на возможные действия), строгий stateless (часто протекает state через session-cookies). Часто называют REST то, что Филдинг назвал бы RPC-over-HTTP — но это уже общая практика. Альтернативы: GraphQL (один endpoint, клиент описывает форму ответа), gRPC (RPC поверх HTTP/2 с protobuf), tRPC (типобезопасный RPC внутри JS-стека).',
                'difficulty' => 2,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Какие основные классы HTTP-статусов и характерные коды?',
                'answer' => 'Пять классов по первой цифре: 1xx — информационные (редко): 100 Continue (можно слать большое тело), 101 Switching Protocols (WebSocket upgrade), 103 Early Hints (CDN отдаёт preload-подсказки до основного ответа). 2xx — успех: 200 OK (всё хорошо, есть тело), 201 Created (создали ресурс, обычно с Location-заголовком), 202 Accepted (приняли, обрабатываем асинхронно), 204 No Content (успех, тела нет — для DELETE и PUT), 206 Partial Content (range-запрос). 3xx — редиректы: 301 Moved Permanently (постоянный, кэшируется), 302 Found (временный, по умолчанию делают GET даже если был POST), 303 See Other (явно GET после POST/PUT — PRG-паттерн), 304 Not Modified (валидный кэш, не отдаём тело), 307 Temporary Redirect (метод и тело сохраняются), 308 Permanent Redirect (то же, но постоянно). 4xx — ошибка клиента: 400 Bad Request (валидация), 401 Unauthorized (не аутентифицирован), 403 Forbidden (аутентифицирован, но не можно), 404 Not Found, 405 Method Not Allowed, 406 Not Acceptable (Accept не соответствует), 409 Conflict (конкурентное изменение, нарушение бизнес-правила), 410 Gone (ресурс был, но удалён навсегда), 422 Unprocessable Entity (валидация — Laravel-стандарт), 429 Too Many Requests (rate limit). 5xx — ошибка сервера: 500 Internal Server Error, 502 Bad Gateway (upstream вернул ошибку), 503 Service Unavailable (перегрузка/maintenance), 504 Gateway Timeout (upstream не ответил). Из курьёзов: 418 I\'m a teapot (RFC 2324, шутка), 451 Unavailable For Legal Reasons (отсылка к Брэдбери).',
                'difficulty' => 2,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'В чём разница между HTTP/1.1, HTTP/2 и HTTP/3?',
                'answer' => 'HTTP/1.1 (1997): текстовый протокол, одно соединение = один запрос за раз. Keepalive позволяет переиспользовать TCP. Pipelining (отправить запрос не дожидаясь ответа предыдущего) — теоретически есть, но в реальности отключён везде из-за HOL blocking и багов прокси. Браузеры открывают 6 параллельных коннектов на хост → разработчики хитрят: domain sharding, CSS sprites, JS bundle. HTTP/2 (2015, RFC 7540): бинарный фрейминг — один TCP содержит много параллельных потоков (streams). Multiplexing решает HOL blocking на уровне HTTP — но не на уровне TCP (потеря пакета останавливает ВСЕ streams). Server push (мог отдать ресурсы до запроса) — отменён в Chrome 106, не оправдал ожиданий. HPACK — сжатие заголовков (общий словарь между запросами). Прирост на типовом сайте: 10-30% быстрее загрузка, особенно при многих мелких ресурсах. Browser требует TLS (RFC не требует, но фактически — да). HTTP/3 (2022, RFC 9114): полностью переработан — HTTP поверх QUIC (поверх UDP). Решает TCP HOL blocking: каждый stream независим, потеря пакета влияет только на свой поток. Встроенное шифрование (нельзя без TLS 1.3). 0-RTT handshake для повторных подключений — на 1 RTT быстрее, чем HTTP/2. Connection migration — клиент может сменить IP (переход WiFi → 4G) без переустановки соединения. Минусы: middleboxes часто блокируют UDP, требуется fallback на HTTP/2. На 2025: ~30% веб-трафика — HTTP/3, поддерживают Google, Cloudflare, AWS CloudFront, Apple, Meta.',
                'difficulty' => 4,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Как работают HTTP-заголовки кэширования: Cache-Control, ETag, Last-Modified?',
                'answer' => 'Два уровня: 1) Freshness — сервер говорит «эта версия валидна N секунд, не дёргайте меня». Cache-Control: max-age=3600 значит свежа 1 час, после — нужно проверить. public — можно кэшировать общими кэшами (CDN, прокси); private — только браузером (для персонализированных страниц). no-cache — кэшируй, но всегда валидируй перед использованием. no-store — вообще не кэшируй (для секретных данных). immutable — никогда не валидируй (для версионированных static-ресурсов: /app.abc123.js). s-maxage — то же, но только для прокси/CDN. stale-while-revalidate=N — можешь отдать устаревший ответ ещё N секунд, пока в фоне валидируешь. 2) Validation — когда фрешность истекла, клиент шлёт условный запрос: ETag: "abc123" — opaque-токен версии ресурса (часто хеш контента или revision id). Клиент шлёт If-None-Match: "abc123"; сервер либо отдаёт 200 с новым контентом и новым ETag, либо 304 Not Modified без тела (экономит трафик и серверное время). Last-Modified / If-Modified-Since — то же, но по дате. ETag точнее (миллисекунды и нет проблемы с CDN clock skew), Last-Modified удобнее для статических файлов. Стратегии: 1) HTML — короткий max-age + must-revalidate (чтобы сразу видеть обновления). 2) Хешированные ассеты (/app.abc.js) — max-age=31536000, immutable (никогда не валидируют). 3) Изображения пользователя — public, max-age=86400 + ETag.',
                'difficulty' => 4,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое HTTP простыми словами?',
                'answer' => '**HTTP** (HyperText Transfer Protocol) — основной протокол веба, по которому браузер и сервер общаются поверх **TCP**.

- Работает по схеме «**запрос-ответ**»: клиент шлёт запрос (метод + URL + заголовки + тело), сервер отвечает (статус + заголовки + тело).
- **Stateless** — каждый запрос самостоятельный, сервер по умолчанию не помнит предыдущий, поэтому для авторизации придумали `Cookie` и сессии.
- Версии: **HTTP/1.1** (1997) — текстовый, легко прочитать; **HTTP/2** (2015) и **HTTP/3** (2022) — бинарные, быстрее, мультиплексируют несколько потоков по одному соединению.
- **HTTPS** — это HTTP внутри **TLS**-шифрования.',
                'code_example' => "GET /api/users HTTP/1.1\nHost: example.com\n\nHTTP/1.1 200 OK\nContent-Type: application/json\n\n[{\"id\":1}]",
                'code_language' => 'http',
                'difficulty' => 1,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое URL и из чего он состоит?',
                'answer' => '**URL** (Uniform Resource Locator) — адрес ресурса в сети. Структура: `схема://пользователь:пароль@домен:порт/путь?запрос#фрагмент`.

Разбор `https://example.com:443/users/42?lang=ru#bio`:

- **`https`** — схема (протокол).
- **`example.com`** — домен (host).
- **`443`** — порт (обычно опускается, если стандартный).
- **`/users/42`** — путь к ресурсу.
- **`?lang=ru`** — query-параметры (фильтры, поиск, пагинация).
- **`#bio`** — фрагмент/якорь — обрабатывается только в браузере и **НЕ отправляется на сервер** (в логах его не будет).

Спецсимволы в URL должны быть закодированы: пробел → `%20`, кириллица → `%D0%9F` и т.д.',
                'code_example' => "https://example.com:443/users/42?lang=ru&page=2#bio\n# scheme://host:port/path?query#fragment",
                'code_language' => 'http',
                'difficulty' => 1,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'В чём разница между GET и POST простыми словами?',
                'answer' => '**`GET`** — «получить данные»:

- Параметры в URL (`?id=5`), тела нет.
- Ответ **кэшируется** браузерами и прокси.
- URL можно положить в закладки, виден в логах и истории браузера.
- Безопасно ретраить (идемпотентен).
- Используется для чтения и поиска.

**`POST`** — «отправить данные на обработку»:

- Параметры в **теле** запроса, в URL не светятся (важно для паролей и больших объёмов).
- **Не кэшируется**.
- Повторная отправка обычно создаёт дубль (отсюда «Подтвердить повторную отправку формы?»).
- Используется для форм, создания записей и любых операций, меняющих состояние на сервере.

Запоминалка: **`GET` читает, `POST` меняет**.',
                'code_example' => "GET /search?q=laravel HTTP/1.1\nHost: example.com\n\nPOST /api/users HTTP/1.1\nHost: example.com\nContent-Type: application/json\n\n{\"name\":\"John\"}",
                'code_language' => 'http',
                'difficulty' => 1,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что означают классы статусов HTTP 2xx/3xx/4xx/5xx простыми словами?',
                'answer' => 'Пять классов по первой цифре:

- **1xx — информационные** (редко встретишь).
- **2xx — успех**: `200 OK`, `201 Created` (создали ресурс), `204 No Content` (успех без тела, типично для `DELETE`).
- **3xx — редирект**: `301` постоянный (закэшируется навсегда), `302` временный, `304 Not Modified` (кэш у клиента валиден, тело не шлём).
- **4xx — виноват клиент**: `400` кривой запрос, `401` не залогинен, `403` запрещено, `404` не найдено, `422` ошибка валидации (Laravel-стандарт для `FormRequest`), `429` слишком много запросов (rate limit).
- **5xx — виноват сервер**: `500` внутренняя ошибка, `502 Bad Gateway` (upstream упал), `503` перегружен/maintenance, `504 Gateway Timeout` (upstream не ответил).',
                'difficulty' => 1,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое HTTP-заголовки (headers)?',
                'answer' => 'Метаданные о запросе/ответе в формате `Имя: значение`. Идут после стартовой строки до пустой строки, за которой начинается тело.

**Заголовки запроса:**

- `Host` — домен, к которому идём (обязателен в HTTP/1.1).
- `Content-Type` — формат тела.
- `Authorization` — токен/логин.
- `Cookie` — куки клиента.
- `User-Agent` — кто клиент: браузер, бот, curl.
- `Accept` — какие форматы ответа клиент готов принять.

**Заголовки ответа:**

- `Content-Type`, `Content-Length`.
- `Set-Cookie` — поставить куку.
- `Cache-Control` — правила кэша.
- `Location` — куда редиректить.

Регистр имени по стандарту не важен (`Content-Type` == `content-type`), но традиционно пишут `Capitalized-Words`. Общий объём заголовков обычно ограничен сервером ~8 КБ.',
                'code_example' => "GET /api/users HTTP/1.1\nHost: example.com\nAccept: application/json\nAuthorization: Bearer eyJhbGciOi...\nUser-Agent: Mozilla/5.0",
                'code_language' => 'http',
                'difficulty' => 1,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое Content-Type и какие самые частые значения?',
                'answer' => 'Заголовок, говорящий получателю, какой формат данных лежит в теле запроса или ответа. Самые частые значения: application/json — JSON (REST API, fetch/axios по умолчанию). application/x-www-form-urlencoded — обычная HTML-форма (key=value&key2=value2). multipart/form-data — форма с файлами (поля разделены boundary). text/html — HTML-страница. text/plain — обычный текст. image/png, image/jpeg — картинки. Можно указать кодировку: Content-Type: text/html; charset=utf-8. Если заголовок не соответствует реальному телу, парсер на той стороне сломается — Laravel вернёт 400 на «JSON» с urlencoded-телом.',
                'code_example' => "POST /api/users HTTP/1.1\nContent-Type: application/json\n\n{\"name\":\"John\"}",
                'code_language' => 'http',
                'difficulty' => 2,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое cookie простыми словами?',
                'answer' => '**Cookie** — маленький кусочек данных (до ~4 КБ), который сервер просит браузер сохранить и слать обратно с каждым запросом к этому сайту. Браузер хранит куки в привязке к домену.

Используются для сессий, авторизации, настроек интерфейса, A/B-тестов, корзин гостей.

- Сервер ставит куку заголовком `Set-Cookie` в ответе.
- Браузер шлёт её обратно заголовком `Cookie`.

**Ключевые атрибуты:**

- `Secure` — только по HTTPS.
- `HttpOnly` — недоступна из JavaScript (защита от XSS-кражи).
- `SameSite=Lax/Strict` — защита от CSRF.
- `Expires` / `Max-Age` — срок жизни (без них кука «сессионная» и умирает с закрытием браузера).
- `Domain` и `Path` — для каких URL отправлять.',
                'code_example' => "# Ответ сервера\nHTTP/1.1 200 OK\nSet-Cookie: session_id=abc123; HttpOnly; Secure; SameSite=Lax\n\n# Следующий запрос браузера\nGET /me HTTP/1.1\nCookie: session_id=abc123",
                'code_language' => 'http',
                'difficulty' => 1,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое сессия простыми словами?',
                'answer' => '**Сессия** — серверное состояние конкретного пользователя между запросами. HTTP сам по себе **stateless**, без сессии сервер не помнит, кто к нему пришёл.

Как это работает:

1. Браузеру в куке кладётся **session id** (например, `PHPSESSID` или `laravel_session`).
2. Сами данные (id залогиненного пользователя, корзина, флаги) хранятся **на сервере** — в файлах (`config/session.php` `driver=file`), `Redis` или БД.
3. При следующем запросе сервер по куке находит запись и достаёт эти данные.

Куку с session id обязательно ставят `HttpOnly` + `Secure` + `SameSite` — её кража равна захвату аккаунта без знания пароля.',
                'difficulty' => 1,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Чем веб-API отличается от обычной веб-страницы?',
                'answer' => 'Кому отдаём — тому и формат:

- **Веб-страница** возвращает **HTML**, который браузер рендерит в визуальную страницу **для человека**.
- **Веб-API** возвращает машинно-читаемые данные (обычно **JSON**, реже XML) **для другой программы** — SPA-фронтенда, мобильного приложения, скрипта, другого сервиса.

По транспорту это те же HTTP-эндпоинты, отличие в:

- `Content-Type`: `application/json` вместо `text/html`.
- Теле: структурированные данные вместо HTML-разметки.
- На ошибку API отвечает кодом статуса плюс JSON-ошибкой, а не HTML-страницей `500`.

Один и тот же бэкенд часто отдаёт оба формата — в Laravel это `routes/web.php` и `routes/api.php`.',
                'difficulty' => 1,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'В чём разница между HTTP и HTTPS?',
                'answer' => '**HTTP** передаёт данные **открытым текстом** — провайдер, владелец Wi-Fi-точки или MITM могут читать пароли, куки, переписку.

**HTTPS = HTTP + TLS** поверх. Даёт три гарантии:

- **Шифрование** — никто посередине не прочтёт.
- **Целостность** — подмена байт будет замечена.
- **Аутентификация сервера** — сертификат от доверенного **CA** подтверждает, что вы общаетесь именно с тем доменом (значок замка в браузере).

**Порты:** HTTP — `80`, HTTPS — `443`.

Современный веб целиком на HTTPS: **Let\'s Encrypt** раздаёт сертификаты бесплатно, браузеры помечают обычный HTTP как «небезопасный», а HTTP/2 и HTTP/3 практически работают только поверх TLS.',
                'difficulty' => 1,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое REST API простыми словами?',
                'answer' => '**REST** — стиль построения HTTP API, где каждый объект (пользователь, заказ, статья) — это **ресурс** со своим URL: `/users`, `/users/42`, `/orders/13`.

Действия над ресурсом выражаются стандартными HTTP-методами:

- `GET` — получить.
- `POST` — создать.
- `PUT` / `PATCH` — обновить.
- `DELETE` — удалить.

Ответы обычно в **JSON**. Сервер **stateless** — каждый запрос самодостаточен, нужный контекст идёт в токене или куке.

Это противоположность **RPC**-стилю, где один URL и куча именованных действий внутри (`/api?action=createUser`).

В Laravel REST-маршруты разворачиваются одной строкой: `Route::apiResource(\'users\', UserController::class)`.',
                'code_example' => "GET    /api/users        # список\nGET    /api/users/42     # один\nPOST   /api/users        # создать\nPUT    /api/users/42     # обновить\nDELETE /api/users/42     # удалить",
                'code_language' => 'http',
                'difficulty' => 1,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Какие HTTP-методы считаются идемпотентными и почему это важно?',
                'answer' => 'Идемпотентный метод можно ретраить без побочного эффекта при повторе. GET — только чтение, повтор безопасен. PUT — заменяет ресурс целиком на одно и то же значение, второй вызов ничего не меняет. DELETE — после первого удаления повтор просто скажет 404/204, ничего лишнего не сломает. POST — НЕ идемпотентен, повтор создаст вторую запись. PATCH — обычно не идемпотентен (частичное изменение). Важность: если сеть «моргнула» и ответ потерялся, клиент может повторить идемпотентный запрос без страха задвоить операцию.',
                'difficulty' => 2,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Какой HTTP-заголовок отвечает за CORS и кто его проверяет?',
                'answer' => 'Главный заголовок — Access-Control-Allow-Origin, его присылает сервер в ответе и в нём перечислены разрешённые origins (или * для всех). Проверяет заголовок БРАУЗЕР, не сервер: если ответ пришёл без нужного заголовка, браузер блокирует доступ JS-кода к телу ответа. curl и Postman CORS игнорируют, потому что они не браузеры. Перед «сложными» запросами (нестандартные методы/заголовки) браузер шлёт preflight OPTIONS и читает Access-Control-Allow-Methods, Access-Control-Allow-Headers, Access-Control-Max-Age.',
                'difficulty' => 2,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое Same-Origin Policy простыми словами?',
                'answer' => 'Правило безопасности браузера: JavaScript со страницы https://a.com по умолчанию НЕ может читать ответы запросов на https://b.com. Origin — это тройка (схема, домен, порт): https://a.com:443 и http://a.com:80 — разные origin. Защищает от того, чтобы зловредный сайт в фоне дёргал ваш банковский API под вашими куками и читал ответы. Cross-origin запросы делать можно, но читать ответ — только если другой сайт явно разрешил через CORS-заголовки.',
                'difficulty' => 2,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое query-параметры в URL?',
                'answer' => '**Query-параметры** — часть URL после знака `?`, в формате `key=value`, склеенные через `&`: `/search?q=laravel&page=2&sort=date`.

- Используются в **`GET`**-запросах для фильтров, поиска, пагинации, сортировки.
- Спецсимволы кодируются по правилам URL-encoding: пробел → `%20` (или `+` в form-encoded), кириллица → `%D0%9F` и т.п. В PHP — `urlencode()` / `http_build_query()`.
- В Laravel читаются через `$request->query(\'page\')` или универсально `$request->input(\'page\')`.
- Один ключ может встречаться несколько раз: `?tag=php&tag=laravel`.',
                'code_example' => "GET /search?q=laravel&page=2&sort=date HTTP/1.1\nHost: example.com\n\n# PHP: \$request->query('page'); // \"2\"",
                'code_language' => 'http',
                'difficulty' => 1,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что значат коды 401 и 403?',
                'answer' => '- **`401 Unauthorized`** — клиент **не аутентифицирован**: токен или кука отсутствуют, истекли или невалидны, сервер не понимает, кто пришёл. Решение — залогиниться или обновить access-токен через refresh.
- **`403 Forbidden`** — клиент **аутентифицирован**, но прав на эту операцию **нет**: сервер тебя знает, но конкретная кнопка не для тебя (нет нужной роли, чужой ресурс, отключённый аккаунт). Решение — запросить доступ у админа.

Запоминалка: **`401` — «кто ты?»**, **`403` — «знаю тебя, но нельзя»**. Несмотря на название `Unauthorized`, по семантике `401` — это именно «не аутентифицирован».',
                'difficulty' => 1,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое JSON и почему он стал стандартом для API?',
                'answer' => '**JSON** (JavaScript Object Notation) — текстовый формат данных. Поддерживает:

- объекты `{"key": "value"}`
- массивы `[1, 2, 3]`
- числа, строки
- `true` / `false` / `null`

Парсится одной строкой в любом языке: `json_decode($s, true)` в PHP, `JSON.parse(s)` в JS, `json.loads` в Python.

**Почему стал стандартом для API:**

- Проще XML (нет тегов, атрибутов, namespace).
- Нативен для JavaScript-фронта.
- Компактнее.
- Не требует схемы — легко эволюционировать.

`Content-Type` для JSON — `application/json`. В Laravel `response()->json($data)` сам выставит заголовок и закодирует данные.',
                'code_example' => "{\n  \"id\": 42,\n  \"name\": \"John\",\n  \"is_admin\": true,\n  \"tags\": [\"admin\", \"editor\"],\n  \"manager\": null\n}",
                'code_language' => 'json',
                'difficulty' => 1,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Пример HTTP-запроса и ответа простыми словами',
                'answer' => 'Запрос — это текст: первая строка «метод путь версия», потом заголовки «Имя: значение», пустая строка, потом тело (для POST/PUT). Ответ — «версия статус», заголовки, пустая строка, тело. Посмотреть руками можно через curl -v или вкладку Network в DevTools браузера.',
                'code_example' => "# Запрос\nGET /api/users/42 HTTP/1.1\nHost: example.com\nAccept: application/json\n\n# Ответ\nHTTP/1.1 200 OK\nContent-Type: application/json\nContent-Length: 38\n\n{\"id\":42,\"name\":\"John\"}",
                'code_language' => 'http',
                'difficulty' => 2,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое Authorization header простыми словами?',
                'answer' => 'Заголовок HTTP, в котором клиент шлёт «удостоверение». Самые частые форматы: Authorization: Bearer <token> — для API-токенов и JWT (Laravel Sanctum/Passport). Authorization: Basic <base64(user:password)> — древняя Basic-аутентификация, только поверх HTTPS. Authorization: Digest ..., Authorization: AWS4-HMAC-SHA256 ... — реже. Сервер по этому заголовку понимает, от чьего имени запрос.',
                'code_example' => "GET /api/me HTTP/1.1\nHost: example.com\nAuthorization: Bearer eyJhbGciOiJIUzI1NiJ9...",
                'code_language' => 'http',
                'difficulty' => 2,
                'topic' => 'networking.http',
            ],
            [
                'category' => 'Сети',
                'question' => 'Что такое CORS, зачем preflight-запрос и как настраивать?',
                'answer' => 'CORS (Cross-Origin Resource Sharing) — браузерный механизм, разрешающий или запрещающий JS на странице A.com обращаться к ресурсам на B.com. По умолчанию same-origin policy: запрос из app.example.com на api.example.com — это cross-origin (даже совпадение поддомена считается разным origin). Защищает от: токены/куки одного сайта не должны утекать в скрипты другого. Простые запросы (simple requests) — GET/HEAD/POST с «безопасными» content-type (application/x-www-form-urlencoded, multipart/form-data, text/plain) — браузер шлёт обычный запрос с заголовком Origin: https://app.example.com. Сервер должен ответить Access-Control-Allow-Origin: https://app.example.com (или *), иначе браузер скроет ответ от JS (но запрос дойдёт до сервера — это важно для CSRF-думания). Preflight — для «сложных» запросов (PUT/DELETE/PATCH, application/json, кастомные заголовки): браузер сначала шлёт OPTIONS-запрос с заголовками Access-Control-Request-Method и Access-Control-Request-Headers. Сервер отвечает Access-Control-Allow-Methods, Access-Control-Allow-Headers, Access-Control-Max-Age (сколько секунд кэшировать preflight, чтобы не дёргать каждый раз — типично 86400). Только потом браузер шлёт реальный запрос. Куки: чтобы cross-origin запрос отправлял куки, нужны Access-Control-Allow-Credentials: true + fetch с credentials: \'include\' + НЕЛЬЗЯ Origin: *, нужно конкретный домен. Типовые ошибки: 1) Allow-Origin: * + Allow-Credentials: true — браузер блокирует. 2) Забыли отвечать на OPTIONS (некоторые middleware фильтруют). 3) Думают, что CORS — это security: на самом деле это POLICY браузера; сервер защищать от прямых запросов всё равно надо.',
                'difficulty' => 4,
                'topic' => 'networking.http',
            ],
        ];
    }
}
