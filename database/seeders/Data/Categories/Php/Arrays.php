<?php

namespace Database\Seeders\Data\Categories\Php;

class Arrays
{
    public static function all(): array
    {
        return [
            [
                'category' => 'PHP',
                'question' => 'Как работает array_map?',
                'answer' => '**`array_map($callback, $array)`** — применяет `$callback` к **каждому элементу** массива и возвращает **новый** массив с результатами.

**Свойства:**
- **не мутирует** исходный массив (иммутабельная операция)
- **ключи сохраняются** для одного массива
- если массивов несколько — **ключи сбрасываются** в `0, 1, 2...`

**С несколькими массивами:** `array_map(fn($x, $y) => $x + $y, $a, $b)` — callback получает по одному элементу из каждого массива; результат — поэлементное применение.

**Спецтрюк:** `array_map(null, $a, $b)` — «склеивание» массивов в пары (zip).',
                'code_example' => '<?php
$nums = [1, 2, 3, 4];
$squares = array_map(fn($x) => $x ** 2, $nums);
// [1, 4, 9, 16]

// С несколькими массивами
$a = [1, 2, 3];
$b = [10, 20, 30];
$sums = array_map(fn($x, $y) => $x + $y, $a, $b);
// [11, 22, 33]

// С null callback - "склеивание"
$result = array_map(null, $a, $b);
// [[1,10], [2,20], [3,30]]',
                'code_language' => 'php',
                'difficulty' => 2,
                'topic' => 'php.arrays',
            ],
            [
                'category' => 'PHP',
                'question' => 'Чем отличаются array_filter и array_map?',
                'answer' => 'Обе функции принимают массив и callback, **не мутируют** оригинал — но делают разное.

- **`array_map`** — **преобразует** каждый элемент. Размер массива не меняется.
- **`array_filter`** — **отсеивает**. Оставляет только те элементы, для которых callback вернул **truthy**. Размер обычно уменьшается.

**Ключи в обоих сохраняются** — после `array_filter` массив может иметь «дыры» в индексах (`[0 => "a", 2 => "c"]`). Чтобы сбросить — оберни в **`array_values()`**.

**`array_filter` без callback** — удаляет все **falsy** значения: `0`, `""`, `null`, `false`, `[]`, `"0"`.

**Третий аргумент `array_filter`** — режим: `ARRAY_FILTER_USE_KEY` (callback получает ключ), `ARRAY_FILTER_USE_BOTH` (ключ и значение).',
                'code_example' => '<?php
$nums = [1, 2, 3, 4, 5];

// filter - оставить чётные
$even = array_filter($nums, fn($x) => $x % 2 === 0);
// [1 => 2, 3 => 4] - ключи сохранены!

// Сбросить ключи
$even = array_values($even);

// filter без callback - убрать falsy
$cleaned = array_filter([1, 0, "", "a", null, false]);
// [0 => 1, 3 => "a"]

// map - преобразовать
$doubled = array_map(fn($x) => $x * 2, $nums);
// [2, 4, 6, 8, 10]',
                'code_language' => 'php',
                'difficulty' => 2,
                'topic' => 'php.arrays',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как работает array_reduce?',
                'answer' => '**`array_reduce($arr, $cb, $initial = null)`** — **сворачивает** массив в одно значение через callback.

**Сигнатура callback:** `fn($carry, $item) => $newCarry`
- **`$carry`** — аккумулятор (на первом шаге — `$initial`)
- **`$item`** — текущий элемент
- возвращает **новое** значение аккумулятора

**Применения:**
- **сумма/произведение**: `array_reduce($n, fn($c, $x) => $c + $x, 0)`
- **конкатенация** строк
- поиск **min/max** при отсутствии готовых функций
- построение **деревьев** из плоских списков
- группировка/индексация

**Подводные камни:**
- по умолчанию `$initial = null` — для арифметики **всегда** передавайте `0`/`""`, иначе на первом шаге будет `null + $x`
- **не передаёт ключи** в callback — для работы с ключами берите `foreach` или `array_walk`
- **не оптимизирован** под PHP — на больших массивах вызов callback на каждом шаге создаёт overhead; в горячем пути иногда быстрее обычный `foreach`

**Альтернативы для частых задач:**
- сумма — **`array_sum()`**
- произведение — **`array_product()`**
- эти C-нативные и в разы быстрее `array_reduce`',
                'code_example' => '<?php
$nums = [1, 2, 3, 4, 5];

// Сумма
$sum = array_reduce($nums, fn($carry, $x) => $carry + $x, 0);
// 15

// Произведение
$prod = array_reduce($nums, fn($c, $x) => $c * $x, 1);
// 120

// Объединение объектов
$users = [["age" => 20], ["age" => 30], ["age" => 25]];
$totalAge = array_reduce($users, fn($c, $u) => $c + $u["age"], 0);
// 75',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.arrays',
            ],
            [
                'category' => 'PHP',
                'question' => 'Чем отличается array_merge от оператора + для массивов?',
                'answer' => 'Две функции выглядят похоже, но ведут себя **совершенно по-разному** — частая ловушка на собесе.

| Поведение | `array_merge($a, $b)` | `$a + $b` |
|---|---|---|
| Конфликт **строкового** ключа | **правый** выигрывает | **левый** выигрывает |
| Конфликт **числового** ключа | элементы добавляются, **перенумеровка** с 0 | левый выигрывает, **без** перенумеровки |
| Сохраняет числовые ключи | **нет** | **да** |

**Простое правило:**
- **`+`** — «union с приоритетом левого», числовые ключи **не трогает**
- **`array_merge`** — «правый перезаписывает строковые, числовые конкатенирует»

**Spread `[...$a, ...$b]`** (PHP 7.4+, со строковыми ключами с PHP 8.1):
- ведёт себя **как `array_merge`** (правый перезаписывает строковые)
- но **переиндексирует** числовые ключи

**Когда что выбирать:**
- слить **конфиги** с дефолтами и переопределениями (правый выигрывает) → **`array_merge`** или **spread**
- объединить два массива с числовыми ключами и **сохранить ключи** → **`+`**
- объединить ассоциативные массивы с **приоритетом левого** → **`+`**',
                'code_example' => '<?php
$a = ["a" => 1, "b" => 2, 0 => "x"];
$b = ["b" => 3, "c" => 4, 0 => "y"];

print_r(array_merge($a, $b));
// ["a" => 1, "b" => 3, "c" => 4, 0 => "x", 1 => "y"]
// числовой ключ перенумерован, "b" взято из $b

print_r($a + $b);
// ["a" => 1, "b" => 2, 0 => "x", "c" => 4]
// "b" из $a, ключ 0 из $a, "c" добавлен из $b

// Spread (PHP 7.4+, с PHP 8.1 со строковыми ключами)
$merged = [...$a, ...$b];',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.arrays',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как проверить наличие элемента в массиве?',
                'answer' => 'Несколько функций — каждая со своими ловушками.

**Проверка значения:**
- **`in_array($val, $arr, true)`** — есть ли элемент, **третий аргумент `true` обязателен** (без него — `==` с приведением, например `0 == "string"` → `true`)
- **`array_search($val, $arr, true)`** — возвращает **ключ** или `false`

**Ловушка `array_search`:** для **первого** элемента вернёт ключ `0`, а `0 == false` → `true`. **Всегда** сравнивайте через **`!== false`**, не через `== false` / `!$result`. Та же проблема у `strpos`.

**Проверка ключа:**

| Функция | Поведение при `null` |
|---|---|
| `isset($arr[$key])` | **`false`** на `null`-значении ⚠️ |
| `array_key_exists($key, $arr)` | **`true`** даже на `null`-значении |

**Производительность:**

| Сценарий | Лучше |
|---|---|
| **Разовая** проверка | `in_array($v, $arr, true)` |
| **Множественные** lookup-ы по одному массиву | **`array_flip` один раз → `isset`** |

**Расчёт:** `M` проверок по `N`-элементному массиву: наивный `in_array` — **O(N×M)**, flip + isset — **O(N+M)**. Но flip аллоцирует новый массив (память O(N) + проход по оригиналу), для разовой проверки невыгодно.

**PHP 8.4:** новые `array_any` / `array_all` / `array_find` / `array_find_key` — возвращают bool/значение и **останавливаются на первом совпадении** (с callback-логикой).',
                'code_example' => '<?php
$users = ["Иван", "Аня", "Петя"];

var_dump(in_array("Аня", $users));      // true
var_dump(in_array("0", $users, true));  // false (строго)

$key = array_search("Иван", $users);    // int(0) - первый элемент!

// ❌ Опасно - 0 == false → true, считаем "не найдено"
if ($key == false) { echo "not found"; } // НЕВЕРНО для первого элемента

// ✅ Правильно - строгое сравнение
if ($key === false) { echo "not found"; }
if (($key = array_search("X", $users)) !== false) { /* нашли */ }

$data = ["name" => null];
isset($data["name"]);              // false (null!)
array_key_exists("name", $data);   // true

// Быстрая проверка для большого набора
$set = array_flip(["a", "b", "c"]); // ["a"=>0, "b"=>1, "c"=>2]
isset($set["a"]); // O(1)',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.arrays',
            ],
            [
                'category' => 'PHP',
                'question' => 'Чем отличаются array_keys и array_values?',
                'answer' => '- **`array_keys($arr)`** — возвращает массив всех **ключей**, перенумерованный с 0.
- **`array_values($arr)`** — возвращает массив **значений**, перенумерованный с 0 (теряет исходные ключи).

**Полезные приёмы:**
- **`array_keys($arr, $value)`** — вернёт только ключи, где значение равно `$value` (можно третьим параметром `true` сделать сравнение строгим)
- **`array_values(array_filter(...))`** — типичная связка для сброса «дырявых» ключей после фильтрации
- **`array_combine($keys, $values)`** — обратная операция: собрать ассоциативный массив из двух массивов',
                'code_example' => '<?php
$user = ["name" => "Иван", "age" => 30, "role" => "admin"];

print_r(array_keys($user));
// [0 => "name", 1 => "age", 2 => "role"]

print_r(array_values($user));
// [0 => "Иван", 1 => 30, 2 => "admin"]

// Ключи с конкретным значением
$status = ["a" => "ok", "b" => "fail", "c" => "ok"];
print_r(array_keys($status, "ok"));
// [0 => "a", 1 => "c"]

// Сброс ключей после filter
$clean = array_values(array_filter($items));',
                'code_language' => 'php',
                'difficulty' => 2,
                'topic' => 'php.arrays',
            ],
            [
                'category' => 'PHP',
                'question' => 'Чем отличается == от === при сравнении массивов?',
                'answer' => 'Сравнение массивов — отдельные правила, не как у скаляров и объектов.

| Оператор | Что требует |
|---|---|
| **`==`** (loose) | те же пары `ключ => значение` в **любом порядке**, значения сравнивает через `==` (с приведением) |
| **`===`** (strict) | те же пары, **одинаковый порядок ключей**, значения сравнивает через `===` |

**Примеры:**

```php
// Индексированные
[1, 2] == [1, 2];      // true
[1, 2] == [2, 1];      // false — разные пары ключ→значение

// Ассоциативные — порядок ключей различает только ===
["x" => 1, "y" => 2] == ["y" => 2, "x" => 1];   // true
["x" => 1, "y" => 2] === ["y" => 2, "x" => 1];  // false ⚠️

// Приведение под ==
["n" => "1"] == ["n" => 1];   // true
["n" => "1"] === ["n" => 1];  // false
```

**Где это всплывает в тестах:**
- **`assertEquals($a, $b)`** — использует `==`, игнорирует порядок и тип
- **`assertSame($a, $b)`** — использует `===`, ловит порядок и тип

`assertSame` поймает баги, где код случайно перемешал порядок ключей.

**Подводный камень JSON-сравнения:** `json_encode` **сохраняет порядок ключей**, поэтому сравнение через `json_encode($a) === json_encode($b)` — **не workaround** для `==`.

**Правило:** для уверенного сравнения структур — **`===`** или `assertSame`.',
                'code_example' => '<?php
// Индексированные массивы
var_dump([1, 2, 3] == [1, 2, 3]);   // true
var_dump([1, 2, 3] === [1, 2, 3]);  // true
var_dump([1, 2] == [2, 1]);          // false (разные пары ключ→значение)
var_dump([1, 2] === [2, 1]);         // false

// Ассоциативные - порядок ключей различает только ===
$a = ["x" => 1, "y" => 2];
$b = ["y" => 2, "x" => 1];

var_dump($a == $b);   // true  - те же пары
var_dump($a === $b);  // false - разный порядок ключей

// Type juggling под == с приведением
var_dump(["n" => "1"] == ["n" => 1]);   // true  (string "1" == int 1)
var_dump(["n" => "1"] === ["n" => 1]);  // false (разные типы)

// На собеседовании: для уверенного сравнения структур всегда ===',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.arrays',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что произойдёт, если внутри foreach изменять сам массив (добавлять/удалять элементы)?',
                'answer' => '**`foreach` внутри работает со СНИМКОМ массива** — через **copy-on-write**:
- пока никто не пишет, физическая копия **не создаётся**
- как только начинаются модификации — PHP **отделяет копию** для итерации
- цикл продолжает идти по **исходным** элементам

**Поведение по форме `foreach`:**

| Форма | Что работает с массивом | Модификации видны в цикле? |
| --- | --- | --- |
| **`foreach ($arr as $v)`** | snapshot (CoW) | **нет** — итерация идёт по копии |
| **`foreach ($arr as $k => $v)`** | snapshot (CoW) | **нет** |
| **`foreach ($arr as &$v)`** | **реальный** массив | **да** — undefined behavior |
| **`foreach ($obj as $v)`** где `$obj instanceof Iterator` | реальный итератор | **да** — зависит от реализации |

**Опасности `foreach by reference` + модификация:**
- **бесконечный цикл** — добавленные в конец элементы продолжают итерироваться
- **пропуски** — при `unset` индексы съезжают
- **краш** — undefined behavior, в разных версиях PHP по-разному

**Правильный паттерн для модификации в процессе:**

| Задача | Решение |
| --- | --- |
| Изменить **значения** | `foreach ($arr as $k => $v) { $arr[$k] = transform($v); }` |
| **Удалить** некоторые элементы | собрать ключи во второй цикл `unset` или `array_filter` |
| **Добавить** элементы | накопить в `$toAdd[]`, **после** цикла `array_merge` |

**Подкапотные нюансы:**
- snapshot **не означает копию памяти**: CoW делает копию **только при первой модификации** в цикле — обычный read-only `foreach` остаётся **бесплатным** по памяти
- `foreach by reference` создаёт **флаг `is_ref`** на zval — отключает CoW для всего массива (минус для последующего кода)',
                'code_example' => '<?php
// foreach по значению - изменения вне цикла не влияют
$arr = [1, 2, 3];
foreach ($arr as $v) {
    if ($v === 2) $arr[] = 100;  // добавляем
    echo $v . " ";                // 1 2 3 - 100 НЕ напечатано
}
print_r($arr);  // массив реально стал [1, 2, 3, 100]

// foreach по ссылке + модификация - undefined behavior
$arr = [1, 2, 3];
foreach ($arr as &$v) {
    if ($v === 2) $arr[] = 100; // потенциальный бесконечный цикл / краш
}
// НЕ ДЕЛАЙТЕ ТАК

// Безопасный паттерн для модификации в процессе:
// собрать изменения отдельно, применить после цикла
$toAdd = [];
foreach ($arr as $v) {
    if (needsExpansion($v)) $toAdd[] = expand($v);
}
$arr = array_merge($arr, $toAdd);',
                'code_language' => 'php',
                'difficulty' => 4,
                'topic' => 'php.arrays',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что делает array_diff и чем отличается от array_intersect?',
                'answer' => '- **`array_diff($a, $b, ...)`** — возвращает значения **из `$a`, которых нет** в остальных массивах. Аналог «вычитания» множеств.
- **`array_intersect($a, $b, ...)`** — возвращает значения, **присутствующие во всех** массивах. Аналог пересечения.

**Оба сравнивают значения через `(string)$x === (string)$y`** — не `==` и не `===`. Поэтому на объектах без `__toString` будет ошибка, а у `float` возможны сюрпризы с округлением.

**Варианты:**
- **`*_key`** (`array_diff_key`, `array_intersect_key`) — сравнивают **только ключи**
- **`*_assoc`** (`array_diff_assoc`, `array_intersect_assoc`) — сравнивают **и ключи, и значения**
- **`array_udiff` / `array_uintersect`** — callback-варианты для **своего** сравнения (например, по `===` или по полю объекта)',
                'code_example' => '<?php
$a = [1, 2, 3, 4, 5];
$b = [3, 4, 5, 6, 7];

print_r(array_diff($a, $b));
// [0 => 1, 1 => 2] - что есть в $a, но нет в $b

print_r(array_intersect($a, $b));
// [2 => 3, 3 => 4, 4 => 5] - общие

// По ключам
$x = ["a" => 1, "b" => 2];
$y = ["a" => 5, "c" => 3];
print_r(array_diff_key($x, $y));   // ["b" => 2]
print_r(array_diff_assoc($x, $y)); // ["a" => 1, "b" => 2]',
                'code_language' => 'php',
                'difficulty' => 2,
                'topic' => 'php.arrays',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как отсортировать массив в PHP?',
                'answer' => 'Семейство функций сортировки — все они **мутируют** исходный массив (передача по ссылке) и возвращают `bool`.

**По значениям:**
- **`sort` / `rsort`** — по возрастанию/убыванию, **ключи теряются** (перенумеровка)
- **`asort` / `arsort`** — по значениям, **ключи сохраняются** (для ассоциативных массивов)

**По ключам:**
- **`ksort` / `krsort`** — сортирует по ключу

**С callback:**
- **`usort($a, fn($x, $y) => $x <=> $y)`** — своя логика сравнения, ключи теряются
- **`uasort`** — то же, но ключи сохраняются
- **`uksort`** — callback сортирует ключи

**Специальные:**
- **`natsort`** — «натуральная» сортировка: `file2 < file10` (обычная сортировка дала бы `file10 < file2`)
- **`array_multisort`** — сортировка нескольких массивов параллельно

**С PHP 8.0** все встроенные сортировки **стабильны** (равные элементы сохраняют порядок).',
                'code_example' => '<?php
$nums = [3, 1, 4, 1, 5, 9, 2, 6];

sort($nums);  // мутирует! [1, 1, 2, 3, 4, 5, 6, 9]
rsort($nums); // по убыванию

$users = ["c" => 3, "a" => 1, "b" => 2];
asort($users); // сохраняет ключи: ["a"=>1, "b"=>2, "c"=>3]
ksort($users); // сортирует по ключам

// Кастомная
$products = [
    ["name" => "B", "price" => 100],
    ["name" => "A", "price" => 50],
];
usort($products, fn($x, $y) => $x["price"] <=> $y["price"]);

// Натуральная
$files = ["img10", "img2", "img1"];
natsort($files); // img1, img2, img10',
                'code_language' => 'php',
                'difficulty' => 2,
                'topic' => 'php.arrays',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как объединить массив в строку и обратно?',
                'answer' => '**Массив → строка:**
- **`implode($glue, $array)`** — склеивает элементы через разделитель. **`join()`** — синоним.

**Строка → массив:**
- **`explode($delimiter, $string, $limit)`** — разбивает по разделителю.
- Третий параметр `$limit`:
  - **положительный** — максимум кусков (последний кусок «соберёт» хвост)
  - **отрицательный** — сколько кусков **отбросить с конца**
  - **`0`** трактуется как `1`

**Альтернативы:**
- **`str_split($str, $length)`** — разбивает на части **фиксированной длины** (по байтам! для UTF-8 — `mb_str_split`)
- **`preg_split("/$regex/", $str)`** — разбивает по **регулярке** (например, по любому из нескольких разделителей)

**Внимание:** разделитель `implode` должен быть **строкой**, а не массивом — иначе ошибка с PHP 8.',
                'code_example' => '<?php
$arr = ["apple", "banana", "cherry"];
echo implode(", ", $arr); // "apple, banana, cherry"

$str = "one,two,three,four";
print_r(explode(",", $str));
// ["one", "two", "three", "four"]

// Ограничение
print_r(explode(",", $str, 2));
// ["one", "two,three,four"]

print_r(explode(",", $str, -1));
// ["one", "two", "three"] - отбросили последний

// На фиксированные части
print_r(str_split("abcdef", 2));
// ["ab", "cd", "ef"]',
                'code_language' => 'php',
                'difficulty' => 2,
                'topic' => 'php.arrays',
            ],
            [
                'category' => 'PHP',
                'question' => 'Какая алгоритмическая сложность у популярных операций над массивами в PHP?',
                'answer' => 'PHP-массив — **упорядоченная hashtable** (`HashTable` в Zend Engine), **не** классический индексный массив C. Это даёт смешанные сложности.

**O(1) — амортизированно:**

| Операция | Что делает |
| --- | --- |
| **`array_push($a, $v)`** / **`$a[] = $v`** | вставка в конец |
| **`array_pop($a)`** | снять с конца |
| **`isset($a[$k])`** | hash-lookup |
| **`array_key_exists($k, $a)`** | hash-lookup (видит ключи с `null`) |
| **`unset($a[$k])`** | пометить ячейку tombstone — **НЕ переиндексирует** оставшиеся integer-ключи |
| **`count($a)`** | хранится отдельно в HashTable |

**O(N):**

| Операция | Почему |
| --- | --- |
| **`array_unshift($a, $v)`** | сдвигает **все** integer-ключи на +1 |
| **`array_shift($a)`** | удаляет первый + сдвигает **все** integer-ключи на −1 |
| **`in_array($v, $a)`** / **`array_search`** | линейный поиск **по значениям** |
| `array_values`, `array_keys`, `array_flip`, `array_merge` | полный проход |
| `array_filter`, `array_map`, `array_reduce` | полный проход + callback |

**O(N log N):**
- `sort`, `asort`, `ksort`, `usort`, `array_multisort` — quicksort-вариант внутри (стабильный с PHP 8.0)

**Senior-приёмы:**

| Антипаттерн | Решение |
| --- | --- |
| `in_array` в цикле — **O(N×M)** | **`array_flip` один раз** + `isset` — **O(N+M)** |
| `array_shift` в цикле — **O(N²)** | **`SplQueue` / `SplDoublyLinkedList`** — O(1) на оба конца |
| `array_unshift` в цикле — **O(N²)** | накопить в обычный массив + **`array_reverse`** в конце |
| FIFO для очереди задач | `SplQueue` либо просто индекс-переменная без мутации |

**Расход памяти:**
- **обычный PHP-массив тяжёлый**: один элемент ≈ **100+ байт** (hashtable bucket + zval)
- **packed array** (последовательные int-ключи начиная с 0) — компактнее (PHP 7+)
- для **больших однотипных данных** — **`SplFixedArray`** (фиксированный размер, только int-ключи) или объекты с **типизированными свойствами**
- **memory_get_usage()** до и после — единственный надёжный способ замерить',
                'code_example' => '<?php
// ❌ Плохо: O(N²) - линейный поиск внутри цикла
$ids = range(1, 100_000);
$lookup = [42, 7, 99_999];
foreach ($lookup as $id) {
    if (in_array($id, $ids)) { /* O(N) на каждой итерации */ }
}

// ✅ Хорошо: O(N) на flip + O(1) на проверку
$flipped = array_flip($ids); // [1=>0, 2=>1, ...]
foreach ($lookup as $id) {
    if (isset($flipped[$id])) { /* O(1) */ }
}

// ❌ Плохо: O(N²) на разворот через unshift в цикле
$result = [];
foreach ($items as $item) {
    array_unshift($result, $item); // сдвиг всех элементов
}

// ✅ Хорошо: O(N) push + reverse
$result = [];
foreach ($items as $item) {
    $result[] = $item;     // O(1)
}
$result = array_reverse($result); // O(N)

// Очередь FIFO
$q = new SplQueue();
$q->enqueue("a"); $q->dequeue(); // обе O(1), без сдвигов',
                'code_language' => 'php',
                'difficulty' => 4,
                'topic' => 'php.arrays',
            ],
            [
                'category' => 'PHP',
                'question' => 'В чём разница между array_slice и array_splice?',
                'answer' => 'Похожие имена — **совершенно разное поведение**, классический вопрос-ловушка.

| | **`array_slice`** | **`array_splice`** |
|---|---|---|
| Сигнатура | `array_slice($arr, $offset, $length)` | `array_splice(&$arr, $offset, $length, $replace)` |
| Мутация исходного | **нет** (immutable) | **да** (передача по **ссылке**) |
| Возвращает | новый массив (срез) | **вырезанные** элементы |
| Замена | — | да, 4-й аргумент `$replacement` |
| Перенумеровка int-ключей | **нет**, если `preserve_keys=true` (4-й arg) | **всегда** |
| Строковые ключи | сохраняются | сохраняются |

**Когда что использовать:**

| Задача | Функция |
|---|---|
| Получить кусок без мутации | **`array_slice`** |
| Удалить элемент **по индексу** с перенумеровкой | **`array_splice($arr, $i, 1)`** |
| Вставить элементы посередине | **`array_splice($arr, $i, 0, [...])`** (0 в `length`) |
| Заменить кусок | **`array_splice($arr, $i, $len, $new)`** |

**Подводные камни:**
- **`unset($arr[$i])`** оставляет «дыру» в индексах (`[0, 1, 3]`) — `array_splice` переиндексирует
- `array_splice` через **ссылку** — редкий случай, когда stdlib-функция мутирует аргумент; легко не заметить
- для **immutable** удаления — `array_values(array_filter($arr, fn($v, $k) => $k !== $i, ARRAY_FILTER_USE_BOTH))`',
                'code_example' => '<?php
$arr = [10, 20, 30, 40, 50];

// array_slice - копия
$piece = array_slice($arr, 1, 2);
// $piece = [20, 30]
// $arr   = [10, 20, 30, 40, 50] - не изменился

// array_splice - мутирует, возвращает вырезанное
$cut = array_splice($arr, 1, 2);
// $cut = [20, 30]
// $arr = [10, 40, 50] - элементы удалены, ключи пере-нумерованы

// array_splice + замена
$arr = [10, 20, 30, 40, 50];
array_splice($arr, 1, 2, ["a", "b", "c"]);
// $arr = [10, "a", "b", "c", 40, 50]

// preserve_keys для slice
$assoc = ["x" => 1, 5 => "z", "y" => 2];
print_r(array_slice($assoc, 1, 2));
// ["x"=>... убрано, числовой ключ пере-нумерован]
print_r(array_slice($assoc, 1, 2, preserve_keys: true));
// числовые ключи сохранены

// "Удалить элемент по индексу" - частый use case
unset($arr[2]);                    // оставит "дыру" в индексах [0,1,3,4]
array_splice($arr, 2, 1);          // переиндексирует, удалит элемент
$arr = array_values(array_filter($arr, fn($_, $i) => $i !== 2,
                                  ARRAY_FILTER_USE_BOTH)); // immutable',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.arrays',
            ],
            [
                'category' => 'PHP',
                'question' => 'Почему array_shift медленный и какие быстрые альтернативы без SPL?',
                'answer' => '**`array_shift($arr)`** удаляет **ПЕРВЫЙ** элемент массива и возвращает его, при этом **РЕИНДЕКСИРУЕТ все целочисленные ключи**: `1→0, 2→1, ... N→N-1`.

**Стоимость:**
- **O(N)** на каждый вызов
- цикл **`while ($x = array_shift($arr))`** даёт **O(N²)** — на 100 000 элементов это **десятки секунд**
- та же проблема у **`array_unshift`** (вставка в начало)

**Сравнение решений:**

| Подход | Сложность total | Когда применять |
| --- | --- | --- |
| **`SplQueue` / `SplDoublyLinkedList`** | **O(N)** | стандартный FIFO с большим N |
| **`array_reverse` + `array_pop`** в цикле | **O(N)** | SPL недоступен, можно поменять направление |
| **Индекс-переменная** (`$i++`) | **O(N)** | не нужно физически удалять, OK если память хватает |
| **Внутренний pointer** (`each`/`next`/`reset`) | O(N) | устарело с PHP 8.0 — `each()` удалён |
| **Generator** обёртка | O(N) | стримим из источника, нет массива целиком |

**Почему `array_pop` — O(1):**
- удаляет **последний** элемент → **не нужно сдвигать** integer-ключи
- HashTable просто уменьшает `nNextFreeElement`

**Когда `array_shift` всё-таки OK:**
- массив **маленький** (десятки-сотни элементов) — overhead на SPL может перевесить
- **разовый** вызов вне цикла
- ключи **строковые** — сдвига integer-индексов всё равно не происходит (но реиндексация остальных может быть)

**Senior-правило:** помнить, что **`array_shift` / `array_unshift` — O(N)**, и для **очередей с большим N** брать **SPL** или **реверс+pop**.',
                'code_example' => '<?php
// ❌ O(N²) - каждое array_shift сдвигает все индексы
$queue = range(1, 100000);
while (count($queue) > 0) {
    $task = array_shift($queue); // O(N) на каждой итерации
    process($task);
}
// На 100k элементов - десятки секунд

// ✅ O(N) total - один раз reverse, дальше pop с конца
$queue = range(1, 100000);
$queue = array_reverse($queue); // O(N) один раз
while (count($queue) > 0) {
    $task = array_pop($queue);   // O(1) - снимает с конца, не сдвигает
    process($task);
}
// Те же 100k - доли секунды

// ✅ Альтернатива - SPL с O(1) на оба конца
$queue = new SplQueue();
foreach (range(1, 100000) as $n) $queue->enqueue($n);
while (!$queue->isEmpty()) {
    $task = $queue->dequeue(); // O(1)
    process($task);
}',
                'code_language' => 'php',
                'difficulty' => 4,
                'topic' => 'php.arrays',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как использовать распаковку массивов со строковыми ключами через ...?',
                'answer' => '**С PHP 8.1** оператор **`...`** внутри литерала массива поддерживает **строковые ключи**:

```php
$config = [...$defaults, ...$custom];
```

**Поведение:**
- работает как **`array_merge`** — правый операнд **перезаписывает** одноимённые строковые ключи левого
- **числовые ключи переиндексируются** с 0 (как в `array_merge`)
- порядок ключей — как при перечислении

**До PHP 8.1:** спред с массивом со строковыми ключами вызывал **fatal error** — приходилось писать `array_merge($defaults, $custom)`.

**Чем спред удобнее `array_merge`:**
- **читабельнее** — литеральный синтаксис, не вызов функции
- можно **вставлять отдельные элементы** между раскрытиями: `[...$a, "extra" => 1, ...$b]`
- работает в **константных выражениях** (8.1+) — можно использовать в дефолтах параметров и константах класса

**Когда спред уступает:**
- если массивов **много и они в массиве массивов** — `array_merge(...$arrays)` короче, чем длинная цепочка spread

**Подводные камни:**
- порядок имеет значение: **последний** упомянутый ключ выигрывает
- при работе с **нестандартными ключами** (`"0"` как строка vs `0` как int) — PHP приведёт строки-цифры к int, можно получить неожиданный мердж',
                'code_example' => '<?php
$defaults = ["timeout" => 30, "retries" => 3];
$custom   = ["timeout" => 10, "ssl" => true];

// PHP 8.1+
$config = [...$defaults, ...$custom];
// ["timeout" => 10, "retries" => 3, "ssl" => true]
// timeout перебит правым (как и в array_merge)',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.arrays',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что делает функция array_is_list()?',
                'answer' => '**`array_is_list($arr): bool`** (PHP 8.1+) — `true`, если массив является **списком**, то есть:
- ключи — **последовательные `int`**
- начинаются с **`0`**
- **без пропусков**

**Что считается списком:**

| Массив | Результат |
|---|---|
| `[1, 2, 3]` | **`true`** |
| `[]` | **`true`** (пустой — тривиально список) |
| `[0 => "a", 1 => "b"]` | `true` |
| `[1 => "a", 2 => "b"]` | `false` — не с 0 |
| `["a" => 1]` | `false` — строковые ключи |
| `[0 => "a", 2 => "b"]` | `false` — пропуск |

**Зачем нужно — `json_encode`:**

PHP-массив один на оба случая, но JSON различает **массив `[...]`** и **объект `{...}`**. `json_encode` смотрит именно на «list-ness»:
- list → `[1, 2, 3]`
- иначе → `{"0": "a", "2": "b"}`

Это типичная причина «странного JSON» — после `unset` или `array_filter` без `array_values` массив перестаёт быть списком, и API внезапно отдаёт object вместо array.

**До 8.1** проверяли так:
```php
$arr === array_values($arr)   // быстрее
array_keys($arr) === range(0, count($arr) - 1)  // медленнее
```

**Применение:** перед `json_encode`, в DTO для валидации, в сериализаторах.',
                'code_example' => '<?php
array_is_list([1, 2, 3]);                  // true
array_is_list([]);                          // true
array_is_list([0 => "a", 1 => "b"]);        // true
array_is_list([1 => "a", 2 => "b"]);        // false — не с 0
array_is_list(["a" => 1]);                  // false — строковые ключи
array_is_list([0 => "a", 2 => "b"]);        // false — пропуск

// json_encode серилизует list как [...], а dict как {...}
echo json_encode([1, 2, 3]);                // "[1,2,3]"
echo json_encode([0 => "a", 2 => "b"]);     // \'{"0":"a","2":"b"}\'',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.arrays',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как работает деструктуризация массива со строковыми ключами?',
                'answer' => '**Деструктуризация со строковыми ключами** (PHP 7.1+):
```php
["name" => $name, "age" => $age] = $person;
```
Извлекает значения по ключам из ассоциативного массива в **одноимённые переменные**.

**Что заменяет:**
- старый `list($name, $age) = ...` (работал **только** с числовыми ключами по порядку)
- цепочки `$name = $person["name"]; $age = $person["age"]`

**Возможности:**
- **переименование переменной**: `["name" => $userName] = $person`
- **вложенная** деструктуризация: `["user" => ["id" => $id]] = $data`
- **в `foreach`**: `foreach ($users as ["id" => $id, "name" => $n]) {...}` — очень удобно
- **в параметрах функции** — **нет**, только присваивание

**Подводные камни:**
- отсутствующий ключ даёт **`Warning: Undefined array key`** и **`null`**
- нет способа задать **дефолт** прямо в деструктуризации (как `const { a = 1 } = ...` в JS)
- обходные пути: `["a" => $a] = $arr + ["a" => "default"]` (через `+`) или `??` после извлечения

**На практике:** чаще всего в `foreach` по списку строк БД или JSON-ответу, когда нужно сразу разложить запись в переменные.',
                'code_example' => '<?php
$person = ["name" => "Иван", "age" => 30, "email" => "i@i.ru"];

["name" => $name, "age" => $age] = $person;
echo $name; // "Иван"

// Вложенная деструктуризация
$data = ["user" => ["id" => 42, "name" => "Аня"]];
["user" => ["id" => $id, "name" => $name]] = $data;

// В foreach
$users = [["id" => 1, "name" => "A"], ["id" => 2, "name" => "B"]];
foreach ($users as ["id" => $id, "name" => $name]) {
    echo "$id: $name ";
}',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.arrays',
            ],
        ];
    }
}
