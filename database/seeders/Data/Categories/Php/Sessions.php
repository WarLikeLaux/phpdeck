<?php

namespace Database\Seeders\Data\Categories\Php;

class Sessions
{
    public static function all(): array
    {
        return [
            [
                'category' => 'PHP',
                'question' => 'Как работают сессии в PHP?',
                'answer' => '**Сессия** — механизм хранения данных пользователя **между запросами**.

**Как устроено:**
1. **`session_start()`** инициализирует/восстанавливает сессию (читает или создаёт ID)
2. **PHP генерирует уникальный `session_id`** и сохраняет его в **куку `PHPSESSID`**
3. данные хранятся **на сервере** в суперглобальном массиве **`$_SESSION`**
4. при завершении скрипта `$_SESSION` сериализуется в **save_handler** (файлы, Redis, БД)
5. на следующем запросе браузер пришлёт ту же куку — PHP найдёт данные по ID

**Хранилища (save_handler):**
- **`files`** (default) — файлы в `session.save_path` (обычно `/tmp` или `/var/lib/php/sessions`)
- **`redis`**, **`memcached`** — для масштабирования за балансировщиком
- **кастомный** — через `SessionHandlerInterface`

**Подводные камни:**
- стандартный `files` ставит **`flock`** при `session_start()` → параллельные AJAX **блокируют** друг друга
- ID-куку нужно защищать флагами **`Secure`**, **`HttpOnly`**, **`SameSite`**',
                'code_example' => '<?php
session_start();

$_SESSION["user_id"] = 42;
$_SESSION["cart"] = ["item1", "item2"];

// В другом запросе
session_start();
echo $_SESSION["user_id"]; // 42

unset($_SESSION["cart"]);
session_destroy();

// Регенерация ID при логине - защита от fixation
session_regenerate_id(true);',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как работают cookies в PHP?',
                'answer' => '**Cookie** — небольшие данные (до **~4 КБ**), хранимые в браузере и **посылаемые в каждом запросе** на сайт-источник.

**Базовые операции:**
- **установка:** **`setcookie()`** — делается **ДО** любого вывода, потому что это HTTP-заголовок
- **чтение:** **`$_COOKIE["name"]`** (доступно начиная **со следующего** запроса)
- **удаление:** `setcookie("name", "", time() - 3600)` — поставить в прошлое

**Параметры (с PHP 7.3 принимаются массивом):**
- **`expires`** — Unix-time; **`0`** = session-cookie (умирает при закрытии браузера)
- **`path`** — на какие пути отправлять
- **`domain`** — на какие хосты (для общего поддомена — `example.com`)
- **`secure`** — только по **HTTPS**
- **`httponly`** — **недоступна из JavaScript** (защита от XSS-кражи)
- **`samesite`** — **`Strict`** / **`Lax`** / **`None`** — защита от CSRF
  - `Strict` — кука не уходит даже по обычным переходам с других сайтов
  - `Lax` — компромисс (по умолчанию в современных браузерах)
  - `None` — нужен **только** вместе с `Secure`',
                'code_example' => '<?php
setcookie("user", "Иван", [
    "expires" => time() + 3600,
    "path" => "/",
    "domain" => "example.com",
    "secure" => true,
    "httponly" => true,
    "samesite" => "Strict",
]);

echo $_COOKIE["user"] ?? "guest";

// Удалить
setcookie("user", "", time() - 3600);',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Какова роль cookie PHPSESSID в работе сессий?',
                'answer' => '**`PHPSESSID`** — HTTP-кука, которая хранит на клиенте **только идентификатор сессии**, а не сами данные.

**Как работает:**
1. при первом **`session_start()`** PHP генерирует уникальный **ID** и шлёт его в заголовке **`Set-Cookie: PHPSESSID=...`**
2. на следующих запросах браузер автоматически возвращает эту куку
3. сервер по ID **находит данные сессии в хранилище** (файлы `/tmp`, Redis, БД)

**Аналогия:** кука — это **«ключ от шкафчика»**, сами вещи (данные `$_SESSION`) лежат на сервере.

**Имя куки** можно сменить через `session.name` в `php.ini` или `session_name()`.',
                'difficulty' => 2,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Из каких этапов состоит жизненный цикл сессии в PHP?',
                'answer' => '**Жизненный цикл — четыре этапа:**

1. **Идентификация** — на старте запроса PHP читает **`PHPSESSID`** из cookie. Если её нет — генерирует **новый ID** и шлёт в `Set-Cookie`.

2. **Открытие и чтение** — **`session_start()`**:
   - открывает **save_handler** (`files`, `redis`, кастомный)
   - берёт **эксклюзивную блокировку** на запись (для `files` это `flock`)
   - читает данные, десериализует через `session.serialize_handler` в **`$_SESSION`**

3. **Работа со скриптом** — приложение читает/пишет **`$_SESSION`** в памяти.

4. **Закрытие** — при завершении скрипта или явном **`session_write_close()`**:
   - данные сериализуются обратно
   - **записываются** в хранилище
   - **блокировка снимается**
   - **вероятностно** (`gc_probability`/`gc_divisor`) запускается **сборщик мусора**

**Важно:** длительные блокировки на этапе 2 — главная причина «зависания» параллельных AJAX-запросов одного пользователя. Лечится ранним `session_write_close()`.',
                'difficulty' => 3,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Почему параллельные запросы одного пользователя блокируют друг друга при работе с сессиями?',
                'answer' => '**Корневая причина — file lock в save_handler `files`.**

**Что происходит при `session_start()`:**
1. PHP читает `PHPSESSID` из куки
2. находит файл `<save_path>/sess_<id>` для этого ID
3. ставит **эксклюзивный `flock(LOCK_EX)`** — пока скрипт не закончится или не вызовет `session_write_close()`
4. читает данные, десериализует в `$_SESSION`

**Симптом:** второй и третий параллельный AJAX **того же пользователя** висят на `session_start()`, **сериализуясь** друг за другом.

**Где это болезненно:**
- **long-polling** — один открытый «висящий» запрос блокирует все остальные
- **параллельные AJAX** — 5 виджетов на странице загружаются последовательно
- **WebSocket fallback** через SSE — не работает совсем
- **REST API**, использующее cookie-сессию

**Решения (от мягкого к радикальному):**

| Подход | Когда применять |
| --- | --- |
| **`session_write_close()`** сразу после последней записи | если данные изменили только в начале |
| **`session_start(["read_and_close" => true])`** (PHP 7.0+) | только чтение в этом запросе |
| **Redis/Memcached** save_handler | хранилище **не блокирует** ID на чтение |
| Кастомный `SessionHandlerInterface` без `flock` | если нужны транзакции на запись |
| **JWT** вместо сессии | stateless API |

**Подводный камень:** даже **Redis-handler** может **блокировать** при `session.lock_expire` > 0 (`php-redis` поддерживает spinlock на ключе) — если хотим **полностью** неблокирующее поведение, выключаем lock явно (`redis.session.locking_enabled = 0`), но тогда последний writer wins при гонке.',
                'difficulty' => 4,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что делает session_write_close() и зачем вызывать его явно?',
                'answer' => '**`session_write_close()`** (alias **`session_commit()`**) выполняет три действия **не дожидаясь** конца скрипта:

1. **сериализует** `$_SESSION` через `session.serialize_handler`
2. **пишет** данные в save_handler (файл / Redis / БД)
3. **снимает блокировку** на ID сессии

**Зачем нужно явно:**

| Сценарий | Эффект |
| --- | --- |
| Долгий запрос (отчёт, экспорт) | освобождает блокировку → параллельные AJAX не висят |
| Long-polling/SSE | без вызова другой пользовательский запрос не пройдёт |
| После всей работы с сессией в начале запроса | вся остальная логика идёт без блокировки |
| Перед `header("Location: ...")` redirect | гарантия, что данные записаны до того, как клиент сделает следующий запрос |

**Подводные камни:**

- **запись в `$_SESSION` после `session_write_close()`** **не сохранится** (без `session_start()` снова)
- **по умолчанию `register_shutdown_function`** автоматически зовёт `session_write_close()` в конце скрипта — явный вызов делает это **раньше**
- **повторный `session_start()`** после `close()` приводит к **новой блокировке** и потенциальной гонке (если ID не сменили)
- **Laravel/Symfony** управляют сессией сами — там вызывать **не нужно**, фреймворк решает, когда коммитить (в Laravel это делает middleware `StartSession::handle()` после контроллера)

**Альтернатива в PHP 7+:** `session_start(["read_and_close" => true])` — открывает, читает, **сразу закрывает** блокировку. Идеально для **чисто-читающих** запросов.',
                'code_example' => '<?php
session_start();

// Прочитали и записали то, что нужно
$_SESSION["last_seen"] = time();

// Снимаем блокировку, чтобы параллельные AJAX/long-polling не висели
session_write_close();

// Долгая работа — пользователь параллельно может ходить по сайту
sleep(5);
generateExpensiveReport();

// Запись после write_close в $_SESSION НЕ попадёт в хранилище
$_SESSION["x"] = 1; // потеряется',
                'code_language' => 'php',
                'difficulty' => 4,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что такое session fixation и как от неё защититься в PHP?',
                'answer' => '**Session fixation** — атака, в которой злоумышленник **навязывает** жертве **известный ему** session ID до её аутентификации, и после успешного логина получает доступ к аккаунту по **тому же ID**.

**Канонический сценарий:**

1. Атакующий заходит на `https://app.com/` и получает `PHPSESSID=ABC123`
2. Шлёт жертве ссылку **`https://app.com/?PHPSESSID=ABC123`** или ставит куку через **XSS на поддомене**
3. Жертва переходит, её браузер **переиспользует** этот ID
4. Жертва логинится — теперь сессия `ABC123` **авторизована**
5. Атакующий, у которого та же кука, **уже залогинен** в аккаунт жертвы

**Защита — комбинация настроек и кода:**

| Мера | Зачем |
| --- | --- |
| **`session_regenerate_id(true)`** при логине | сменить ID сразу после смены уровня доверия; `true` — удалить старый файл |
| **`session.use_strict_mode = 1`** | PHP игнорирует «придуманный» клиентом ID, для которого нет записи в хранилище, и создаёт новый |
| **`session.use_only_cookies = 1`** | запрет передавать ID через URL (классический вектор фиксации) |
| **`session.use_trans_sid = 0`** | не дописывать ID к ссылкам в HTML |
| **`SameSite=Lax/Strict`** на куке | защита от того, что злоумышленник установит куку через cross-site |
| Регенерация ID при **смене привилегий** | повышение роли, привязка 2FA, смена пароля |

**Подводный камень с параллельными запросами:** регенерация ID в момент, когда у клиента «в полёте» несколько AJAX, может **гнать гонку**. PHP это понимает — после `regenerate_id` старый файл некоторое время помечается как «depricated» и параллельные запросы успевают получить новый ID через `Set-Cookie`. Управляется через `session.sid_length` и `session.sid_bits_per_character` (для энтропии ID).',
                'code_example' => '<?php
session_start();

if (authenticate($_POST["email"], $_POST["password"])) {
    // Сменить ID сразу после логина — закрыть fixation
    session_regenerate_id(true); // true = удалить старый файл сессии
    $_SESSION["user_id"] = $user->id;
}

// php.ini:
// session.use_strict_mode = 1
// session.cookie_secure = 1
// session.cookie_httponly = 1',
                'code_language' => 'php',
                'difficulty' => 4,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что такое session hijacking и какие настройки cookie его минимизируют?',
                'answer' => '**Session hijacking** — **кража валидного** ID сессии у уже залогиненного пользователя. В отличие от fixation, атакующий **не навязывает** ID, а **похищает** существующий.

**Векторы кражи:**

| Вектор | Защита |
| --- | --- |
| **XSS** — `document.cookie` из JS | **`HttpOnly`** делает куку невидимой для JS |
| **Перехват трафика** (HTTP, Wi-Fi MITM) | **`Secure`** запрещает отдавать куку по HTTP |
| **Утечка в логах** (`access.log` с query-string) | **`use_only_cookies = 1`** + не логировать `Cookie:` header |
| **CSRF на чужом сайте** (старая кука уйдёт в любой запрос) | **`SameSite=Lax`** (default в современных браузерах) или **`Strict`** |
| **Поддомен с XSS** | **`Domain`** ограничивает скоуп; не задавать `.example.com` если не нужно делиться |
| **Утечка через Referer** | сейчас браузеры не шлют чувствительные куки в Referer, но не передавать ID в URL остаётся правилом |

**Минимально необходимый набор настроек:**

```ini
session.cookie_secure   = 1     ; только HTTPS
session.cookie_httponly = 1     ; недоступна из JS
session.cookie_samesite = "Lax" ; "Strict" если приложение не принимает cross-site POST
session.use_only_cookies = 1    ; не принимать ID из URL
session.use_strict_mode = 1     ; отвергать unknown ID
session.sid_length      = 48    ; больше энтропии (default 32)
session.cookie_lifetime = 0     ; session-cookie; долгоживущие — отдельно через remember-me
```

**Дополнительные меры на уровне приложения:**

- **fingerprinting** — связать сессию с **`User-Agent` + IP-сетью** (не точным IP — mobile меняет IP); при расхождении — инвалидировать
- **idle timeout** — продлевать сессию только активным запросом
- **absolute lifetime** — даже активная сессия живёт максимум N часов
- **2FA** — крадённая кука бесполезна без второго фактора при чувствительных операциях
- мониторинг **одновременных сессий** одного пользователя из разных гео',
                'difficulty' => 4,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'За что отвечает session.use_strict_mode = 1?',
                'answer' => '**Strict mode** заставляет PHP **отвергать неинициализированные** ID сессий, присланные клиентом.

**Без strict mode (default):**
- клиент шлёт `Cookie: PHPSESSID=ARBITRARY123`
- PHP создаёт **файл** `sess_ARBITRARY123` если его нет
- использует **тот ID, который придумал клиент**
- результат: атакующий может **навязать** жертве заранее известный ему ID (session fixation)

**Со `session.use_strict_mode = 1`:**
- клиент шлёт `Cookie: PHPSESSID=ARBITRARY123`
- PHP смотрит save_handler: записи `sess_ARBITRARY123` нет
- **отвергает** этот ID, **генерирует новый**, шлёт `Set-Cookie: PHPSESSID=<новый>`
- атакующий **не может** заранее предсказать, какой ID получит жертва

**Что это закрывает:**

| Атака | До strict mode | После |
| --- | --- | --- |
| **Fixation через URL** (`?PHPSESSID=ATTACKER`) | работает | не работает |
| **Fixation через XSS на поддомене** | работает | не работает |
| **Fixation через подбор существующего ID** | работает | не работает |
| Hijacking (кража **валидного** ID) | работает | **работает** — strict mode не защищает |

**Зачем по умолчанию выключен:**
- исторически PHP позволял **«гостевую» сессию** до первого `session_start()` с любым клиентским ID — удобно для трекинга гостей в URL
- современный подход не использует ID-в-URL — фича превращается в дыру

**Best practice:** включать **всегда** в проде. Рекомендация присутствует и в **PHP manual**, и в **OWASP Session Management Cheat Sheet**.',
                'difficulty' => 4,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Когда вызывать session_regenerate_id(true)?',
                'answer' => '**Правило:** регенерировать ID при любой **смене уровня доверия** пользователя — **не на каждом запросе**.

**Сигнатура:** `session_regenerate_id(bool $delete_old_session = false): bool`

**Параметр `$delete_old_session`:**
- **`true`** — удалить **старый файл сессии** из хранилища
- **`false`** — оставить старый файл (он истечёт по `gc_maxlifetime`)

**Когда вызывать с `true`:**

| Событие | Почему |
| --- | --- |
| **Успешный логин** | старая «гостевая» сессия → новая «авторизованная», атакующий с ID до логина теряет доступ |
| **Логаут** | плюс `session_destroy()` — обнуление |
| **Повышение привилегий** | user → admin: старая роль → новая |
| **Привязка 2FA** | смена security context |
| **Смена пароля** | инвалидация старой сессии (старая кука перестаёт работать) |
| **Смена email** | то же |

**Почему НЕ регенерировать на каждом запросе:**

- **race condition с параллельными вкладками** — пока одна вкладка получила новую куку, у другой ещё старая, и её запросы летят с устаревшим ID
- лишние **сетевые round-trips**: каждый запрос порождает `Set-Cookie`
- износ **save_handler** — Redis-операции `DEL` + `SET` на каждый запрос
- **некоторые мобильные клиенты** некорректно обрабатывают смену кук в течение сессии

**Подводный камень с `false`:** старый файл остаётся **валидным** до GC. Если атакующий уже **украл** этот ID, регенерация **без `true`** его **не выкинет**.

**Подводный камень с `true`:** если у клиента **в полёте** параллельные запросы со старым ID, они получат `null`-сессию. PHP 7.1+ это смягчает через **временный alias** «старый ID → новый» на короткий период (`session.sid_length`/`session.lazy_write`).',
                'difficulty' => 4,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как реализовать собственное хранилище сессий в PHP?',
                'answer' => '**Кастомный save_handler** позволяет хранить сессии в SQL, Redis, DynamoDB, etcd — где угодно.

**Главный интерфейс — `SessionHandlerInterface`:**

| Метод | Что делает | Возврат |
| --- | --- | --- |
| `open($savePath, $name)` | начало сессии (открыть соединение) | `bool` |
| `close()` | конец сессии (закрыть соединение) | `bool` |
| `read($id)` | прочитать данные по ID | сериализованная строка или `""` |
| `write($id, $data)` | сохранить данные | `bool` |
| `destroy($id)` | удалить сессию (логаут) | `bool` |
| `gc($maxLifetime)` | удалить устаревшие | `int` (PHP 7+: число удалённых) |

**Дополнительные интерфейсы:**

| Интерфейс | Зачем |
| --- | --- |
| `SessionUpdateTimestampHandlerInterface` | методы `validateId()` (для `use_strict_mode`) и `updateTimestamp()` (продление без перезаписи) |
| `SessionIdInterface` | кастомная **генерация ID** (`create_sid()`) — крипто-сильный random |

**Регистрация:**

```php
session_set_save_handler($handler, true);
//                                   ^ true = автоматический register_shutdown_function
session_start();
```

**Базовый шаблон Redis-обработчика:**

```php
final class RedisSessionHandler
    implements SessionHandlerInterface, SessionUpdateTimestampHandlerInterface
{
    public function __construct(
        private Redis $redis,
        private int $ttl = 1440,
    ) {}

    public function open(string $path, string $name): bool { return true; }
    public function close(): bool { return true; }

    public function read(string $id): string {
        return (string) $this->redis->get("sess:$id");
    }

    public function write(string $id, string $data): bool {
        return (bool) $this->redis->setex("sess:$id", $this->ttl, $data);
    }

    public function destroy(string $id): bool {
        return $this->redis->del("sess:$id") >= 0;
    }

    public function gc(int $maxLifetime): int|false {
        return 0; // Redis сам через TTL
    }

    public function validateId(string $id): bool {
        return $this->redis->exists("sess:$id") > 0;
    }

    public function updateTimestamp(string $id, string $data): bool {
        return $this->redis->expire("sess:$id", $this->ttl);
    }
}
```

**Подводные камни:**

- `validateId()` критичен для **`use_strict_mode`** — без него strict-режим не работает
- **`updateTimestamp()`** даёт оптимизацию: продление сессии **без** перезаписи всего payload (важно для больших сессий)
- **`gc()`** в Redis/DynamoDB не нужен — у них **встроенный TTL**; в SQL нужен явный `DELETE WHERE last_seen < ?`
- Laravel и Symfony **уже имеют** готовые обработчики — кастомный пишут только для экзотических хранилищ',
                'difficulty' => 4,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как горизонтально масштабировать сессии PHP между несколькими веб-серверами?',
                'answer' => '**Корневая проблема:** дефолтный `save_handler = files` хранит сессии **локально** на каждом веб-сервере. За балансировщиком запросы одного пользователя попадают на **разные ноды** — нода не видит данных, которые записала другая.

**Три стратегии решения:**

| Подход | Плюсы | Минусы |
| --- | --- | --- |
| **Sticky sessions** | минимум кода: балансировщик закрепляет клиента по cookie/IP | при выпадении ноды все её сессии теряются; неравномерная нагрузка |
| **Централизованное хранилище** (Redis/Memcached/БД) | любой воркер читает одни данные; ноды stateless; легко скейлить | нужен высокодоступный кластер хранилища |
| **JWT** (stateless) | вообще не нужно хранилище сессий | сложно отзывать токен; payload не зашифрован |

**Канонический setup на Redis:**

```ini
; php.ini
session.save_handler = redis
session.save_path    = "tcp://redis-prod.internal:6379?auth=...&database=0&persistent=1"
session.cookie_secure   = 1
session.cookie_httponly = 1
session.cookie_samesite = "Lax"
session.use_strict_mode = 1
session.gc_maxlifetime  = 86400
```

**Для Memcached:**
```ini
session.save_handler = memcached
session.save_path    = "memcache1:11211,memcache2:11211"
```

**Что должно совпадать на всех нодах:**

| Параметр | Почему |
| --- | --- |
| `session.save_handler` + `save_path` | одно хранилище |
| `session.name` | имя куки |
| `session.cookie_domain` | чтобы кука приходила на все ноды |
| `session.serialize_handler` | иначе одна нода не прочитает данные другой |
| **версия PHP** для бинарного сериализатора | различия в формате между мажорами |

**Подводный камень с блокировками:** **`phpredis`** по умолчанию ставит **spinlock** на ключе при `session_start()`. Параллельные AJAX того же пользователя через **разные ноды** всё равно сериализуются (выигрыш только в межнодовом скейлинге, не в межзапросном). Управление: `redis.session.locking_enabled = 0`, либо `session_start(["read_and_close" => true])` для read-only.

**Альтернатива в Laravel/Symfony:** `SESSION_DRIVER=redis` — фреймворк сам инкапсулирует обработчик, конфиг не трогаем.',
                'difficulty' => 4,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как работает сборка мусора сессий в PHP и почему файлы могут «жить» дольше gc_maxlifetime?',
                'answer' => '**GC сессий — вероятностный, а не плановый.**

**Что происходит при `session_start()`:**

1. PHP бросает «монетку» — random `1..session.gc_divisor`
2. если число `<= session.gc_probability` — запускается **`gc()`** обработчика
3. `gc()` для `files` обходит `save_path` и **удаляет** файлы, у которых `mtime` старше `session.gc_maxlifetime`

**Дефолты:**

| Параметр | Default | Смысл |
| --- | --- | --- |
| `session.gc_probability` | `1` | числитель |
| `session.gc_divisor` | `1000` (Debian/Ubuntu: **`0`**) | знаменатель |
| `session.gc_maxlifetime` | `1440` (24 минуты) | возраст для удаления |

**Шанс GC** = `gc_probability / gc_divisor` = **0.1%** на запрос (по умолчанию). При малом трафике на одну ноду может пройти **час** между фактическими запусками — файлы залёживаются.

**Почему на Debian/Ubuntu `gc_probability = 0`:**

- эти дистрибутивы отключают встроенный GC и поставляют **`/etc/cron.d/php`** с системным скриптом `/usr/lib/php/sessionclean`
- скрипт раз в **30 минут** ищет файлы в `/var/lib/php/sessions` старше `gc_maxlifetime` и удаляет
- причина: GC через `session_start()` зависит от трафика и **может пропускать сессии месяцами** на тихих сайтах

**Подводные камни:**

| Проблема | Следствие |
| --- | --- |
| Несколько PHP-версий с разными `save_path` | один cron не покрывает все, сессии остаются |
| Cron отключён в Docker-контейнере | бесконечный рост `/var/lib/php/sessions` |
| Разные `gc_maxlifetime` для разных pool-ов | cron использует **самое большое** значение |
| Multi-server с локальными файлами | каждая нода чистит **свои** файлы; «сиротские» от выпавшей ноды никто не приберёт |

**Решения для прода:**

- **Redis/Memcached** — встроенный **TTL**, никакой `gc()` не нужен
- **БД-handler** — отдельная cron-задача `DELETE WHERE last_seen < ?`
- **`session.gc_probability = 1, gc_divisor = 100`** на серверах без cron — каждый сотый запрос чистит

**Проверка, что cron работает:** `find /var/lib/php/sessions -type f -mmin +60 | wc -l` — на здоровой системе ноль или единицы.',
                'difficulty' => 4,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Почему нельзя класть в $_SESSION ресурсы вроде PDO-соединения или дескриптора файла?',
                'answer' => '**Причина — тип `resource` не сериализуется.**

**Что такое `resource`:**
- указатель на **живой объект ядра PHP**: соединение **PDO**/`mysqli`, дескриптор файла **`fopen`**, картинка **GD**, поток **`curl`**, контекст шифрования
- существует **только в рамках текущего процесса**

**Что произойдёт:**
1. при завершении скрипта PHP сериализует **`$_SESSION`** в хранилище
2. `resource` **не имеет осмысленного сериализованного представления** — PHP пишет `i:0;` или генерирует warning
3. в следующем запросе на месте ресурса будет **`int(0)`** или `null` — соединение **«умерло»**

**Что можно класть:**
- **скаляры** — `int`, `string`, `float`, `bool`
- **массивы** из скаляров
- **объекты без живых ресурсов** (DTO, value objects) — реализуют `__serialize`/`__unserialize` при необходимости

**Что вместо ресурса хранить:**
- **ID** записи в БД, а соединение поднимать заново
- **имя файла**, а не дескриптор
- **параметры подключения**, а не сам объект клиента',
                'difficulty' => 3,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что произойдёт, если вызвать session_start() после того, как уже что-то выведено в ответ?',
                'answer' => '**Симптомы:** warning **`headers already sent by ...`**, сессия не запускается, кука **`PHPSESSID`** не отправляется.

**Почему так:**
- **`session_start()`** добавляет заголовок **`Set-Cookie: PHPSESSID=...`**
- HTTP-заголовки уходят клиенту **до** тела ответа
- после первого `echo`, `print`, BOM в файле, пробела перед `<?php`, **HTML вне `<?php ... ?>`** — заголовки **уже отправлены**

**Типичные причины вывода до `session_start()`:**
- **BOM** в начале PHP-файла (`<?php`)
- **пробел или перевод строки** перед `<?php`
- что-то вне тегов в подключаемом `include`
- ошибки/предупреждения PHP, выводимые на экран

**Лечение:**
- **`session_start()` в самом начале** скрипта — стандарт
- **`ob_start()`** в начале — включает буферизацию вывода, заголовки можно слать позже
- проверить файлы на **BOM** и пустые строки перед `<?php`',
                'difficulty' => 3,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как разделить сессию между поддоменами одного домена?',
                'answer' => '**Два условия одновременно:**

**1. Кука должна уходить на все поддомены:**
- **`session.cookie_domain = "example.com"`** (без ведущей точки — современный канонический вид по RFC 6265)
- так кука **`PHPSESSID`** уйдёт и на `api.example.com`, и на `admin.example.com`
- ведущая точка `.example.com` исторически означала «и все поддомены»; в RFC 6265 точка игнорируется, но **браузеры её принимают**

**2. Все хосты должны ходить в общее хранилище:**
- **`session.save_handler = redis`** + общий **`session.save_path`**
- или общая БД, кастомный `SessionHandlerInterface`
- **локальные файлы** каждого сервера **не подойдут** — разные ноды увидят разные данные

**Ещё важно совпадение настроек на всех хостах:**
- **`session.name`** (имя куки, по умолчанию `PHPSESSID`)
- **`session.cookie_path`** (обычно `/`)
- **`session.serialize_handler`** — иначе данные одной ноды не прочитаются другой',
                'difficulty' => 3,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Чем session.cookie_lifetime отличается от session.gc_maxlifetime?',
                'answer' => '**Два независимых параметра — клиент vs сервер.**

| | **`session.cookie_lifetime`** | **`session.gc_maxlifetime`** |
| --- | --- | --- |
| **Где действует** | в **браузере** | на **сервере** |
| **Что определяет** | срок жизни **самой куки** `PHPSESSID` | срок жизни **данных** в хранилище |
| **Значение `0`** | session-cookie — умирает с закрытием браузера | (не используется) |
| **Кто удаляет** | браузер | сборщик мусора PHP |

**Подводный камень:** значения **никак не связаны автоматически**.
- если **`cookie_lifetime = 86400`**, а **`gc_maxlifetime = 1440`** (24 минуты) → у клиента кука жива, но **данные на сервере** уже удалены → пользователь видит «непонятный логаут»
- обратная ситуация: данные живут, но кука исчезла → пользователь анонимен, хотя в Redis всё ещё есть его сессия

**Правило:** выставляйте оба параметра **согласованно** — например, оба по `86400` для 24-часовой сессии.',
                'difficulty' => 3,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Чем сессии в PHP отличаются от cookies по месту хранения, объёму и безопасности?',
                'answer' => '**Cookies:**
- лежат **на клиенте** в браузере
- лимит **~4 КБ на куку**
- **отправляются в каждом запросе** (увеличивают трафик)
- **видны пользователю** — можно открыть DevTools и прочитать

**Сессии:**
- данные хранятся **на сервере** (файлы, Redis, БД)
- на клиент уходит **только короткий ID** в куке
- объём данных **практически не ограничен**
- содержимое **пользователю не видно**

**Безопасность:** сессии надёжнее, **но ID-кука становится точкой атаки** — её нужно защищать флагами:
- **`Secure`** — только HTTPS
- **`HttpOnly`** — недоступна из JS (защита от XSS-кражи)
- **`SameSite=Lax/Strict`** — защита от CSRF',
                'difficulty' => 2,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что стоит и что не стоит хранить в $_SESSION?',
                'answer' => '**Уместно хранить** короткие данные, которые живут между запросами одного пользователя:
- **ID авторизованного пользователя** (но не весь объект `User`)
- **CSRF-токен**
- **flash-сообщения** («Профиль сохранён»)
- состояние **wizard-мастера** или промежуточного шага
- **корзина** для гостя (для авторизованных — лучше в БД)

**НЕ стоит хранить:**
- **пароли в открытом виде** — даже временно
- **access-токены сторонних API** без необходимости
- **тяжёлые ORM-модели** (`User` со всеми relations)
- **`resource`-типы** (PDO, дескрипторы файлов — не сериализуются)
- **большие наборы данных** (списки на тысячи строк)

**Правило:** для тяжёлого — **достать по ID из БД** в каждом запросе, а не возить в сессии.',
                'difficulty' => 2,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Почему в RoadRunner и Swoole не стоит использовать обычный $_SESSION и session_start()?',
                'answer' => '**Корневая причина — модель жизни процесса меняется.**

**Классический FPM:**
- каждый запрос — **новый процесс/поток**
- **`$_GET`, `$_POST`, `$_SESSION`, `$_SERVER`** — суперглобалы, инициализируются под запрос и **умирают** в конце
- `session_start()` поднимает state из save_handler в `$_SESSION`, в конце SAPI коммитит
- **никакой утечки** между запросами — память освобождается

**RoadRunner/Swoole/FrankenPHP:**
- воркер **живёт часами/днями**
- между запросами **состояние** PHP остаётся в памяти
- если использовать стандартный `session_start()` — `$_SESSION` **наследуется** от **предыдущего** запроса того же воркера
- результат: пользователь A видит данные пользователя B

**Дополнительные проблемы:**

| Проблема | Почему |
| --- | --- |
| `$_SESSION` не очищается между запросами | переменная остаётся в памяти воркера |
| `session_id()` глобален в процессе | следующий запрос «думает», что у него тот же ID |
| `flock` для `files` блокирует **весь воркер** | один зависший long-poll вешает всю ноду |
| `register_shutdown_function` сработает только при **остановке воркера** | сессия не закоммитится между запросами |
| Глобальные `$_COOKIE` / `$_SERVER` | потенциальная утечка чужих заголовков |

**Правильный путь — PSR-7/PSR-15 middleware:**

- PSR-7 **`ServerRequestInterface`** содержит атрибуты сессии **per-request**, не в глобале
- Middleware на старте достаёт сессию из Redis/БД по ID из cookie, кладёт в request-атрибут
- Контроллер работает с этим объектом сессии (Laravel `$request->session()`, Symfony `$request->getSession()`)
- Middleware на финише коммитит изменения и сбрасывает запрос-state

**Что предлагают фреймворки:**

| Фреймворк | Решение |
| --- | --- |
| **Laravel + Octane** | сессия идёт через `SessionManager`, request-scoped binding, `SESSION_DRIVER=redis` |
| **Symfony + RoadRunner runtime** | сессия через `RequestStack`, `Symfony\\Component\\Runtime\\RoadRunnerRuntime` сбрасывает state |
| **Spiral/RoadRunner** | свой пакет `spiral/session` поверх PSR-7 |
| **Чистый Swoole** | руками: middleware + Redis-handler без `session_start()` |

**Что ещё нужно «обнулять»** в long-running воркерах: статика классов, ServiceContainer scoped-bindings, Eloquent global scopes, registered listeners.',
                'difficulty' => 4,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Какой session.serialize_handler выбрать и почему?',
                'answer' => '**`session.serialize_handler`** определяет, как PHP **сериализует `$_SESSION`** перед записью в save_handler. Три значения:

| Handler | Формат | Поддержка ключей | Совместим с `serialize()` |
| --- | --- | --- | --- |
| **`php`** (default) | `key\|value` | **ломается на `\|` в ключе**, числовые ключи теряются | нет |
| **`php_binary`** | `<len-byte><key><value>` | `\|` допустим, **имя ключа ≤ 127 символов** (один байт длины) | нет |
| **`php_serialize`** | стандартный `serialize($_SESSION)` | **любые** ключи, вложенные структуры | **да** |

**Конкретные проблемы handler-а `php`:**

```php
$_SESSION["a|b"] = 1;  // ❌ парсер уйдёт в нечитаемое состояние
$_SESSION[42] = 1;     // числовой ключ → приведётся в строку, может потеряться
```

**Что даёт `php_serialize`:**

- работает с **любыми ключами** (включая `\|`, `!`, числовые)
- корректно сериализует **объекты с `__serialize`/`__unserialize`** (PHP 7.4+)
- симметричен **`serialize()` / `unserialize()`** — можно отлаживать данные через `unserialize($redis->get(...))`
- стандартизированный формат, понятен всем PHP-инструментам

**Когда `php_serialize` особенно нужен:**
- сессия содержит **массив с ассоциативными ключами**, которые могут включать спецсимволы
- кладёте **объекты** value-object
- интеграция с **другим языком**, читающим сессии PHP (Python `phpserialize`, Go `php_serialize`)

**Рекомендация:** **`php_serialize`** — для современных проектов **default-выбор**. `php` оставлен для legacy-совместимости, `php_binary` — нишевый случай для бинарных данных.

**Безопасность:** при использовании `php_serialize` обязательно учитывайте **`unserialize` POP-chains**. Если злоумышленник может **записать** в сессию (через XSS + регистрацию ID), `unserialize` на чтении может **инстанциировать произвольный класс**. Защита: `unserialize($data, ["allowed_classes" => [...]])` в кастомном обработчике + `session.use_strict_mode = 1`.',
                'difficulty' => 4,
                'topic' => 'php.sessions',
            ],
            [
                'category' => 'PHP',
                'question' => 'Зачем в современных API сессии заменяют на JWT?',
                'answer' => '**Проблема сессий для API:** они **stateful** — требуют серверного состояния.

**Чем это плохо за балансировщиком:**
- нужны **sticky sessions** (по IP/cookie) — запрос привязан к конкретной ноде
- **или** общее хранилище (Redis/БД) + **блокировки** в `files`-handler
- горизонтальное масштабирование усложняется

**Что даёт JWT (stateless):**
- идентичность и **claims** (`sub`, `exp`, `roles`) лежат **в самом токене**
- токен **подписан** (HMAC или асимметрично) — любой узел проверит **без обращения к хранилищу**
- балансировщик может слать запросы куда угодно
- легко интегрируется с **мобильными клиентами**, **SPA**, **сторонними сервисами**

**Платой становятся:**
- **сложность отзыва** до истечения `exp` — нужен blacklist или короткий `exp` + refresh-токен
- **payload не зашифрован** (только base64) — нельзя класть секреты
- **больший размер** — токен уходит в каждом запросе целиком

**Когда сессии всё ещё уместны:**
- классическое **веб-приложение** с логин-формой и серверным рендером
- нужна **мгновенная** инвалидация сессии (выход на всех устройствах)
- нет SPA/мобильного клиента',
                'difficulty' => 3,
                'topic' => 'php.sessions',
            ],
        ];
    }
}
