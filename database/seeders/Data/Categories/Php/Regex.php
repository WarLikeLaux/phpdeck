<?php

namespace Database\Seeders\Data\Categories\Php;

class Regex
{
    public static function all(): array
    {
        return [
            [
                'category' => 'PHP',
                'question' => 'Как использовать регулярные выражения в PHP?',
                'answer' => 'В PHP regex реализован через **PCRE** (Perl Compatible Regular Expressions) — отсюда префикс **`preg_`** у всех функций.

**Структура шаблона:** **`/pattern/flags`** — оборачивается в разделитель (обычно `/`, можно `#`, `~`).

**Главные флаги после закрывающего разделителя:**
- **`i`** — без учёта регистра
- **`m`** — многострочный (`^` и `$` матчат каждую строку)
- **`s`** — точка `.` матчит `\n`
- **`u`** — UTF-8 (обязательно для кириллицы)
- **`x`** — extended (пробелы и комментарии игнорируются)

**Основные функции:**
- **`preg_match`** — найти первое совпадение
- **`preg_match_all`** — все совпадения
- **`preg_replace`** — заменить
- **`preg_replace_callback`** — замена через callback
- **`preg_split`** — разбить строку по шаблону',
                'code_example' => '<?php
// preg_match - первое совпадение
if (preg_match("/(\\d+)-(\\d+)/", "abc 123-456 xyz", $m)) {
    print_r($m);
    // [0 => "123-456", 1 => "123", 2 => "456"]
}

// preg_match_all - все
preg_match_all("/\\w+/", "one two three", $m);
print_r($m[0]); // ["one", "two", "three"]

// Замена с callback
$result = preg_replace_callback(
    "/\\d+/",
    fn($m) => $m[0] * 2,
    "a1 b2 c3"
);
// "a2 b4 c6"

// Именованные группы
preg_match("/(?<year>\\d{4})-(?<month>\\d{2})/", "2026-05", $m);
echo $m["year"]; // 2026',
                'code_language' => 'php',
                'difficulty' => 2,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что такое регулярное выражение и через что оно реализовано в PHP?',
                'answer' => '**Регулярное выражение** — шаблон для **поиска и проверки** строк по правилам.

**Из чего состоит шаблон:**
- **литералы** — обычные символы (`abc`)
- **метасимволы** — `.` (любой символ), **`\d`** (цифра), **`\w`** (слово), **`\s`** (пробел)
- **классы** — `[a-z]`, `[^0-9]`
- **квантификаторы** — `*`, `+`, `?`, `{n,m}`
- **якоря** — `^` (начало), `$` (конец), `\b` (граница слова)
- **группы** — `(...)` для захвата, `(?:...)` без захвата

**В PHP regex реализован через расширение PCRE** (Perl Compatible Regular Expressions), отсюда префикс **`preg_`** у функций. Шаблон оборачивают в **разделители** (обычно `/`, можно `#`, `~`), после закрывающего — модификаторы: **`i`** (без регистра), **`u`** (UTF-8), **`m`** (multiline), **`s`** (dotall).',
                'code_example' => '<?php
// Проверка
if (preg_match("/^\\d{4}-\\d{2}-\\d{2}$/", "2026-05-20")) {
    echo "дата ОК";
}

// Поиск с группами
preg_match("/(\\d+)-(\\d+)/", "id 123-456", $m);
print_r($m); // [0=>"123-456", 1=>"123", 2=>"456"]

// Все вхождения
preg_match_all("/\\w+/u", "one two три", $m);
print_r($m[0]); // ["one","two","три"]',
                'code_language' => 'php',
                'difficulty' => 2,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Зачем шаблону PCRE нужны разделители и какие символы можно использовать?',
                'answer' => '**PCRE-функции PHP требуют разделители вокруг шаблона**: `"/pattern/flags"`. Всё после закрывающего разделителя — **модификаторы** (`i`, `m`, `s`, `u` и др.).

**Какие символы подходят:**
- **любой не-алфавитно-цифровой** символ и не **`\`**
- чаще берут **`/`** — привычно
- для URL и путей удобнее **`#`** или **`~`** — не нужно экранировать слэши внутри
- **парные** скобки **`{}`**, **`[]`**, **`()`**, **`<>`** — открывающая в начале, закрывающая в конце
- **одиночный** `[` или `]` без пары — **нельзя**

**Зачем это нужно:**
- PHP не отличает «начало шаблона» от «начала строки» — разделители помечают границы
- модификаторы должны быть **после** шаблона, а не внутри

**Подводный камень:** если разделитель встречается внутри шаблона, его надо **экранировать** (`\/`). Это и есть главный мотив выбрать `#` для URL-шаблонов.',
                'code_example' => '<?php
// Слэш как разделитель — экранирование URL-слэшей
preg_match("/https?:\\/\\/example\\.com/", $url);

// # — слэши внутри не экранируем
preg_match("#https?://example\\.com#", $url);

// Парные скобки
preg_match("{<\\w+>}", "<div>");
preg_match("(\\d+)", "abc 42");

// Модификаторы после закрывающего разделителя
preg_match("/HELLO/i", "hello"); // 1 — case-insensitive',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'В чём разница между preg_match и preg_match_all?',
                'answer' => '**Сравнение:**

| | **`preg_match`** | **`preg_match_all`** |
| --- | --- | --- |
| Сколько ищет | **первое** совпадение и стоп | **все** непересекающиеся |
| Возвращает | `1` — найдено, `0` — нет, `false` — ошибка | количество совпадений (`int`) или `false` |
| Структура `$matches` | `[0 => полное, 1 => group1, ...]` | по умолчанию **PREG_PATTERN_ORDER** |

**Режимы раскладки у `preg_match_all`:**
- **`PREG_PATTERN_ORDER`** (default): `$m[0]` — все полные совпадения, `$m[1]` — все значения первой группы, и так далее
- **`PREG_SET_ORDER`**: `$m[0]` — первое совпадение целиком (со всеми группами), `$m[1]` — второе, и так далее — удобно для `foreach`

**Подводный камень:** проверка результата **`if (preg_match(...))`** скрывает ошибку (`false`) от «не найдено» (`0`). Правильно — **`=== false`** для ошибок, **`=== 1`** или просто истинность для совпадения.',
                'code_example' => '<?php
$text = "id 100, id 200, id 300";

preg_match("/id (\\d+)/", $text, $m);
// $m = ["id 100", "100"] — только первое

preg_match_all("/id (\\d+)/", $text, $m);
// $m[0] = ["id 100", "id 200", "id 300"]   все совпадения
// $m[1] = ["100", "200", "300"]             группа 1',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Чем preg_replace отличается от preg_replace_callback?',
                'answer' => '**Параллельное сравнение:**

| | **`preg_replace`** | **`preg_replace_callback`** |
| --- | --- | --- |
| Что подставляет | **строку** с обратными ссылками `$1`, `\\1` | то, что **вернёт колбэк** |
| Аргумент колбэка | — | массив `$matches` (как у `preg_match`) |
| Когда выбрать | **статическая** перестройка строки | замена **зависит от содержимого** совпадения |

**Когда нужен callback:**
- произвести **вычисление** (умножить число, перевести единицы)
- **обратиться к БД** или внешнему сервису по содержимому совпадения
- **привести регистр** буквы по условию
- **отрендерить** Markdown-вставку в HTML

**Связанный:** **`preg_replace_callback_array`** — несколько шаблонов и колбэков одним вызовом (PHP 7+).

**Подводный камень `preg_replace`:** в строке замены **`$0`** — полное совпадение, **`$1`**, **`$2`** — группы. Если в данных есть символ **`$`**, его нужно экранировать как **`\\$`**.',
                'code_example' => '<?php
// preg_replace — статичная подстановка через $1
echo preg_replace("/(\\d+)/", "[$1]", "a 5 b 42");
// "a [5] b [42]"

// preg_replace_callback — логика в коде
echo preg_replace_callback(
    "/\\d+/",
    fn(array $m) => $m[0] * 2,
    "a 5 b 42",
);
// "a 10 b 84"',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что делает preg_filter и чем он отличается от preg_replace?',
                'answer' => '**`preg_filter`** работает так же, как **`preg_replace`** — заменяет совпадения, но **на массиве оставляет только те элементы, где реально произошла замена**. Несовпавшие — **отбрасываются**.

**Параллельное сравнение на массиве:**

| Вход | `preg_replace("/^\\d+$/", "[$0]", ...)` | `preg_filter("/^\\d+$/", "[$0]", ...)` |
| --- | --- | --- |
| `"42"` | `"[42]"` | `"[42]"` |
| `"abc"` | `"abc"` (без изменений) | **отброшено** |

**Когда удобен `preg_filter`:**
- одновременно **отфильтровать и преобразовать** массив за один вызов
- собрать **только** валидные элементы по шаблону

**Подводный камень:** ключи **сохраняются**, поэтому нумерация массива на выходе может быть «дырявой» — при необходимости пропускайте через `array_values()`.',
                'code_example' => '<?php
$mixed = ["42", "abc", "100", "xx", "7"];

// preg_replace — оставляет всё
$a = preg_replace("/^\\d+$/", "[$0]", $mixed);
// ["[42]", "abc", "[100]", "xx", "[7]"]

// preg_filter — только совпавшие, преобразованные
$b = preg_filter("/^\\d+$/", "[$0]", $mixed);
// [0 => "[42]", 2 => "[100]", 4 => "[7]"]

// Дырявые ключи — нормализовать
$clean = array_values($b);',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Зачем нужна preg_split и чем она отличается от explode?',
                'answer' => '**`preg_split`** разбивает строку **по регулярному выражению**, а не по фиксированной подстроке.

**Сравнение:**

| | **`explode`** | **`preg_split`** |
| --- | --- | --- |
| Разделитель | **только литеральная** строка | **regex-шаблон** |
| Скорость | быстрее | чуть медленнее (PCRE-парсинг) |
| Гибкость | минимальная | резать по любому пробельному, по нескольким символам, по границам слов |

**Полезные флаги:**
- **`PREG_SPLIT_NO_EMPTY`** — отбросить пустые куски
- **`PREG_SPLIT_DELIM_CAPTURE`** — включить **захваченные группы** разделителя в результат
- **`PREG_SPLIT_OFFSET_CAPTURE`** — вернуть смещения каждого фрагмента

**Когда брать что:**
- разделитель **постоянный** (запятая, табуляция) → **`explode`**
- разделитель **переменный** (любые пробелы, несколько вариантов) → **`preg_split`**',
                'code_example' => '<?php
// explode — только литеральный разделитель
explode(",", "a,b,c"); // ["a", "b", "c"]

// preg_split — любая последовательность пробелов
preg_split("/\\s+/", "one  two\\tthree\\nfour");
// ["one", "two", "three", "four"]

// Несколько вариантов разделителя
preg_split("/[,;]\\s*/", "a, b; c,  d");
// ["a", "b", "c", "d"]

// С флагом NO_EMPTY
preg_split("/-/", "--a--b--", -1, PREG_SPLIT_NO_EMPTY);
// ["a", "b"] — без пустых строк',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что значат метасимволы \\d, \\w, \\s и их инвертированные версии?',
                'answer' => '**Сокращённые классы символов:**
- **`\d`** — цифра, по умолчанию `[0-9]` (для юникодных цифр нужен `(*UCP)` или `\p{Nd}`, одного флага `u` недостаточно)
- **`\w`** — «словный» символ: латинские буквы, цифры и `_`, т. е. `[A-Za-z0-9_]`
- **`\s`** — пробельный: пробел, табы, `\n`, `\r`, вертикальная табуляция, форм-фид

**Заглавные версии — инвертированные:**
- **`\D`** — всё, что НЕ цифра
- **`\W`** — всё, что НЕ слово
- **`\S`** — всё, что НЕ пробел

**Часто комбинируют в классах:**
- `[^\s]+` — последовательность непробелов
- `[\d\s,]` — цифры, пробелы и запятые
- `[\w-]+` — slug-подобная строка',
                'code_example' => '<?php
preg_match("/\\d+/", "цена 350 руб", $m);
echo $m[0]; // "350"

preg_match("/\\w+/", "user_42 hi", $m);
echo $m[0]; // "user_42"

preg_match("/\\S+/", "  hello world", $m);
echo $m[0]; // "hello"

// \\D — НЕ цифра
preg_match("/\\D+/", "12abc34", $m);
echo $m[0]; // "abc"',
                'code_language' => 'php',
                'difficulty' => 2,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что такое квантификаторы и какие они бывают?',
                'answer' => '**Квантификатор** задаёт, **сколько раз** предыдущий элемент должен повториться.

**Основные:**
- **`?`** — 0 или 1 (опционально)
- **`*`** — 0 или больше
- **`+`** — 1 или больше
- **`{n}`** — ровно `n`
- **`{n,}`** — `n` и больше
- **`{n,m}`** — от `n` до `m`

**Режимы работы:**
- **жадный** (по умолчанию) — захватывает **максимум**, потом отдаёт назад (`\.*`)
- **ленивый** — `?` после квантификатора (`*?`, `+?`, `??`): захватывает **минимум**, потом добирает
- **притяжательный** — `+` после (`*+`, `++`, `?+`): захватывает максимум и **не отдаёт** — защита от **катастрофического бэктрекинга**',
                'code_example' => '<?php
// Жадный — захватывает максимум
preg_match("/<.+>/", "<a><b>", $m);
echo $m[0]; // "<a><b>" — съел всё

// Ленивый — минимум
preg_match("/<.+?>/", "<a><b>", $m);
echo $m[0]; // "<a>"

// Точное число
preg_match("/^\\d{4}-\\d{2}-\\d{2}$/", "2026-05-20"); // 1

// Диапазон
preg_match("/^[a-z]{3,10}$/", "hello"); // 1',
                'code_language' => 'php',
                'difficulty' => 2,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'В чём разница между жадными и ленивыми квантификаторами?',
                'answer' => '**Три режима** одного и того же квантификатора `*`, `+`, `?`, `{n,m}`:

| Режим | Синтаксис | Стратегия | Что отдаёт назад |
| --- | --- | --- | --- |
| **Жадный** (default) | `.*`, `.+` | захватывает **максимум**, потом отдаёт назад **по одному символу** | да, при бэктрекинге |
| **Ленивый** | `.*?`, `.+?` | захватывает **минимум**, потом **дозахватывает** | да, расширяется |
| **Притяжательный** | `.*+`, `.++` (PCRE) | захватывает максимум и **НЕ отдаёт** | **нет** — фиксирует |

**Как это видно на одной строке `"<a><b>"`:**

- **`/<.+>/`** (жадный) → `"<a><b>"` — съел до последнего `>`
- **`/<.+?>/`** (ленивый) → `"<a>"` — остановился на первом `>`
- **`/<.++>/`** (притяжательный) → совпадения **нет** — `.++` сожрал всё до конца строки и отказался отдавать символ для финального `>`

**Перформанс-следствие:** жадный `.*` на длинном тексте порождает **много бэктрекинга** — для каждого «не получилось» движок отдаёт символ назад и пробует заново. Если ленивый или притяжательный даёт тот же результат — берите их, особенно когда шаблон в горячем пути.

**Антипаттерн:** `<.+>` для HTML — в реальности нужен `<[^>]+>` (узкий класс **избавляет от бэктрекинга вообще**, и работает быстрее, чем `<.+?>`).',
                'difficulty' => 4,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что такое атомарные группы и зачем они нужны?',
                'answer' => '**Атомарная группа** `(?>...)` — группа, чей результат **«замораживается»** после успешного матчинга. Движок **не имеет права** возвращаться внутрь и пробовать другие варианты совпадения этой группы.

**Зачем нужно:**
- защита от **катастрофического бэктрекинга** — обычная группа `(a+)+` на «плохом» входе порождает экспоненциальный перебор; атомарная **обрубает** перебор внутри себя
- **производительность** — там, где альтернативы заведомо не пересекаются, бэктрекинг внутри группы бесполезен

**Эквивалентность с притяжательными квантификаторами:**

| Запись | Эквивалент |
| --- | --- |
| `X++` | `(?>X+)` |
| `X*+` | `(?>X*)` |
| `X?+` | `(?>X?)` |

**Канонический пример лечения ReDoS:**

```
"(a+)+$"   ← патологический бэктрекинг на "aaaaaaaaab"
"(?>a+)+$" ← фикс: внутреннее a+ не сдаёт назад
"a++$"     ← то же самое короче
```

**Подводные камни:**
- атомарная группа **не захватывает** (как `(?:...)`), хотя визуально похожа на захватывающую
- избыточная атомарность может **скрыть** реально нужное совпадение — если правильное разбиение требует отдать символ назад',
                'difficulty' => 4,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что такое катастрофический бэктрекинг и как его предотвращать?',
                'answer' => '**Catastrophic backtracking (ReDoS)** — патологическое поведение движка regex, когда **число попыток разбиения растёт экспоненциально** относительно длины несовпадающего входа. На патологическом шаблоне `(a+)+$` строка из 30 `"a"` + одна `"b"` даёт ~**2^30 ≈ миллиард** попыток матчинга — процесс висит секундами/минутами или валится по `pcre.backtrack_limit`.

**Типичные виновники:**
- **вложенные** квантификаторы: `(a+)+`, `(\\w+)*`
- **пересекающиеся альтернативы**: `(a|aa)+`, `(\\d|\\d+)+`
- **жадные `.*`** + «выглядящий не-завершающий» якорь
- разные части шаблона **могут одинаково «захватить» один и тот же символ** — для каждого `aa` движок пробует и `a+a+`, и `a+ a+`, и так далее

**Лечение — четыре стратегии:**

| Приём | Пример |
| --- | --- |
| Узкий класс вместо `.` | `<[^>]+>` вместо `<.+>` |
| Притяжательный квантификатор | `\\d++` вместо `\\d+` |
| Атомарная группа | `(?>\\w+)` вместо `(\\w+)` |
| Якоря, ограничивающие поиск | `^pattern$` вместо `pattern` |

**Защита в проде:**
- никогда не **строить regex из недоверенного ввода** без `preg_quote`
- ставить **разумный `pcre.backtrack_limit`** (по умолчанию 1 000 000)
- проверять результат через **`=== false`** и `preg_last_error() === PREG_BACKTRACK_LIMIT_ERROR`
- проверять regex статикой (ReDoS-чекеры в SAST, например `safe-regex`)',
                'difficulty' => 4,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Чем захватывающая группа отличается от незахватывающей?',
                'answer' => '**Захватывающая `(...)`** — делает **две вещи** одновременно:
- **группирует** под общий квантификатор
- **сохраняет** совпавший фрагмент в нумерованном слоте — доступен через `$matches[1]`, обратную ссылку `\\1` в шаблоне, подстановку `$1` в `preg_replace`

**Незахватывающая `(?:...)`** — только **группировка**, без сохранения:
- работает **чуть быстрее**
- **не сбивает нумерацию** остальных групп
- упрощает чтение шаблона — видно «эта группа только для квантификатора, она не понадобится»

**Правило хорошего стиля:** использовать **`(?:...)`** **всегда**, когда содержимое не понадобится — это и быстрее, и устойчивее к правкам шаблона.

**Связанные виды групп:**
- **`(?P<name>...)`** или **`(?<name>...)`** — именованная захватывающая
- **`(?>...)`** — атомарная (не отдаёт назад при бэктрекинге)
- **`(?=...)`**, **`(?!...)`** — lookahead-утверждения нулевой ширины',
                'code_example' => '<?php
// Захватывающая — фрагмент попадает в $m
preg_match("/(\\d+)-(\\d+)/", "id 100-200", $m);
// $m = ["100-200", "100", "200"]

// Незахватывающая (?:...) — только группировка
preg_match("/(?:https?):\\/\\/(\\w+)/", "https://example.com", $m);
// $m = ["https://example", "example"] — http/https не захвачены',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что такое именованные группы захвата и как к ним обращаться?',
                'answer' => '**Синтаксис:** **`(?<name>...)`** или **`(?P<name>...)`** — оба варианта рабочие.

**Где появляется имя:**
- **`$matches["name"]`** **И** **`$matches[N]`** — доступно и по имени, и по номеру
- внутри **шаблона** обратная ссылка: **`\\k<name>`** или **`(?P=name)`**
- в **`preg_replace`** замена: **`${name}`** или **`$<name>`**

**Что это даёт:**
- шаблон становится **самодокументируемым** — видно, что группа значит
- **устойчиво к правкам** — добавили скобки в начале, нумерация сбилась, а имена остались
- упрощает работу с большими шаблонами (URL-парсинг, лог-строки)

**Подводный камень:** имена групп должны быть **уникальны в пределах шаблона** (если только не используется `J`-флаг `(?J)` — но это редкий случай и часто отключён).',
                'code_example' => '<?php
preg_match(
    "/(?<year>\\d{4})-(?<month>\\d{2})-(?<day>\\d{2})/",
    "2026-05-20",
    $m,
);
echo $m["year"];  // "2026"
echo $m["month"]; // "05"

// Подстановка по имени в preg_replace
echo preg_replace(
    "/(?<y>\\d{4})-(?<m>\\d{2})-(?<d>\\d{2})/",
    "$<d>.$<m>.$<y>",
    "2026-05-20",
); // "20.05.2026"',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что такое обратная ссылка в регулярном выражении?',
                'answer' => '**Backreference** — требование, чтобы фрагмент строки **совпал с тем, что уже захватила** более ранняя группа в **этом же** матчинге.

**Синтаксис:**

| Где | Запись | Что значит |
| --- | --- | --- |
| **Внутри шаблона** | `\\1`, `\\2`, …, `\\99` | по номеру группы |
| **Внутри шаблона** | `\\k<name>` или `(?P=name)` | по имени |
| **В строке замены `preg_replace`** | `$1`, `${1}`, `${name}` | **подстановка**, НЕ обратная ссылка |

**Важное различие:**
- **`\\1` внутри шаблона** — требование «здесь должно быть **то же самое**, что в группе 1»
- **`$1` или `\\1` в строке замены** — «вставь сюда **содержимое** группы 1» (две разные семантики, не путать)

**Канонические применения:**
- **дубликаты слов**: `/\\b(\\w+)\\s+\\1\\b/i` → `"hello hello world"`
- **парные кавычки одного типа**: `/(["\']).*?\\1/` → `"text"` или `\'text\'`
- **парные теги**: `/<(\\w+)>.*?<\\/\\1>/`

**Перформанс:**
- обратные ссылки **выводят regex из класса «регулярных»** в смысле теории — нужен **non-deterministic automaton** с памятью, что иногда мешает оптимизациям и **отключает JIT** в PCRE2
- избегайте в горячих местах, если можно решить иначе',
                'difficulty' => 4,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что такое lookahead и lookbehind в регулярных выражениях?',
                'answer' => '**Утверждения нулевой ширины (zero-width assertions)** — проверяют **контекст** вокруг позиции, но **не «съедают»** символы.

**Четыре формы:**

| Конструкция | Смысл |
| --- | --- |
| **`(?=X)`** | положительный **lookahead** — дальше должно идти `X` |
| **`(?!X)`** | отрицательный lookahead — дальше **НЕ** должно идти `X` |
| **`(?<=X)`** | положительный **lookbehind** — позади должно быть `X` |
| **`(?<!X)`** | отрицательный lookbehind — позади **НЕ** должно быть `X` |

**Чем удобно:**
- найти **по контексту**, не включая контекст в совпадение
- проверить условие, не двигая курсор: можно собрать несколько lookahead подряд
- сократить шаблоны валидации (пароль: «есть цифра», «есть буква»)

**Подводный камень для lookbehind:** в классическом PCRE длина должна быть **фиксированной**. PCRE2 (PHP 7.3+) поддерживает альтернативы разной длины, но каждая ветка по-прежнему должна быть **конечной** — `.*` внутри запрещено.',
                'code_example' => '<?php
// Lookahead — число перед "USD", сам "USD" не захватываем
preg_match("/\\d+(?= USD)/", "100 USD, 200 EUR", $m);
echo $m[0]; // "100"

// Negative lookahead — слово, за которым НЕ идёт двоеточие
preg_match_all("/\\w+(?!:)/", "name: ivan age: 30", $m);

// Lookbehind — цена после "$"
preg_match("/(?<=\\$)\\d+/", "цена $42", $m);
echo $m[0]; // "42"',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Какие ограничения у lookbehind в PCRE?',
                'answer' => '**Корневая причина:** lookbehind заставляет движок **«отступить назад»** на известное число символов и проверить шаблон оттуда. Без фиксированной длины он не знает, **на сколько** отступать.

**Эволюция:**

| Версия | Ограничение |
| --- | --- |
| Старый PCRE (PHP < 7.3) | **строго фиксированная длина** — `(?<=abc)`, но не `(?<=ab|abc)` |
| PCRE2 (PHP **7.3+**) | альтернативы **разной длины** разрешены, но **каждая ветка — конечная** |
| Все версии | **`*`, `+`, `{n,}` внутри lookbehind — запрещены** |

**Что разрешено в PCRE2:**

```
(?<=abc)        OK — фикс. длина 3
(?<=ab|abcd)    OK — разные длины, но каждая конечна
(?<=\\w{3,5})    OK — диапазон ограничен сверху
(?<=\\w*)        ERROR — неограниченный квантификатор
(?<=.*foo)      ERROR — .* без верхней границы
```

**Обходные пути, если упёрлись в ограничение:**

| Приём | Когда применять |
| --- | --- |
| Захватить контекст в группу и работать с ней | если контекст нужен в результате |
| **`\\K`** (PCRE) — «забыть» всё захваченное до этой точки | альтернатива lookbehind без длины |
| Перевернуть строку через `strrev` + lookahead | для UTF-8 несовместимо |
| Парсить вручную (мини-парсер) | если шаблон слишком сложный для regex |

**`\\K` — удобный трюк:** `/foo\\K\\d+/` совпадёт с `\\d+`, но захваченным считается **только** часть после `\\K` — фактически unbounded lookbehind.',
                'difficulty' => 4,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что делают флаги i, m, s в PCRE?',
                'answer' => '**Три самых ходовых модификатора шаблона:**

- **`i`** (**case-insensitive**) — регистронезависимый поиск
  - по умолчанию работает для **ASCII**
  - для Unicode case-folding нужны **и** `u`, **и** UCP-режим (`(*UCP)` или явные `\\p{...}`)
  - `/iu` без UCP умеет только ASCII case-folding (`A`==`a`, но `А`!=`а` для кириллицы)

- **`m`** (**multiline**) — меняет смысл якорей:
  - **`^`** и **`$`** совпадают **не только с краями всей строки**, но и с началом/концом **каждой строки** между `\\n`
  - сами `^`/`$` остаются **нулевой ширины** — `\\n` не «съедается»

- **`s`** (**dotall / single line**) — **`.`** начинает совпадать **с любым** символом, включая **`\\n`**
  - без флага точка не пересекает перевод строки

**Часто используют в комбинации:** **`/.../imsu`** — нечувствительно к регистру, многострочно, точка ест `\\n`, UTF-8.

**Связанные:** **`x`** (extended — пробелы и комментарии в шаблоне игнорируются), **`U`** (инвертирует жадность по умолчанию).',
                'difficulty' => 3,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Зачем нужен модификатор u в регулярных выражениях PHP?',
                'answer' => '**Без флага `u`** PCRE работает **побайтово**:
- `.` совпадает с **одним байтом**, а не кодпоинтом → может **разрезать** кириллический символ пополам (2 байта) или эмодзи (4 байта)
- `\\w`, `\\d`, `\\s` — **только ASCII** (`[A-Za-z0-9_]`)
- `[а-я]` работает **случайно**, потому что байты кириллицы и не-кириллицы перекрываются

**С флагом `u`:**
- шаблон и субъект интерпретируются как **UTF-8**
- `.` совпадает с **одним кодпоинтом** (1-4 байта)
- доступны **`\\p{...}`** — Unicode-свойства (`\\p{L}`, `\\p{Cyrillic}`, `\\p{N}`)
- `\\w`/`\\d`/`\\s` — **по-прежнему ASCII по умолчанию**; для расширения нужен **`(*UCP)`** или явные `\\p{...}`

**Сравнение на одной строке `"Привет"`:**

| Шаблон | Без `u` | С `u` |
| --- | --- | --- |
| `/./` | первый **байт** `\\xD0` | первый **символ** `"П"` |
| `/^.{6}$/` | не совпадёт (12 байт) | совпадёт (6 кодпоинтов) |
| `/^\\w+$/` | не совпадёт | не совпадёт **без `(*UCP)`** |
| `/^\\p{L}+$/u` | ошибка (нет `u`) | совпадёт |

**Подводный камень безопасности:**
- невалидный UTF-8 в режиме `u` возвращает `false` и устанавливает `preg_last_error() === PREG_BAD_UTF8_ERROR`
- если **не проверяете** `=== false`, считаете «не совпало» и **молча пропускаете** битый ввод — потенциальная дыра

**Перформанс:** UTF-8-режим **немного медленнее** ASCII, но JIT нормально с ним работает.',
                'difficulty' => 4,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что такое Unicode-свойства \\p{L}, \\p{N} и зачем они нужны?',
                'answer' => '**Unicode property escapes** — классы символов на основе **категорий Unicode**, заменяющие наивные ASCII-классы вида `[A-Za-z]`.

**Основные категории букв и цифр:**

| Класс | Что включает | Пример |
| --- | --- | --- |
| `\\p{L}` | **любая буква** в любом языке | `A`, `я`, `漢`, `α` |
| `\\p{Lu}` | заглавная буква | `A`, `Я`, `Σ` |
| `\\p{Ll}` | строчная буква | `a`, `я`, `σ` |
| `\\p{N}` | **любая цифра** | `0-9`, `٠-٩`, `௦-௯` |
| `\\p{Nd}` | десятичная цифра | арабские, индийские, тайские |
| `\\p{M}` | combining mark (диакритика) | `̈`, `̃` |
| `\\p{P}` | пунктуация | `.,!?` |
| `\\p{S}` | символы | `+`, `$`, `©` |
| `\\p{Z}` | пробельные разделители | space, `\\u00A0` |
| `\\p{C}` | управляющие/невидимые | `\\n`, BOM, zero-width |

**Скрипты:** `\\p{Cyrillic}`, `\\p{Han}`, `\\p{Greek}`, `\\p{Arabic}`, `\\p{Latin}` — символы конкретной письменности.

**Инверсия:** `\\P{...}` (большая P) — дополнение к множеству.

**Где обязательно использовать вместо ASCII-классов:**

| Задача | Наивно (плохо) | Unicode-aware |
| --- | --- | --- |
| Имя пользователя | `[A-Za-z]+` | `\\p{L}+` |
| Slug | `[a-z0-9-]+` | `[\\p{Ll}\\p{Nd}-]+` |
| «Что-нибудь не пробел» | `\\S+` | `\\S+` (норм) или `[^\\p{Z}]+` |

**Обязательное условие в PHP:** работает **только** с флагом `u`. Без `u` — error compile-time.',
                'difficulty' => 4,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что значат якоря ^, $, \\A, \\z и \\b?',
                'answer' => '**Якоря — утверждения нулевой ширины** о позиции в субъекте.

| Якорь | Что обозначает |
| --- | --- |
| **`^`** | начало субъекта (или каждой строки в режиме `m`) |
| **`$`** | конец субъекта (или каждой строки в `m`); **также допускает финальный `\\n`** |
| **`\\A`** | **абсолютное** начало субъекта, **независимо от флагов** |
| **`\\z`** | **абсолютный** конец, **строго**, без `\\n` |
| **`\\Z`** | конец субъекта, **может пропустить** финальный `\\n` |
| **`\\b`** | **граница слова** — переход между `\\w` и `\\W` |
| **`\\B`** | **отсутствие** границы слова |

**Подводный камень:** `$` в **по умолчанию** совпадает прямо **перед финальным `\\n`**. Из-за этого валидация `/^\\d+$/` пропускает `"123\\n"`. Для **строгой валидации** используйте **`\\A...\\z`** — они привязаны к концам ввода независимо от флагов и переносов.

**`\\b` при `u`:** в Unicode-режиме граница слова учитывает Unicode-категории слов, что иногда даёт неожиданные совпадения на знаках пунктуации.',
                'difficulty' => 3,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Чем безопасная валидация со \\A и \\z отличается от ^ и $?',
                'answer' => '**Тонкость, которая часто становится уязвимостью.**

| Якорь | Где совпадает | Зависит от флагов |
| --- | --- | --- |
| `^` | начало субъекта **или** строки в режиме `m` | **да** |
| `$` | конец субъекта **или строки в `m`**, **+** перед финальным `\\n` | **да** |
| `\\A` | **абсолютное** начало субъекта | **нет** |
| `\\z` | **абсолютный** конец, **без** компромисса с `\\n` | **нет** |
| `\\Z` | конец субъекта, **может пропустить** финальный `\\n` | нет, но похож на `$` |

**Конкретная дыра в валидации:**

```
/^\\d+$/   на "123\\n"   → совпадает (1)  ⚠️
/\\A\\d+\\z/ на "123\\n"   → НЕ совпадает (0)
```

**Где это становится опасным:**
- бэкенд проверяет `id` через `/^\\d+$/` → пропускает `"42\\n"`
- значение уходит в логи, заголовки, файловые пути — **CRLF injection**
- в HTTP-заголовке `\\n` ломает структуру и позволяет инъекцию ответа

**Правило:** для **валидации недоверенного ввода** всегда **`\\A...\\z`** (или **`\\A...\\Z`**, если допустим единственный финальный перевод строки).

**Связанное:** `preg_match("/^\\d+$/", $input)` плюс `trim($input)` **не панацея** — после `trim()` могут остаться невидимые символы (U+200B zero-width space). Лучше явные `\\A\\z` и `\\p{...}`.',
                'difficulty' => 4,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что такое pcre.backtrack_limit и pcre.recursion_limit?',
                'answer' => '**Две INI-настройки**, защищающие процесс от зависания на патологическом regex.

| Директива | Что ограничивает | Default | При превышении |
| --- | --- | --- | --- |
| `pcre.backtrack_limit` | число **шагов бэктрекинга** на один вызов | **1 000 000** | `false` + `PREG_BACKTRACK_LIMIT_ERROR` |
| `pcre.recursion_limit` | максимальная **глубина рекурсии** при `(?R)`, `(?N)`, вложенных группах | **100 000** | `false` + `PREG_RECURSION_LIMIT_ERROR` |
| `pcre.jit` | включён ли JIT-компилятор | **1** | (см. отдельную карточку) |

**Что важно для middle/senior:**

1. **`preg_match` возвращает три значения** — `1` (найдено), `0` (не найдено), **`false`** (ошибка). Если вы пишете `if (preg_match(...))` — **ошибка молча превращается в «не нашёл»**. Это полноценная дыра: на патологическом вводе валидатор даёт `false` → код считает «не совпало с паттерном инъекции» → пропускает атаку.

2. **Правильная обработка:**

```php
$r = preg_match($pattern, $input);
if ($r === false) {
    $err = preg_last_error();
    // PREG_BACKTRACK_LIMIT_ERROR, PREG_RECURSION_LIMIT_ERROR,
    // PREG_BAD_UTF8_ERROR, PREG_JIT_STACKLIMIT_ERROR, ...
    throw new RuntimeException("regex failure: " . preg_last_error_msg());
}
```

3. **`preg_last_error_msg()`** (PHP **8.0+**) сразу даёт человекочитаемое сообщение.

4. **Поднимать лимиты ≠ решение** — патологический шаблон при `backtrack_limit = 100_000_000` просто будет висеть в 100 раз дольше. Чините **шаблон**, а не лимит.

5. JIT использует **отдельный стек** (`pcre.jit_stacklimit`), на сложных рекурсивных шаблонах его превышение даёт `PREG_JIT_STACKLIMIT_ERROR`.',
                'difficulty' => 4,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Зачем нужен JIT в PCRE и как его включают в PHP?',
                'answer' => '**PCRE JIT** — компилятор шаблона **в нативный машинный код** под архитектуру CPU (x86_64, ARM64). Один скомпилированный шаблон работает в **2-10×** быстрее интерпретации, особенно на сложных выражениях с альтернативами и квантификаторами.

**Как устроено в PHP:**

| Шаг | Что происходит |
| --- | --- |
| 1. `preg_*` принимает шаблон | парсится PCRE2-парсером в внутреннюю AST |
| 2. **Оптимизация** | constant folding, peep-hole оптимизации |
| 3. **JIT-компиляция** | если `pcre.jit=1` — генерация нативного кода |
| 4. **Кеширование** | в течение запроса один шаблон компилируется один раз |
| 5. **Матчинг** | вызов нативного кода вместо интерпретации |

**Управление:**
- **`pcre.jit`** — `1`/`0` (default `1` в современных сборках)
- **`pcre.jit_stacklimit`** — стек JIT (default 32K)
- проверка статуса: `ini_get("pcre.jit")`

**Когда JIT не помогает или мешает:**

- **рекурсивные шаблоны** `(?R)`, `(?N)` — JIT их не компилирует, фолбэк на интерпретатор
- **обратные ссылки** `\\1` — частично снижают эффективность JIT
- **сложные lookbehind** разной длины — могут не JIT-иться
- **превышение `jit_stacklimit`** — `false` + `PREG_JIT_STACKLIMIT_ERROR` (в интерпретаторе тот же шаблон отработал бы)

**Диагностика «загадочного `false`»:** временно выключите JIT через `ini_set("pcre.jit", "0")` — если поведение меняется, проблема в JIT и нужно либо увеличить `jit_stacklimit`, либо переписать шаблон.

**Производительность:** на типичном веб-приложении выигрыш от JIT — единицы процентов общего времени (regex обычно не bottleneck). Реальный выигрыш — на тяжёлой обработке текста (логи, шаблонизаторы, парсеры).',
                'difficulty' => 4,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как корректно проверять результат preg_match?',
                'answer' => '**Три исхода `preg_match`** — и почему `if (preg_match(...))` это **баг безопасности**.

| Возврат | Что значит |
| --- | --- |
| `1` | совпадение **найдено** |
| `0` | совпадение **не найдено** |
| `false` | **ошибка** (битый шаблон, лимит, UTF-8, JIT-stack) |

**Слабое сравнение `if (preg_match(...))`:**
- `false` → `false` (как «не нашёл»)
- `0` → `false` (как «не нашёл»)
- **ошибка молча превращается в «не совпало»**

**Где это становится дырой:**

```php
// ❌ Если ввод вызовет ReDoS — backtrack_limit вернёт false,
// и валидатор «пропустит» атакующий ввод как «не подходящий шаблону инъекции»
if (preg_match("/dangerous_pattern/", $userInput)) {
    throw new Exception("blocked");
}
process($userInput); // на самом деле невалидно, но проскочило
```

**Правильная проверка:**

```php
$r = preg_match($pattern, $input, $m);
if ($r === false) {
    // ошибка — отдельно логируем и решаем
    throw new RuntimeException(preg_last_error_msg()); // PHP 8.0+
}
if ($r === 1) {
    // совпало
}
// $r === 0 — не совпало
```

**Коды `preg_last_error()`:**
- `PREG_NO_ERROR` — всё ОК
- `PREG_INTERNAL_ERROR` — баг движка
- `PREG_BACKTRACK_LIMIT_ERROR` — ReDoS или поднять `pcre.backtrack_limit`
- `PREG_RECURSION_LIMIT_ERROR` — слишком глубокая рекурсия
- `PREG_BAD_UTF8_ERROR` — мусор в UTF-8 при флаге `u`
- `PREG_BAD_UTF8_OFFSET_ERROR` — оффсет внутри multi-byte
- `PREG_JIT_STACKLIMIT_ERROR` — переполнение стека JIT

**`preg_match_all`** ведёт себя так же: `0`-`N` совпадений или `false`. Та же ловушка.',
                'difficulty' => 4,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что такое «лучший трюк regex» — приём с исключением контекста?',
                'answer' => '**«Best regex trick ever»** (термин из stackoverflow-фольклора) — приём для поиска **цели вне определённого контекста**, без сложных lookbehind переменной длины.

**Идея:** шаблон вида **`ИгнорируемоеA|ИгнорируемоеB|(Цель)`**, где:
- слева **альтернативы**, которые **«съедают»** куски, в которых цель искать **не надо**, но **не захватывают**
- справа **захватывающая группа** с тем, что **нужно** вернуть
- после матчинга проходим по `$matches[1]` и **фильтруем пустые** (если совпала левая альтернатива, группа 1 пуста)

**Канонический пример — найти слово вне строковых литералов:**

```
"(?:[^"\\\\]|\\\\.)*"  |  \\b(target)\\b
└────── строки с эскейпами, не захватываем ──┘   └ цель в группе 1 ┘
```

На вводе `\'word "this is target word" another target\'`:
- первая `"..."` совпадает с левой альтернативой → группа 1 пуста, пропускаем
- последний `target` совпадает с правой → группа 1 = `"target"`

**Где применяется:**
- **CSS/JS-парсинг** — найти что-то **не в комментариях** (`/* ... */`)
- **поиск идентификаторов в коде** — пропуская строковые литералы
- **SQL** — найти таблицу, не вошедшую в кавычки
- **MarkDown** — найти `[link](url)`, **не внутри** code-fence \\`\\`\\`...\\`\\`\\`

**Почему это лучше lookbehind:**
- работает на **контекстах произвольной длины** (строка может быть в килобайт)
- быстрее: движок матчит линейно, не пытаясь «оглянуться назад»
- читаемо: каждая альтернатива — отдельный игнорируемый шаблон

**Подводный камень:** альтернативы должны быть **взаимно непересекающимися**, иначе бэктрекинг.',
                'difficulty' => 4,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как экранировать пользовательский ввод перед подстановкой в шаблон?',
                'answer' => '**Всегда пропускайте динамическую строку через `preg_quote`**, иначе её спецсимволы (`. + * ? [ ] ( ) | \\` и др.) **сломают шаблон** или приведут к **regex-инъекции**.

**Сигнатура:** **`preg_quote(string $str, ?string $delimiter = null): string`**.

**Важно про второй аргумент:**
- передавайте **разделитель** вашего шаблона (`"/"`, `"#"`, `"~"`)
- иначе пользовательская строка с **`/`** случайно «закроет» шаблон и часть ввода будет интерпретирована **как модификаторы**
- это полноценная инъекционная дыра — `pattern/i` → можно подсунуть `/...../e` (deprecated `e`-флаг)

**ReDoS:** даже после экранирования следите за **квантификаторами** в пользовательском вводе — атакующий может построить вход, на котором ваш шаблон зависнет. Подробнее — в карточке про катастрофический бэктрекинг.',
                'code_example' => '<?php
$userInput = "1.2.3-test/x";

// ❌ Опасно — точки и слэш ломают шаблон
$bad = "/^" . $userInput . "$/";   // /^1.2.3-test/x$/
preg_match($bad, "1A2B3-test"); // совпало! "." = любой

// ✅ Правильно — preg_quote с разделителем
$safe = "/^" . preg_quote($userInput, "/") . "$/";
// /^1\\.2\\.3\\-test\\/x$/
preg_match($safe, "1A2B3-test"); // 0 — точное совпадение

// Динамический поиск по подстроке
function findLike(array $haystack, string $needle): array {
    $rx = "#" . preg_quote($needle, "#") . "#i";
    return array_filter($haystack, fn($s) => preg_match($rx, $s));
}',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Какие принципы AGRA лежат в основе хорошего стиля regex?',
                'answer' => '**AGRA** — мнемоника **четырёх правил** для быстрых и предсказуемых regex.

| Буква | Принцип | Что делать |
| --- | --- | --- |
| **A** — Anchor | привязка | использовать `^`, `$`, `\\A`, `\\z`, `\\b`, чтобы не сканировать весь субъект |
| **G** — Greed | жадность | осознанно выбирать `*` / `*?` / `*+` / атомарную группу |
| **R** — Repeat | повторы | избегать `.*`, предпочитать узкие классы (`[^>]*`, `[^"\\\\]*`) |
| **A** — Atomic | атомарность | оборачивать неоднозначные части в `(?>...)`, чтобы убрать бэктрекинг |

**На примере парсинга HTML-атрибута:**

```
❌  /href=".*"/             — жадный .* + ReDoS-риск
🆗  /href=".*?"/            — ленивый, но всё ещё бэктрекит
✅  /\\Ahref="[^"]*"/        — Anchor + узкий класс, бэктрекинга нет
✅  /\\Ahref="(?>[^"]*)"/    — Atomic, гарантированно О(N)
```

**Почему именно эти четыре:**
- **Anchor** убирает поиск «по всему субъекту» → линейная сложность
- **Greed** контролирует «зачем движок идёт назад» → меньше переборов
- **Repeat** через узкий класс **исключает пересечение альтернатив** → нет ReDoS
- **Atomic** **фиксирует** результат там, где он действительно фиксирован → нет ложного бэктрекинга

**Связанные принципы:**
- **DRY** в шаблоне — `(?<digit>\\d)` плюс `\\k<digit>` вместо дублирования
- **`x`-флаг** для длинных шаблонов с комментариями
- **тесты** для регрессий — PHPUnit-сценарии с патологическими входами',
                'difficulty' => 4,
                'topic' => 'php.regex',
            ],
            [
                'category' => 'PHP',
                'question' => 'Зачем в regex используют рекурсию (?R) и (?N)?',
                'answer' => '**PCRE-рекурсия** — расширение PCRE поверх классических регулярных выражений, позволяющее **«вызвать шаблон сам себя»** или **отдельную группу**.

**Синтаксис:**

| Запись | Что делает |
| --- | --- |
| `(?R)` | вызвать **весь шаблон** рекурсивно |
| `(?N)` | вызвать **группу с номером N** |
| `(?&name)` | вызвать **именованную группу** |
| `(?P>name)` | то же, синтаксис Python-стиля |

**Канонический пример — сбалансированные скобки:**

```
/\\(  (?:  [^()]+  |  (?R)  )*  \\)/x
└─ открыть ─┘ └── что-то не-скобка ИЛИ рекурсия ──┘ └─ закрыть ─┘
```

Совпадает с `((a)b(c(d)))`, потому что внешние `(...)` через `(?R)` ловят вложенные.

**Что нельзя описать классическим regex** (Chomsky type-3) и можно через рекурсию:
- сбалансированные **парные структуры** (скобки, теги)
- **палиндромы** произвольной длины
- **вложенные** контексты (комментарии `/* ... */` с вложенными `/* */`)

**Это уже не regex в формальном смысле** — это **context-free grammar**, выраженная PCRE-расширениями.

**Подводные камни:**

| Проблема | Следствие |
| --- | --- |
| **JIT не компилирует** рекурсивные шаблоны | фолбэк на интерпретатор, выигрыша в скорости нет |
| Ограничение **`pcre.recursion_limit`** | глубокая вложенность → `false` + `PREG_RECURSION_LIMIT_ERROR` |
| **Стек рекурсии** в C-коде | реальный лимит зависит от размера стека процесса |
| **Хрупкость к правкам** | переименование группы рушит `(?&name)` |

**Правило senior-уровня:** для **разового** парсинга вложенной структуры — нормально. Для **парсера языка/протокола в проде** — берите настоящий парсер (`nikic/php-parser`, ANTLR, рукописный recursive-descent).',
                'difficulty' => 4,
                'topic' => 'php.regex',
            ],
        ];
    }
}
