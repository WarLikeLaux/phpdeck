<?php

namespace Database\Seeders\Data\Categories\Php;

class Algorithms
{
    public static function all(): array
    {
        return [
            [
                'category' => 'PHP',
                'question' => 'Как корректно перевернуть строку в PHP с поддержкой UTF-8?',
                'answer' => '**Проблема `strrev()`:**
- работает **побайтово**, не понимает UTF-8
- разрезает **2-3-4-байтовые символы** пополам — получается битый мусор
- для `"Привет"` вернёт некорректную последовательность байт

**Правильный путь (UTF-8-safe):**
1. **`mb_str_split($s, 1, "UTF-8")`** — разбить на массив **символов**, а не байт
2. **`array_reverse()`** — развернуть
3. **`implode("")`** — склеить обратно

**Сложность:** O(n) время, O(n) память (на массив-копию), где n — число **символов**.

**Подводные камни:**
- эмодзи с **модификаторами** (флаги стран, тон кожи, ZWJ-семьи: 👨‍👩‍👧) — формально несколько code points, при разворачивании рассыпаются на куски; для корректной работы нужно разбивать на **графемы** через `grapheme_str_split()` из `ext-intl`
- для **чистого ASCII** (логины, URL-safe) `strrev()` остаётся самым быстрым

**Правило:** в современных приложениях `strrev` использовать только если **гарантированно** знаете, что строка ASCII.',
                'code_example' => '<?php
echo strrev("Привет");                       // мусор: битые байты

function mb_strrev(string $s): string {
    return implode("", array_reverse(mb_str_split($s, 1, "UTF-8")));
}

echo mb_strrev("Привет");                    // "тевирП"
echo mb_strrev("hello");                     // "olleh"',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.algorithms',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что такое линейный поиск и какая у него сложность?',
                'answer' => '**Линейный поиск** — проходит массив **последовательно**, сравнивает каждый элемент с искомым и возвращает индекс при первом совпадении.

**Сложность:**
- **O(1)** — лучший случай (искомое первым)
- **O(n)** — средний и худший (искомое последним или отсутствует)
- **O(1)** по дополнительной памяти

**Когда применять:** не требует **предварительной сортировки** — работает на любых данных и любых итерируемых структурах (массивы, связные списки, генераторы).

**В PHP:**
- **`in_array($needle, $haystack, true)`** — есть ли элемент (strict — обязательно!)
- **`array_search($needle, $haystack, true)`** — индекс или `false`
- ручной `foreach`

**Когда НЕ применять:** на больших массивах с **частыми** поисками. Выгоднее один раз построить hash-map (`array_flip`) за O(n) и потом искать за **O(1)** через `isset`, либо отсортировать (O(n log n)) и искать бинарным поиском за O(log n).',
                'code_example' => '<?php
function linearSearch(array $arr, mixed $target): int|false {
    foreach ($arr as $i => $v) {
        if ($v === $target) return $i;
    }
    return false;
}

linearSearch([10, 20, 30, 40], 30); // 2

// Встроенные аналоги
in_array(30, [10, 20, 30], strict: true); // true
array_search(30, [10, 20, 30], strict: true); // 2',
                'code_language' => 'php',
                'difficulty' => 2,
                'topic' => 'php.algorithms',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как работает бинарный поиск и какие у него требования?',
                'answer' => '**Идея:** делим интервал поиска пополам — сравниваем со средним элементом и отбрасываем половину, в которой искомого точно нет.

**Сложность:**
- **O(log n)** по времени — на 1 млн элементов это **~20 шагов** вместо миллиона
- **O(1)** по памяти (итеративно) или **O(log n)** (рекурсивно — стек)

**Обязательные требования:**
1. **массив отсортирован** по тому же ключу, по которому ищем
2. **произвольный доступ** за O(1) — линейный список не подойдёт

**Подводные камни:**
- **середина**: `intdiv($l + $r, 2)` — в PHP `int` 64-битный, переполнения почти не бывает, но в C/Java пишут `$l + ($r - $l) / 2` от греха
- **условие выхода**: `while ($l <= $r)` — иначе пропустите элемент в крайней позиции
- неотсортированный массив → **зацикливание** или неверный результат, без предупреждения

**Когда применять:**
- **статичные или редко меняющиеся** справочники (load → sort → много read)
- **в БД** для этого есть **индексы** (по сути — те же бинарные деревья), `WHERE id = ?` уже быстрый

**Когда НЕ применять:** часто обновляемые данные — стоимость поддержания сортировки перевешивает выигрыш от поиска.',
                'code_example' => '<?php
function binarySearch(array $sorted, int $target): int|false {
    $l = 0; $r = count($sorted) - 1;
    while ($l <= $r) {
        $mid = intdiv($l + $r, 2);              // не $l + ($r - $l)/2 — оба OK
        if ($sorted[$mid] === $target) return $mid;
        $sorted[$mid] < $target ? $l = $mid + 1 : $r = $mid - 1;
    }
    return false;
}

binarySearch([1, 5, 8, 12, 20, 33, 47], 12); // 3',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.algorithms',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что такое пузырьковая сортировка и почему её не используют в продакшене?',
                'answer' => '**Bubble sort** на каждом проходе сравнивает **соседние элементы** и меняет местами, если они стоят не по порядку. За `n` проходов наибольшие элементы «всплывают» в конец массива (отсюда название).

**Сложность:**
- **O(n²)** по времени — средний и худший случай
- **O(n)** — лучший случай (уже отсортированный массив, с флагом раннего выхода)
- **O(1)** по памяти — работает **in-place**
- **стабильна** — равные элементы сохраняют порядок

**Почему не пишут в продакшене:**
- даже на 1000 элементов проигрывает **quicksort**/**mergesort** на порядки
- встроенная **`sort()`** (с PHP 8 стабильная) реализована эффективнее

**Зачем учить:** классический пример для собесов и учебных задач — простой код, понятная инвариантность. Реальное применение — почти нулевое.',
                'code_example' => '<?php
function bubbleSort(array $a): array {
    $n = count($a);
    for ($i = 0; $i < $n - 1; $i++) {
        $swapped = false;
        for ($j = 0; $j < $n - 1 - $i; $j++) {
            if ($a[$j] > $a[$j + 1]) {
                [$a[$j], $a[$j + 1]] = [$a[$j + 1], $a[$j]];
                $swapped = true;
            }
        }
        if (! $swapped) break; // ранний выход — массив уже отсортирован
    }
    return $a;
}

print_r(bubbleSort([3, 1, 4, 1, 5, 9, 2])); // [1,1,2,3,4,5,9]',
                'code_language' => 'php',
                'difficulty' => 2,
                'topic' => 'php.algorithms',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как работает быстрая сортировка (Quick Sort) и какая у неё сложность?',
                'answer' => '**Идея:**
1. Выбираем **pivot** (опорный элемент)
2. **Partition** — переставляем массив так, что слева — меньше pivot, справа — больше
3. Рекурсивно сортируем левую и правую части

**Сложность:**

| Случай | Время | Когда |
|---|---|---|
| **Средний** | **O(n log n)** | случайный pivot |
| **Лучший** | O(n log n) | pivot всегда делит пополам |
| **Худший** | **O(n²)** | pivot — мин/макс (например, первый элемент на уже отсортированных) |

**Память:** O(log n) на стек (in-place partition), либо O(n) в наивной реализации со списками.

**Почему на практике часто быстрее `mergesort`:**
- лучшая **локальность кеша** (работает с непрерывной памятью)
- меньше аллокаций
- меньше констант под `O(n log n)`

**Стабильность:**
- **не стабилен** в общем случае
- до PHP 8.0 встроенная `sort()` была **нестабильной**
- с **PHP 8.0** стабильность встроенных функций сортировки **гарантирована** (хоть внутри всё ещё quicksort-вариант — там добавлен tiebreaker по исходному индексу)

**Защита от худшего случая:** random pivot, median-of-three, **introsort** (переключение на heapsort при глубокой рекурсии — используется в std::sort, std::collections и др.).',
                'code_example' => '<?php
function quickSort(array $a): array {
    if (count($a) <= 1) return $a;

    $pivot = $a[0];
    $left = $right = [];
    for ($i = 1; $i < count($a); $i++) {
        $a[$i] < $pivot ? $left[] = $a[$i] : $right[] = $a[$i];
    }
    return [...quickSort($left), $pivot, ...quickSort($right)];
}

quickSort([3, 6, 1, 8, 2, 5]); // [1, 2, 3, 5, 6, 8]',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.algorithms',
            ],
            [
                'category' => 'PHP',
                'question' => 'Чем сортировка слиянием отличается от quicksort?',
                'answer' => '**Идея merge sort:**
1. Делим массив пополам
2. Рекурсивно сортируем половины
3. **Сливаем** (`merge`) отсортированные половины в один отсортированный массив за O(n)

**Сравнение с quicksort:**

| Свойство | Merge sort | Quick sort |
|---|---|---|
| **Время средний** | O(n log n) | O(n log n) |
| **Время худший** | **O(n log n)** | O(n²) |
| **Память** | **O(n)** на буфер | O(log n) на стек |
| **Стабильность** | **да** | нет |
| **Локальность кеша** | хуже (copies + merge) | лучше |
| **In-place** | нет | да |

**Когда выбирать merge sort:**
- важна **стабильность** (сортировка по нескольким полям подряд: сначала по дате, потом по имени)
- нужен **гарантированный худший случай** O(n log n) — например, для real-time/SLA
- **внешняя сортировка** на дисках/файлах (merge естественно подходит для streaming)

**Когда выбирать quick sort:**
- ограничена память (in-place)
- данные в памяти и нужна максимальная скорость
- стабильность не важна

**В PHP** встроенная `sort()` после 8.0 стабильная — для большинства задач писать своё не нужно.',
                'code_example' => '<?php
function mergeSort(array $a): array {
    $n = count($a);
    if ($n <= 1) return $a;

    $mid = intdiv($n, 2);
    $left  = mergeSort(array_slice($a, 0, $mid));
    $right = mergeSort(array_slice($a, $mid));

    $r = []; $i = $j = 0;
    while ($i < count($left) && $j < count($right)) {
        $left[$i] <= $right[$j] ? $r[] = $left[$i++] : $r[] = $right[$j++];
    }
    return array_merge($r, array_slice($left, $i), array_slice($right, $j));
}

mergeSort([5, 2, 8, 1, 9, 3]); // [1, 2, 3, 5, 8, 9]',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.algorithms',
            ],
            [
                'category' => 'PHP',
                'question' => 'Какой подход у задачи Two Sum и почему hash map даёт O(n)?',
                'answer' => '**Задача Two Sum:** найти **индексы двух** элементов массива, сумма которых равна `target`.

**Наивное решение** — два вложенных цикла, проверяющих все пары:
- **O(n²)** по времени, O(1) по памяти
- работает, но на 10⁵ элементов уже секунды

**Оптимальное решение через хэш-таблицу:**

Идём по массиву **одним проходом**. Для каждого элемента `x`:
1. ищем в таблице **дополнение** `target - x`
2. если есть — **нашли пару** (вернуть индексы)
3. если нет — кладём `x => индекс` в таблицу

**Сложность:** O(n) по времени, O(n) по памяти.

**Почему O(n):** и `isset`, и присваивание в хэш-таблицу — **амортизированно O(1)**. PHP-массив внутри как раз hash map, поэтому конструкция «ключ — значение элемента, значение — его индекс» работает идеально.

**Подводные камни:**
- если массив **отсортирован** — лучше использовать **two-pointers** за O(1) памяти
- если нужно найти **все пары**, а не одну — задача меняется, hash map уже не достаточно',
                'code_example' => '<?php
function twoSum(array $nums, int $target): ?array {
    $seen = []; // value => index
    foreach ($nums as $i => $x) {
        $need = $target - $x;
        if (isset($seen[$need])) return [$seen[$need], $i];
        $seen[$x] = $i;
    }
    return null;
}

twoSum([2, 7, 11, 15], 9);  // [0, 1]
twoSum([3, 2, 4], 6);       // [1, 2]',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.algorithms',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как проверить, что строка является палиндромом?',
                'answer' => '**Палиндром** — строка, читающаяся одинаково в обе стороны: «топот», «level», «А роза упала на лапу Азора».

**Способ 1 — сравнить с разворотом:**
- для ASCII: **`strrev($s) === $s`**
- для UTF-8: `implode(array_reverse(mb_str_split($s))) === $s`
- сложность: **O(n)** время, **O(n)** память (на копию)

**Способ 2 — два указателя (эффективнее по памяти):**
- один с начала, другой с конца, идут навстречу
- при первом несовпадении → **`false`**
- сложность: **O(n)** время, **O(1)** память

**Часто на собесах добавляют:** игнорировать регистр и неалфавитные символы. Тогда строку **нормализуют** перед сравнением:
- `mb_strtolower($s)` — нижний регистр
- `preg_replace("/[^a-zа-я0-9]/iu", "", $s)` — убрать пробелы и знаки',
                'code_example' => '<?php
// Простой способ (ASCII)
function isPalindrome(string $s): bool {
    return $s === strrev($s);
}
isPalindrome("level"); // true

// Два указателя — O(1) памяти
function isPalindromeTwoPtr(string $s): bool {
    $l = 0; $r = strlen($s) - 1;
    while ($l < $r) {
        if ($s[$l] !== $s[$r]) return false;
        $l++; $r--;
    }
    return true;
}

// С нормализацией
$s = mb_strtolower(preg_replace("/[^a-zа-я0-9]/iu", "", "А роза упала на лапу Азора"));
// "арозаупаланалапуазора"',
                'code_language' => 'php',
                'difficulty' => 2,
                'topic' => 'php.algorithms',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как решить FizzBuzz и зачем эту задачу спрашивают на собесах?',
                'answer' => '**Задача:** пройти числа от `1` до `n`. Для кратных:
- **3** — печатать `"Fizz"`
- **5** — `"Buzz"`
- **15** (и 3, и 5) — `"FizzBuzz"`
- иначе — само число

**Ключевая ловушка — порядок проверок.** Ветка «кратно 15» должна идти **первой**, иначе число `15` зайдёт в первую же подходящую ветку `% 3 === 0` и выведет `"Fizz"`. Альтернатива — `% 15 === 0` через **`AND`**: `$i % 3 === 0 && $i % 5 === 0`.

**Сложность:** **O(n)** по времени, **O(1)** по памяти.

**Зачем спрашивают:** не для алгоритма, а как **фильтр базовой грамотности** — кандидат должен без подсказки написать цикл, ветвление и не споткнуться о порядок проверок. Если ошибся — это сигнал плохой внимательности.',
                'code_example' => '<?php
function fizzBuzz(int $n): void {
    for ($i = 1; $i <= $n; $i++) {
        echo match (true) {
            $i % 15 === 0 => "FizzBuzz",   // сперва самый строгий случай
            $i % 3  === 0 => "Fizz",
            $i % 5  === 0 => "Buzz",
            default       => $i,
        };
        echo "\n";
    }
}
fizzBuzz(15);',
                'code_language' => 'php',
                'difficulty' => 2,
                'topic' => 'php.algorithms',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как считать факториал и в чём разница между рекурсивной и итеративной реализацией?',
                'answer' => '**Факториал** `n!` — произведение всех целых от `1` до `n`. По определению **`0! = 1`** и **`1! = 1`**.

**Итеративная реализация** (цикл с накопителем):
- **O(n)** по времени
- **O(1)** по памяти
- безопасна для любых `n` (в пределах разрядности `int`)

**Рекурсивная реализация** (`n! = n * (n-1)!`):
- **O(n)** по времени
- **O(n)** по **стеку вызовов** — каждый кадр занимает память
- в PHP **нет хвостовой оптимизации**, поэтому на больших `n` можно словить **переполнение стека**

**Главная техническая проблема:** факториал быстро **переполняет `int64`** — уже **`21!`** не помещается. Для больших значений — **`BCMath`** (`bcmul`) или **`GMP`** с произвольной точностью; результат будет строкой.

**Best practice:** в продакшене — **итеративно**.',
                'code_example' => '<?php
// Итеративно — рекомендуется
function factorial(int $n): int {
    $r = 1;
    for ($i = 2; $i <= $n; $i++) {
        $r *= $i;
    }
    return $r;
}
echo factorial(5); // 120

// Рекурсивно — лаконично, но стек O(n)
function factorialR(int $n): int {
    return $n <= 1 ? 1 : $n * factorialR($n - 1);
}

// Большие числа — BCMath, результат строка
echo bcmul(bcmul("1", "2"), "..."); // или цикл с bcmul',
                'code_language' => 'php',
                'difficulty' => 2,
                'topic' => 'php.algorithms',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как эффективно вычислить n-е число Фибоначчи?',
                'answer' => '**Прямая рекурсия** `fib(n) = fib(n-1) + fib(n-2)`:
- **O(2ⁿ)** по времени — одни и те же значения пересчитываются **экспоненциально** (дерево вызовов раздувается)
- `fib(40)` уже секунды, `fib(50)` — десятки минут

**Способы оптимизации:**

| Способ | Время | Память |
|---|---|---|
| **Мемоизация** (top-down DP) | O(n) | O(n) |
| **Итеративно** с двумя переменными (bottom-up DP) | **O(n)** | **O(1)** |
| **Матричное возведение** `[[1,1],[1,0]]ⁿ` | **O(log n)** | O(1) |
| **Формула Бине** через золотое сечение | O(1) | O(1) (но **float**, теряется точность от ~n=70) |

**В продакшене:**
- обычные случаи — **итеративно с двумя переменными** (O(n) время, O(1) память)
- очень большие `n` (миллионы) — **матричное возведение** за O(log n)

**Переполнение `int64`:**
- **`fib(93)` уже не помещается** в 64-битный int
- для бо́льших `n` — **`BCMath`** (`bcadd`) или **`GMP`** (`gmp_add`)
- результат становится **строкой**',
                'code_example' => '<?php
// ❌ O(2^n) — наивная рекурсия, fib(40) уже секунды
function fibSlow(int $n): int {
    return $n < 2 ? $n : fibSlow($n - 1) + fibSlow($n - 2);
}

// ✅ O(n) время, O(1) память — итеративно
function fib(int $n): int {
    $a = 0; $b = 1;
    for ($i = 0; $i < $n; $i++) {
        [$a, $b] = [$b, $a + $b];
    }
    return $a;
}

fib(10);  // 55
fib(50);  // 12586269025',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.algorithms',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как найти дубликаты в массиве за линейное время?',
                'answer' => '**Идея:** **hash map счётчиков**. Идём по массиву одним проходом, ключ — значение элемента, value — количество вхождений. После прохода берём ключи с **`count > 1`**.

**Сложность:** O(n) по времени, O(n) по памяти.

**Варианты в PHP:**

| Подход | Что делает |
|---|---|
| **`array_count_values($arr)`** + фильтр | возвращает `value => count`, дальше `array_keys(array_filter(...))` |
| **`array_diff_assoc($arr, array_unique($arr))`** | элементы из `$arr`, которых нет в уникальном — но **без `array_unique`** результат включает дубли |
| Через `isset` в `foreach` (вручную) | максимальный контроль (например, ранний выход на первом дубле) |

**Альтернативы:**

| Подход | Время | Память | Сохраняет порядок? |
|---|---|---|---|
| **Hash map** | **O(n)** | O(n) | да |
| Сортировка + проход парами | O(n log n) | O(1) (in-place) | **нет** — массив пересортирован |

**Подводные камни:**
- `array_count_values()` работает **только** со `string`/`int` элементами (не с объектами/массивами)
- для **объектов** — `SplObjectStorage` или нормализация в строки (например, `spl_object_id`)
- если важна **только сам факт** наличия дубликата (а не сколько) — лучше `count($arr) !== count(array_unique($arr))`',
                'code_example' => '<?php
$arr = [1, 2, 3, 2, 4, 1, 5];

// Способ 1 — array_count_values + filter
$dups = array_keys(array_filter(
    array_count_values($arr),
    fn(int $c) => $c > 1,
));
// [1, 2]

// Способ 2 — без счётчиков
$dups = array_unique(array_diff_assoc($arr, array_unique($arr)));
// [1, 2]',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.algorithms',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что такое техника двух указателей и где её применяют?',
                'answer' => '**Two pointers** — техника с **двумя индексами**, которые движутся по структуре одновременно. Главное достоинство — **O(1)** дополнительной памяти при O(n) времени.

**Типовые схемы:**

| Схема | Описание | Пример задачи |
|---|---|---|
| **Навстречу** (`l` слева, `r` справа) | сходятся к центру | Two Sum на отсортированном, палиндром, реверс |
| **С разной скоростью** (slow/fast) | один на 1 шаг, другой на 2 | детект цикла в списке (алгоритм Флойда), середина списка |
| **Скользящее окно** (sliding window) | оба идут в одну сторону, расстояние меняется | подстрока без повторов, max сумма подмассива длины k |

**Классический пример — Two Sum на отсортированном массиве:**
- `l` в начале, `r` в конце
- `sum < target` → двигаем `l++`
- `sum > target` → двигаем `r--`
- `sum === target` → нашли
- сложность **O(n)** время, **O(1)** память (vs hash map O(n)/O(n))

**Ограничения:**
- обычно требует **отсортированных** данных или особой структуры задачи
- не работает там, где порядок важен и нельзя его поменять (но slow/fast и sliding window сохраняют порядок)

**Где встречается в PHP:** очистка ассоциативных массивов от дублей через `array_unique` после `sort`, рукописный парсинг строк.',
                'code_example' => '<?php
// Two Sum на отсортированном массиве — O(n) время, O(1) память
function twoSumSorted(array $sorted, int $target): ?array {
    $l = 0; $r = count($sorted) - 1;
    while ($l < $r) {
        $sum = $sorted[$l] + $sorted[$r];
        if ($sum === $target) return [$l, $r];
        $sum < $target ? $l++ : $r--;
    }
    return null;
}

twoSumSorted([1, 3, 5, 8, 11, 14], 11); // [1, 3] (3 + 8)',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.algorithms',
            ],
            [
                'category' => 'PHP',
                'question' => 'Как обнаружить цикл в связном списке алгоритмом slow/fast pointer?',
                'answer' => '**Алгоритм Флойда** («черепаха и заяц»):

1. Оба указателя стартуют с **головы списка**
2. **`slow`** двигается на **1** шаг за итерацию
3. **`fast`** — на **2** шага

**Возможные исходы:**
- **`fast === null`** (или `fast->next === null`) → цикла нет, список конечный
- **`fast === slow`** → есть цикл

**Почему встречаются:** когда оба внутри цикла, расстояние между ними **уменьшается на 1** на каждом шаге (fast догоняет на 1). Через ≤ длины цикла шагов они обязательно совпадут.

**Сложность:**

| Решение | Время | Память |
|---|---|---|
| **Флойд** | O(n) | **O(1)** |
| Hash set посещённых узлов | O(n) | O(n) |

**Расширения:**
- найти **начало цикла**: после встречи переместить `slow` в голову, дальше двигать обоих на 1 — встретятся **в точке начала цикла**
- найти **длину цикла**: после встречи зафиксировать `slow` и считать шаги, пока `fast` не вернётся

**В PHP:** редко применяется напрямую (связные списки экзотика), но идея slow/fast используется в task scheduler-ах и при детекте бесконечных циклов в графах.',
                'code_example' => '<?php
class Node { public ?Node $next = null; public function __construct(public int $val) {} }

function hasCycle(?Node $head): bool {
    $slow = $head; $fast = $head;
    while ($fast !== null && $fast->next !== null) {
        $slow = $slow->next;          // 1 шаг
        $fast = $fast->next->next;    // 2 шага
        if ($slow === $fast) return true;
    }
    return false;
}',
                'code_language' => 'php',
                'difficulty' => 3,
                'topic' => 'php.algorithms',
            ],
            [
                'category' => 'PHP',
                'question' => 'Что такое динамическое программирование и когда его применять?',
                'answer' => 'Динамическое программирование — техника решения задач, у которых есть оптимальная подструктура (ответ собирается из ответов на подзадачи) и перекрывающиеся подзадачи (одни и те же подзадачи решаются многократно). Решение хранит ответы подзадач в таблице или массиве и переиспользует их, превращая экспоненциальную рекурсию в полиномиальное время. Реализуется двумя способами: top-down — рекурсия с мемоизацией, bottom-up — итеративное заполнение таблицы от базовых случаев. Классические задачи — рюкзак, наибольшая общая подпоследовательность, разбиение монетами, Фибоначчи; типичная сложность — O(n*m) по числу состояний.',
                'difficulty' => 4,
                'topic' => 'php.algorithms',
            ],
            [
                'category' => 'PHP',
                'question' => 'Когда уместно применять каждый из этих алгоритмов в реальной PHP-разработке?',
                'answer' => 'Где какие алгоритмы и структуры реально применяются в PHP-разработке:

**Бинарный поиск**
- большие **отсортированные справочники** в памяти (lookup по ID, диапазон IP)
- в БД эту роль играют **индексы** (B-Tree), `WHERE id = ?` уже бинарный поиск

**Очереди (FIFO)**
- **Laravel Queues**, RabbitMQ, Redis Streams
- фоновые задачи: отправка писем, генерация PDF, ресайз картинок, вебхуки

**Стеки (LIFO) и рекурсия**
- парсеры **вложенных структур**: HTML/XML, JSON, AST
- шаблонизаторы (Blade, Twig)
- проверка сбалансированности скобок, walk по дереву каталогов

**Сортировки**
- вывод списков по цене, дате, алфавиту
- **на больших данных** — `ORDER BY` в **СУБД** (индексы + оптимизатор)
- in-memory `usort()` оправдан только на относительно небольших коллекциях

**Хэш-таблицы (ассоциативные массивы PHP)**
- **дедупликация** (`array_unique`, `array_flip`)
- кеши в памяти (`Cache::remember`)
- частотные словари, группировка
- быстрый lookup без БД

**Графы и BFS/DFS**
- зависимости пакетов в Composer
- иерархия категорий, дерево комментариев
- права доступа (RBAC с наследованием)

**Динамическое программирование**
- редко в типичной CRUD-разработке
- встречается в биллинге, расчёте скидок, оптимизации маршрутов',
                'difficulty' => 3,
                'topic' => 'php.algorithms',
            ],
        ];
    }
}
