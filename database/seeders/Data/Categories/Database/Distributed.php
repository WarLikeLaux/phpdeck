<?php

namespace Database\Seeders\Data\Categories\Database;

class Distributed
{
    public static function all(): array
    {
        return [
            [
                'category' => 'Базы данных',
                'question' => 'Что такое шардирование (sharding) простыми словами?',
                'answer' => '**Шардирование** — горизонтальное разделение одной БД на **независимые части (шарды)**, каждая из которых живёт **на своём сервере** и хранит **только свой подмножество строк**.

**Чем отличается от репликации:**

| | **Шардирование** | **Репликация** |
|---|---|---|
| Данные | **разделяются** | **копируются** |
| Масштабирует | **запись + объём** | **чтение + HA** |
| На каждом узле | часть таблицы | полная копия |

**Плюсы:**
- **горизонтальное масштабирование** — узким местом перестаёт быть один сервер;
- параллельные `INSERT`/`UPDATE` по разным шардам;
- меньшие индексы (быстрее `B-tree` lookup внутри шарда).

**Минусы и боль:**
- **нет глобального `JOIN`** между шардами (или дорогой scatter-gather);
- **транзакции через шарды** требуют `2PC`/saga — медленно и хрупко;
- **ребалансировка** при добавлении шарда — переезд данных;
- усложняется **уникальность** (`UNIQUE` глобальный → distributed unique generator вроде `Snowflake`);
- **hot shard** — кривой ключ → 80% запросов в один шард, остальные простаивают.

**Когда брать:** только когда **репликация чтения** и **вертикальный scale-up** уже не вытягивают.',
                'difficulty' => 4,
                'topic' => 'database.distributed',
            ],
            [
                'category' => 'Базы данных',
                'question' => 'Какие есть стратегии шардирования?',
                'answer' => 'Четыре основных подхода — выбор сильно влияет на боль при росте кластера.

| Стратегия | Как работает | Плюсы | Минусы |
|---|---|---|---|
| **Range** | разбивка по **диапазону ключа** (id 1-1M → shard1, 1M-2M → shard2) | простая логика, range-сканы по одному шарду | **hot range** (последние id — все вставки в shard N) |
| **Hash** | `hash(key) % N` | **равномерное** распределение | при добавлении шарда **ребалансируется ~всё** (`% N` меняется) |
| **Consistent hashing** | хеш-кольцо, узлы и ключи на одной окружности | при ±1 узле двигается **только 1/N** данных | сложнее реализовать, нужна **виртуализация** (vnodes) от перекоса |
| **Directory-based** | внешний сервис-маппер `key → shard` | максимальная **гибкость**, можно вручную перевозить тенанта | **SPOF**, лишний hop, требует HA для каталога |

**Выбор ключа шарда (sharding key) важнее самой стратегии:**
- по `user_id` — данные пользователя на одном шарде, **транзакции дёшевы**, но крупные клиенты создают **hot shard**;
- по `tenant_id` — multi-tenant SaaS, изоляция;
- по `timestamp` — для time-series; **антипаттерн** для OLTP (все свежие записи в один шард).

**Где встречается:**
- **`Consistent hashing`** — `DynamoDB`, `Cassandra`, `Memcached`, `Riak`;
- **`Range`** — `HBase`, `BigTable`, `Vitess` (с автоматическим split);
- **`Directory`** — Slack/Figma (per-tenant routing).',
                'code_example' => '// Hash шардинг — простой rendezvous
function pickShard(string $key, int $shards): int {
    return crc32($key) % $shards;
}

// Range — лучше через таблицу-маппинг
$ranges = [
    [\'from\' => 0,        \'to\' => 1_000_000, \'shard\' => \'db1\'],
    [\'from\' => 1_000_000,\'to\' => 2_000_000, \'shard\' => \'db2\'],
];

// Consistent hashing — флёр псевдокода
// 1) Расставить N виртуальных узлов на кольце по hash(node_id + i)
// 2) hash(key) → бинпоиск по кольцу → ближайший узел по часовой
// При добавлении нового узла переезжает только 1/N ключей.',
                'code_language' => 'php',
                'difficulty' => 5,
                'topic' => 'database.distributed',
            ],
            [
                'category' => 'Базы данных',
                'question' => 'Что такое consistent hashing простыми словами?',
                'answer' => '**Consistent hashing** — способ распределять ключи между серверами так, что **добавление/удаление узла переезжает только ~1/N данных**, а не всё, как при простом `hash(key) % N`.

**Как устроено хеш-кольцо:**
1. **Узлы** и **ключи** хешируются в одно числовое пространство (например, `0..2^32`);
2. Точки расставляются на **окружности**;
3. Для каждого ключа идём по кольцу **по часовой стрелке** до первого узла — он и владелец;
4. При **добавлении** узла он «откусывает» сегмент только у соседа справа;
5. При **удалении** ключи переходят к следующему по кольцу.

**Проблема перекоса** — если узлов мало, они могут оказаться рядом на кольце → неравномерная нагрузка.
**Решение:** **виртуальные узлы (vnodes)** — каждый физический сервер раскладывают `100–500` точек на кольце. Тогда даже при ±1 узле перераспределение **гладкое**.

**Сравнение с `hash % N`:**

| | `hash % N` | **Consistent hashing** |
|---|---|---|
| Переезд при `+1 узле` | **почти всё** | **~1/N** |
| Сложность | тривиальная | средняя (нужны vnodes) |
| Балансировка | идеальная | хорошая с vnodes |

**Где применяется:**
- **`DynamoDB`** — оригинальная статья Amazon Dynamo (2007);
- **`Cassandra`** — почти точная копия идеи Dynamo;
- **`memcached`** клиенты (`ketama`);
- **CDN** — выбор edge-узла под ключ;
- **partition assignment** в распределённых системах.',
                'difficulty' => 5,
                'topic' => 'database.distributed',
            ],
            [
                'category' => 'Базы данных',
                'question' => 'Что такое репликация (replication) простыми словами?',
                'answer' => '**Репликация** — **копирование данных** одной БД на другие серверы (**реплики**). Одни и те же данные хранятся в нескольких местах, как резервная копия в реальном времени.

**Зачем нужна:**
- **отказоустойчивость** — один сервер упал, есть запасной (**failover**);
- **масштабирование чтения** — `SELECT`-запросы балансируются по репликам;
- **географическая близость** — реплика в регионе пользователя снижает latency;
- **аналитика без нагрузки** на мастер — отдельная read-реплика под BI.

**Репликация vs шардирование:**

| | **Репликация** | **Шардирование** |
|---|---|---|
| Что с данными | **дублируются** | **разделяются** |
| Масштабирует | **чтение**, отказоустойчивость | **запись**, объём |

На практике их **сочетают**: каждый шард имеет свои реплики.',
                'difficulty' => 3,
                'topic' => 'database.distributed',
            ],
            [
                'category' => 'Базы данных',
                'question' => 'Master-slave vs Master-master репликация?',
                'answer' => 'Две классические топологии — кардинально разная сложность.

| | **Master-Slave** (primary-replica) | **Master-Master** (multi-leader) |
|---|---|---|
| Запись | **один** мастер | **несколько** мастеров |
| Чтение | мастер + реплики | любая нода |
| Конфликты записи | **невозможны** | **возможны** — нужно разрешение |
| Сложность | низкая | высокая |
| Failover | **promote replica** при падении мастера | автоматический в обе стороны |
| Геораспределение | плохо (запись через океан) | хорошо |

**Master-Slave:**
- запись летит на **primary**, бинарный лог стримится на реплики;
- реплики обычно **read-only**;
- при падении мастера один из replica **повышается** (`failover`) — обычно через **`Orchestrator`**, **`Patroni`**, **`Sentinel`** или managed (RDS, Cloud SQL).

**Master-Master подводные камни:**
- **конфликты при одновременной записи** одной строки в разных мастерах → стратегии: **LWW** (last-write-wins по timestamp), **CRDT**, ручной merge;
- **auto-increment коллизии** — лечат `auto_increment_offset`/`auto_increment_increment` (узел A пишет нечётные id, B — чётные);
- **`Galera Cluster`**, **`MySQL Group Replication`**, **`PostgreSQL BDR`** — синхронные multi-master с conflict detection на коммите.

**На практике:** **master-slave с автоматическим failover** покрывает 95% задач; master-master берут только для геораспределённой записи или blue-green миграций.',
                'difficulty' => 4,
                'topic' => 'database.distributed',
            ],
            [
                'category' => 'Базы данных',
                'question' => 'Asynchronous vs Synchronous replication?',
                'answer' => 'Три режима, разный trade-off между **производительностью** и **гарантией durability**.

| | **Async** | **Semi-sync** | **Sync** |
|---|---|---|---|
| Мастер отвечает клиенту | **сразу после записи на себя** | после получения **ACK ≥ 1 реплики** | после **fsync на всех** репликах |
| Latency | минимальный | +1 RTT | +1 RTT + worst replica |
| Риск потери при падении мастера | **есть** (несколько секунд писем) | **минимальный** | **нет** |
| Если реплика лежит | мастер не страдает | таймаут → деградация в async | мастер **встаёт** |

**Asynchronous** (по умолчанию в MySQL и PostgreSQL streaming replication):
- мастер пишет в свой `redo`/`WAL`, сразу `COMMIT`-ит клиенту;
- bin-log/WAL стримится репликам **в фоне**;
- при крахе мастера до отправки **последние транзакции теряются**.

**Synchronous:**
- `Galera Cluster`, **MySQL Group Replication** (single-primary mode), **PostgreSQL synchronous_commit = on + synchronous_standby_names**;
- транзакция считается зафиксированной, **когда WAL применён на N репликах**;
- **гарантия zero data loss** ценой latency.

**Semi-sync** (MySQL **`rpl_semi_sync_master_enabled`**) — **компромисс**:
- мастер ждёт ACK от **хотя бы одной** реплики (не fsync, а получения в relay log);
- при таймауте **деградирует в async** — система не встаёт целиком.

**Правило:** для **финансов и платежей** — semi-sync минимум, лучше sync; для **аналитики и кешей** — async достаточно.',
                'code_example' => '-- PostgreSQL: настроить synchronous replication
-- postgresql.conf
-- synchronous_commit = on
-- synchronous_standby_names = \'ANY 1 (replica1, replica2)\'  -- ждём 1 из 2

-- MySQL semi-sync (master + slave plugins)
INSTALL PLUGIN rpl_semi_sync_master SONAME \'semisync_master.so\';
SET GLOBAL rpl_semi_sync_master_enabled = 1;
SET GLOBAL rpl_semi_sync_master_timeout = 1000; -- 1s до деградации в async',
                'code_language' => 'sql',
                'difficulty' => 4,
                'topic' => 'database.distributed',
            ],
            [
                'category' => 'Базы данных',
                'question' => 'Что такое replication lag и как с ним жить?',
                'answer' => '**Replication lag** — задержка между моментом записи на мастер и моментом, когда эти данные становятся видны на реплике.

**Откуда берётся:**
- **сеть** — RTT мастер↔реплика, перегруженный канал;
- **single-threaded SQL thread** на реплике (классический MySQL) — реплика не успевает применять параллельные транзакции мастера;
- **тяжёлые транзакции** — `ALTER TABLE`, длинные `UPDATE`, бэкап на реплике;
- **DDL под нагрузкой**.

**Где это бьёт по продукту — read-after-write violation:**
> Пользователь добавил комментарий → редирект на список → запрос пошёл на отставшую реплику → **«моего комментария нет»**.

**Стратегии «жить с лагом»:**
1. **Read-your-writes routing** — пользователя, который только что писал, **N секунд читать с мастера** (sticky session, redis-флаг с TTL);
2. **Read-after-write per request** — внутри одной HTTP-сессии после `INSERT`/`UPDATE` принудительно читать **с мастера**;
3. **GTID / LSN tracking** — клиент запоминает позицию записи и ждёт, пока реплика её догонит;
4. **`synchronous_commit = remote_apply`** в PG (один из самых дорогих режимов) — мастер ждёт **применения** на реплике;
5. **Semi-sync replication** — снижает риск потери, но не лаг чтения;
6. **Мониторинг и алерты** на лаг:
   - PostgreSQL: `pg_stat_replication`, `pg_last_xact_replay_timestamp()`;
   - MySQL: `SHOW REPLICA STATUS \\G` → `Seconds_Behind_Source`, или `pt-heartbeat`;
   - SLO обычно `< 1s`, алерт `> 5s`.

**В Laravel:**
- `DB::connection(\'read\')` vs `\'write\'`;
- `sticky => true` в `database.php` — после первой записи в рамках запроса все чтения идут на мастер.',
                'code_example' => '// config/database.php — sticky-режим
\'mysql\' => [
    \'read\'  => [\'host\' => [\'replica1\', \'replica2\']],
    \'write\' => [\'host\' => [\'master\']],
    \'sticky\' => true, // после write — читаем с master до конца запроса
],

// PostgreSQL: мониторить лаг в секундах
// SELECT now() - pg_last_xact_replay_timestamp() AS replica_lag;',
                'code_language' => 'php',
                'difficulty' => 4,
                'topic' => 'database.distributed',
            ],
            [
                'category' => 'Базы данных',
                'question' => 'Что такое CAP-теорема простыми словами?',
                'answer' => '**CAP-теорема** (Brewer, 2000): в условиях **сетевого разделения** (network partition) распределённая система **обязана выбрать** между двумя гарантиями.

**Три буквы:**
- **`C` — Consistency** — все ноды видят **одно и то же значение**, как будто система single-node;
- **`A` — Availability** — **каждый** запрос получает ответ (без ошибки);
- **`P` — Partition tolerance** — система **работает** при разрыве связи между нодами.

**Главный пойнт:** `P` **нельзя отключить** — сеть всегда может порваться. Реальный выбор делается **во время разделения** — между `C` и `A`:
- **`CP`** — отказать клиенту, чтобы не вернуть устаревшие данные;
- **`AP`** — ответить с потенциально устаревшими данными, но не упасть.

| Система | Поведение |
|---|---|
| **`MongoDB`** | tunable через `read concern` / `write concern`. `majority` writes → ближе к **CP**; `w:1` → **AP** |
| **`DynamoDB`** | по умолчанию **eventually consistent reads** (AP), но умеет strongly consistent reads **по запросу** |
| **`Cassandra`** | `QUORUM` → **CP**, `ONE` → **AP**, **per-query** настройка |
| **`HBase`**, **`MongoDB` (majority)** | **CP** |
| **`PostgreSQL` с sync replication** | **CP** |

**PACELC** (расширение): **else (no partition)** — выбор между **`L`atency** и **`C`onsistency**. `Cassandra` — `PA/EL`, `MongoDB majority` — `PC/EC`.

**Что важно знать:**
- **CAP-ярлыки условны** — почти все промышленные БД tunable;
- «**чистого CA**» **не существует** — это single-node системы, где партиционирования нет по определению;
- на собесе ждут понимания, что **выбор делается per-операцию**, а не на всю БД.',
                'difficulty' => 5,
                'topic' => 'database.distributed',
            ],
            [
                'category' => 'Базы данных',
                'question' => 'BASE vs ACID?',
                'answer' => 'Две **противоположные философии** дизайна систем.

**`ACID`** (реляционные БД, традиционные транзакции):
- **`A`tomicity** — транзакция либо целиком, либо вообще;
- **`C`onsistency** — после транзакции БД в валидном состоянии (constraints соблюдены);
- **`I`solation** — параллельные транзакции не мешают друг другу;
- **`D`urability** — закоммиченное сохранится после краха.

**`BASE`** (NoSQL, распределённые системы):
- **`B`asically `A`vailable** — приоритет **доступности и graceful degradation**: при отказе/перегрузке система **отвечает хоть как-то** (возможно с ослабленной консистентностью или партиальными данными), а не падает целиком;
- **`S`oft state** — состояние может **меняться без явного входного сигнала** (фоновая репликация, sync, expire);
- **`E`ventual consistency** — реплики **рано или поздно сойдутся**, но в моменте могут расходиться.

| | **ACID** | **BASE** |
|---|---|---|
| Согласованность | **строгая, всегда** | **eventual** |
| Доступность под нагрузкой | может отказать | **отвечает всегда** |
| Транзакции через сущности | да | обычно нет |
| Типичные системы | `PostgreSQL`, `MySQL InnoDB`, `Oracle` | `Cassandra`, `DynamoDB`, `Riak` |
| Когда брать | **финансы, биллинг, инвентарь** | **ленты соцсетей, аналитика, IoT** |

**На практике** — **гибрид**: PostgreSQL для денег + Cassandra для логов + Redis для счётчиков просмотров. Главное — **сознательно выбрать** под бизнес-требования, а не «потому что модно».',
                'difficulty' => 4,
                'topic' => 'database.distributed',
            ],
            [
                'category' => 'Базы данных',
                'question' => 'Что такое eventual consistency?',
                'answer' => '**Eventual consistency** (постепенная согласованность) — модель, при которой **если перестать делать обновления**, через какое-то время **все реплики придут к одному состоянию**, но **в моменте** они могут расходиться.

**Простыми словами:** «подожди немного — и все увидят одно и то же».

**Спектр уровней consistency** (от слабого к строгому):

| Модель | Гарантия |
|---|---|
| **Eventual** | реплики сойдутся **когда-нибудь** |
| **Read-your-writes** | пользователь видит **свои** записи сразу |
| **Monotonic reads** | один и тот же клиент не видит более старую версию после более новой |
| **Causal** | сохраняется **причинно-следственный** порядок |
| **Linearizable / Strong** | как single-node |

**Типичные системы и нюансы:**
- **`Cassandra`**, **`Riak`** — нативно eventual, можно подкрутить через QUORUM;
- **`DynamoDB`** — по умолчанию eventually consistent reads (**дешевле, быстрее**); **strongly consistent reads** доступны **по запросу** (× 2 RCU);
- **`MongoDB`** — по дефолту eventual, через `readConcern: "majority"` приближается к strong;
- **`Amazon S3`** — **раньше был классическим примером** eventual; с **декабря 2020** даёт **strong read-after-write consistency** для `GET`/`PUT`/`LIST` — в актуальных списках уже не упоминается.

**Боль для разработчика:**
- сразу после `INSERT` следующий `SELECT` может **не найти строку**;
- **double-spend** в распределённых системах без линейности;
- сложно отлаживать (race conditions нерегулярные).

**Где ОК:** ленты соцсетей, лайки/счётчики, telemetry, аналитика.
**Где НЕ ОК:** **деньги, остатки на складе, blocking-сценарии** (брони, билеты) — нужен `linearizable` уровень.',
                'difficulty' => 4,
                'topic' => 'database.distributed',
            ],
        ];
    }
}
